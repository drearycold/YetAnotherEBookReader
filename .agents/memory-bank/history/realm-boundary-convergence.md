# Realm Boundary Convergence History

Archived records for Realm boundary shrinkage, repository ownership, mapper extraction, DatabaseService/AppContainer Realm cleanup, and persistence boundary hardening.

Source: split from `.agents/memory-bank/activeContext.md` on 2026-07-03. Entries are preserved as historical records unless this file explicitly says otherwise.

## Recent Changes & Decisions

- **Models Final Boundary Sweep (2026-07-04, complete):** Completed the final planned `Models/` pass after Realm boundary convergence. Removed the remaining composition-root reverse dependency from Calibre value models by replacing `CalibreLibrary.plugin*WithDefault` and `CalibreBook` Goodreads display computed properties with pure configuration-driven helpers in `CalibreLibraryPluginPreferences`; Book Detail, dictionary viewer, and Goodreads auto-sync call sites now pass DSReader Helper configuration from their owning container/manager/view model. Moved the metadata Realm write queue from `AppContainer.SaveBooksMetadataRealmQueue` to `DatabaseService.metadataWriteQueue`, preserving the `saveBooksMetadata` label and QoS while removing manager/persistence dependency on an AppContainer static. Narrowed `AppContainerProtocol.searchCacheRepository` from concrete `RealmSearchCacheStore` to `SearchCacheRepository`, while keeping the concrete store internal to AppContainer and still using it for category cache. Replaced remaining production-path `print(...)` calls in Models with `OSLog`, removed unused SwiftUI imports from managers, and left `AppContainer.shared` only inside `AppContainer.swift` where the composition root owns the environment fallback. Validation: static scans passed for no `AppContainer.shared` outside `Models/App/AppContainer.swift`, no `SaveBooksMetadataRealmQueue`, no concrete `var searchCacheRepository: RealmSearchCacheStore`, no `print(...)` in Models, and no RealmSwift imports in Domain/Managers; focused `CalibreBookManagerTests`, `BookDetailViewModelTests`, `DatabaseBootstrapperTests`, `UnifiedSearchServiceTests`, `UnifiedSearchIntegrationTests`, `UnifiedCategoryServiceTests`, and `V2MigrationDependencyTests` passed; standard iOS Simulator build passed; `git diff --check` passed.
- **Models/Realm Low-Priority Residual Boundary Cleanup (2026-07-03, complete):** Closed the remaining low-risk Realm residual boundaries identified after the high-priority internal pass. Removed the old `Persistable` protocol and the domain-type `managedObject()` / `init(managedObject:)` convenience bridge from `CalibreServer`, `CalibreBook`, `BookDeviceReadingPosition`, and `BookDeviceReadingPositionHistory`; mapping tests now call the existing explicit `makeRealmObject()` / `toDomain(...)` mapper APIs directly. Renamed `Persistable.swift` to `RealmListExtensions.swift`, keeping only the Realm `List.replaceAll(...)` mapper helper and preserving iOS/Catalyst target membership. Removed UI date-formatting computed properties from `CalibreActivityLogEntry`, `BookDeviceReadingPositionRealm`, and `BookDeviceReadingPositionHistoryRealm`; `RealmActivityLogRepository.mapToUI` now owns activity-log display date strings with the same fallback text, while reading-position UI continues to use domain display helpers. Deleted the unused transient `CalibreLibrarySearchObject.loading` / `error` fields. Compatibility guardrails were preserved: `PDFOptions` schema name and `CalibreActivityLogEntry.endpoingURL` persisted field remain unchanged, with no schema version bump. Validation: old `Persistable` bridge scans passed with no matches; Realm date helper and search-cache transient scans passed; `endpoingURL` / `PDFOptions` remain only in expected Realm schema/mapper locations; focused `RealmDomainMappingTests`, `ActivityLogRepositoryTests`, `CalibreActivityLoggerTests`, `ActivityListViewModelTests`, `RealmReadingPositionRepositoryTests`, `UnifiedSearchServiceTests`, and `UnifiedSearchIntegrationTests` passed using the shared DerivedData cache; standard iOS Simulator build passed; `git diff --check` passed.
- **Models/Realm Internal Boundary Shrink (2026-07-03, complete):** Closed the high-priority internal Realm boundary leaks without changing Realm schema or repository APIs. Removed `CalibreServer.realm(in:)` from `CalibreServerRealm.swift`; the remaining test coverage now opens per-server sidecar Realms through `serverScopedRealmProvider.configuration(for:)` instead of making Realm schema depend on `AppContainer`. Moved legacy `CalibreBookRealm.migrateReadPos(...)` orchestration out of the schema object: `LegacyReadPositionRealmMappers.swift` now only parses `readPosData` into detached `BookDeviceReadingPosition` values, while `DatabaseBootstrapper.migrateLegacyReadPosData()` owns constructing the `CalibreBook`, saving positions through `ReadingPositionRepositoryProtocol`, and clearing `readPosData`. Split PDF preference responsibilities to match the Readium pattern: `PDFPreferenceValue` and PDF enums now live in Domain, PDF/UI adapter behavior (`fillColor`, dark-mode helpers, reader-engine preference conversion) lives in `Views/PDFView/PDFPreferenceAdapter.swift`, and `PDFOptions` value mapping lives in `Models/Realm/PDFOptionsRealmMappers.swift`; `PDFOptionsRealm.swift` no longer imports UIKit. Moved Calibre last-read-position entry/CFI encode-decode helpers from `BookReadingPositionRealm.swift` into `Models/Calibre/CalibreReadingPositionMappers.swift`. The later low-priority cleanup pass has since closed the `Persistable` bridge, Realm-object date display helpers, and search cache transient-field follow-ups. Validation: Realm internal boundary scans passed for `AppContainer` / `ReadingPositionRepositoryProtocol`, `import UIKit`, and legacy method names; the broad PDF/payload scan now only reports the expected `PDFPreferenceValue` references in `PDFOptionsRealmMappers.swift`; focused `MockAppContainerFactoryTests`, `DatabaseBootstrapperTests`, `RealmReadingPositionRepositoryTests`, `ReaderPreferenceRepositoryTests`, `YabrPDFViewControllerTests`, `YabrReadiumReaderViewControllerTests`, `RealmDomainMappingTests`, and `BookMetadataSyncWorkerTests` passed (75 tests); standard iOS Simulator build passed with existing warnings.
- **Outer Schema Helper Boundary Shrink (2026-07-03, complete):** Removed the remaining outer-layer direct uses of `CalibreLibraryRealm.PrimaryKey`, `CalibreBookRealm.PrimaryKey`, and `CalibreBookRealm.RatingDescription` from App/View/Calibre value-model paths. `CalibreLibrary.identity(...)`, `CalibreBook.identity(...)`, and `CalibreBook.ratingDescription(for:)` now own the pure value identity/display logic while the Realm schema helper methods delegate to those helpers to preserve existing primary-key string formats and rating text. `AppContainerProtocol.library(forServerUUID:libraryName:)` now indexes `calibreLibraries` through the pure library identity; `LibraryViewModel` resolves failed/deleted sync book titles through `BookRepositoryProtocol.getBook(library:bookId:)`; `LibraryInfoBookListViewModel` uses the pure rating display helper. Added `RealmDomainMappingTests.testRealmSchemaHelpersDelegateToDomainHelpers` to guard compatibility. Validation: outer schema helper scans passed with no matches in the targeted App/View/Calibre paths; focused `CalibreDataSplitTests`, `LibraryInfoBookListViewModelTests`, `LibraryViewModelTests`, and `RealmDomainMappingTests` passed (51 tests); standard iOS Simulator build passed; `git diff --check` passed.
- **Repository Mapper Helper Boundary Cleanup (2026-07-03, complete):** Moved the remaining low-risk repository-local mapper helpers back into the Realm mapper boundary. `BookBookmarkRealm.toValue()` / `init(value:)` and `BookHighlightRealm.toValue()` / `init(value:)` now live in `Models/Realm/AnnotationRealmMappers.swift` instead of `AnnotationRepository.swift`. `PositionIdentity` plus the `BookDeviceReadingPosition.identity` and `BookDeviceReadingPositionRealm.identity` helpers now live in `Models/Realm/ReadingPositionRealmMappers.swift` instead of `ReadingPositionRepository.swift`; `ReadingPositionRepository` now relies on type inference around the same identity grouping/deduplication logic. No repository APIs, Realm schema fields, or annotation/reading-position behavior changed. Validation: repository helper static scans passed with no mapper helper definitions left in the repository files and `RealmDomainMappingTests` still exercising `BookBookmarkRealm(value:)`, `BookHighlightRealm(value:)`, and `toValue()`; focused `RealmDomainMappingTests`, `RealmAnnotationRepositoryTests`, `RealmReadingPositionRepositoryTests`, and `ReadingSessionManagerTests` passed (43 tests); standard iOS Simulator build passed; `git diff --check` passed.
- **ReadingPositionRepository historyBook Boundary Shrink (2026-07-03, complete):** Removed `ReadingPositionRepositoryProtocol.historyBook(for:bookId:)` and the `RealmReadingPositionRepository` implementation that opened the main metadata Realm and constructed `CalibreBookRealm.PrimaryKey` from the reading-position repository. `ReadingPositionHistoryViewModel` now composes the history page through `container.bookRepository.getBook(library:bookId:)`, preserving the existing fallback to `bookManager.readingBook`; `debugPositions`, `positionHistory`, and session sidecar Realm behavior remain inside the reading-position repository. `MockReadingPositionRepository` no longer carries `historyBook` fields, and the Book Detail history test now asserts book lookup through `MockBookRepository`. Validation: `historyBook` scan passed with no matches; `ReadingPositionRepository.swift` no longer references `CalibreBookRealm` / `CalibreBookRealm.PrimaryKey`; focused `RealmReadingPositionRepositoryTests`, `BookDetailViewModelTests`, and `ReadingSessionManagerTests` passed (40 tests); standard iOS Simulator build passed; `git diff --check` passed.
- **ActivityLogRepository File Boundary Split (2026-07-03, complete):** Split activity log repository declarations out of `AnnotationRepository.swift` into `Models/Repositories/ActivityLogRepository.swift`, leaving the annotation repository file focused on bookmarks/highlights/annotations. `ActivityLogUIEntry`, `ActivityLogRepositoryProtocol`, and `RealmActivityLogRepository` keep their existing external API and UI behavior. `RealmActivityLogRepository.mapToUI` no longer constructs `CalibreBookRealm.PrimaryKey`; it resolves the `CalibreLibrary` from a `CalibreLibrarySnapshotProviding` snapshot and uses the existing `BookRepositoryProtocol.getBook(library:bookId:)` value lookup to fill book titles. The existing `container:` initializer remains for AppContainer call sites, and a narrower `librarySnapshotProvider:` initializer supports lightweight repository tests without constructing the full app container. Xcode project target membership was updated for iOS and Catalyst. Validation: activity-log split scans passed with no activity declarations left in `AnnotationRepository.swift` and no `CalibreBookRealm.PrimaryKey` in `ActivityLogRepository.swift`; focused `ActivityLogRepositoryTests`, `CalibreActivityLoggerTests`, `ActivityListViewModelTests`, and `BookDetailViewModelTests` passed (30 tests); standard iOS Simulator build passed; `git diff --check` passed.
- **CalibreServerDSReaderHelper Realm Boundary Shrink (2026-07-03, complete):** Converted outward-facing `CalibreServerDSReaderHelper` into a pure `Hashable` value type in `CalibrePluginModels.swift`, carrying `port`, `configurationData`, and `CalibreDSReaderHelperConfiguration` encode/decode behavior without Realm. Renamed the Realm embedded object implementation to `CalibreServerDSReaderHelperRealm` and kept it inside the Realm boundary with `value`/`apply`/`toValue` mapper methods; the Realm runtime class name remains `CalibreServerDSReaderHelper` to avoid a schema rename and no schema version bump was made. `ServerRepositoryProtocol` method names stayed unchanged while `RealmServerRepository` now maps between value and Realm object internally. View, Network, Manager, and AppContainer call sites continue to work with the pure value, and tests that write `CalibreServerRealm.dsreaderHelper` directly now do so through `CalibreServerDSReaderHelperRealm(value:)`. Validation: DSReader Helper Realm boundary scans passed with no app-layer `CalibreServerDSReaderHelperRealm` leakage and no new `RealmSwift` imports in Views/Network/Managers; focused `RealmServerRepositoryTests`, `RealmDomainMappingTests`, `CalibreServerManagerTests`, `DSReaderHelperConnectorTests`, and `BookDetailViewModelTests` passed (63 tests before the mapper guard, then `RealmServerRepositoryTests` passed with the added schema-class-name mapper test); standard iOS Simulator build passed; `git diff --check` passed.
- **Repositories Realm Boundary Analysis (2026-07-03, findings recorded):** Direct Realm usage inside concrete `Realm*Repository` implementations is mostly expected, but the unreasonable boundaries identified in this pass have now been closed. Completed follow-ups: `CalibreServerDSReaderHelper` is now a pure value at the repository boundary; activity log repository declarations were split out of `AnnotationRepository.swift`; `RealmActivityLogRepository.mapToUI` no longer builds `CalibreBookRealm.PrimaryKey`; `RealmReadingPositionRepository.historyBook(...)` was deleted and history book lookup now composes through `BookRepositoryProtocol`; annotation mapper convenience helpers and reading-position identity helpers moved back to `Models/Realm/*Mappers.swift`. `RealmSearchCacheStore` and `ServerScopedRealmConfigurationProviding` are acceptable Realm boundaries for now, though `RealmSearchCacheStore` remains a directory/naming inconsistency because it owns Realm cache implementation while living under `Models/Repositories`.
- **Managers Realm Boundary Shrink (2026-07-03, complete):** Removed direct Realm usage from `Models/Managers/`. `CalibreBookManager` no longer imports `RealmSwift`, opens Realm, references `CalibreBookRealm` / `CalibreLibraryRealm`, or exposes Realm bridge conversion methods; book primary-key creation, local-domain lookup, metadata entry persistence, sync-list writes, and deleted-book cleanup now go through `BookRepositoryProtocol` / `RealmBookRepository` with pure value inputs and `BookMetadataPersistenceResult` outputs. `ReadingSessionManager` now delegates default reading-history Realm queries to `ReadingPositionRepositoryProtocol.positionHistory(...)`. `CalibreLibraryManager` sync code now passes `BookMetadataSyncRecord` values instead of Realm dictionaries, and manager method names were cleaned from `*Realm` wrappers to repository-oriented save/delete names. The unused network-side `CalibreServerService.handleLibraryBookOne(library:bookRealm:...)` Realm bridge was removed. Validation: Managers Realm scans passed with no matches; focused `CalibreBookManagerTests`, `CalibreLibraryManagerTests`, `ReadingSessionManagerTests`, `RealmBookRepositoryTests`, `RealmReadingPositionRepositoryTests`, `BookDetailViewModelTests`, `UnifiedSearchServiceTests`, and `UnifiedSearchIntegrationTests` passed (91 tests); standard iOS Simulator build passed; `git diff --check` passed.
- **Domain Realm Boundary Shrink (2026-07-03, complete):** Removed direct Realm usage from `Models/Domain/`. Deleted the now-empty `BookPreference.swift` after moving the pure `BookAnnotation.PrefId(...)` helper into `CalibreCoreModels.swift`; the per-server reader sidecar `Realm.Configuration`, migration block, schema object list, and Application Support file path now live in `DefaultServerScopedRealmConfigurationProvider`. `SortCriteria` is a pure domain enum again, with `PersistableEnum` conformance moved to `Models/Realm/CalibreSearchCache.swift`. AppContainer and reader preference repository fallbacks now resolve through the default provider instead of Domain. Validation: Domain Realm scans passed with no matches; focused `DefaultServerScopedRealmConfigurationProviderTests`, `ReaderPreferenceRepositoryTests`, `RealmReadingPositionRepositoryTests`, `UnifiedSearchServiceTests`, `UnifiedSearchIntegrationTests`, and `UnifiedCategoryServiceTests` passed (51 tests); after deleting `BookPreference.swift`, focused `ReaderPreferenceRepositoryTests` passed (14 tests); standard iOS Simulator build passed with existing warnings; `git diff --check` passed.
- **LibrarySearchService Realm Boundary Shrink (2026-07-03, complete):** Removed direct `RealmSwift`, `Realm`, and `CalibreBookRealm` usage from `Models/Services/LibrarySearchService.swift`. Reused the existing `SearchCacheRepository` / `RealmSearchCacheStore` boundary instead of adding a new repository layer: the protocol now exposes value-type book lookup, local library search, and metadata-entry writes; `RealmSearchCacheStore` owns the Realm predicates, primary-key lookup, and metadata cache writes. Extracted Calibre metadata payload parsing into shared `CalibreBookMetadataValue` / `CalibreBook.applyMetadataValue(...)` in `Models/Calibre/CalibrePayloadModels.swift`; `CalibreBookRealm.applyMetadataEntry(...)` now delegates through the same value while keeping Realm-specific list/data writes in `Models/Realm/CalibreRealmMappers.swift`, and `CalibreServerService.handleLibraryBookOne(...)` no longer duplicates metadata field mapping. Added focused `UnifiedSearchIntegrationTests` coverage for local search filtering/sorting/pagination and metadata write/readback, plus `CalibreServerServiceTests` coverage that metadata refresh preserves existing format cache fields. Validation: `LibrarySearchService` Realm scans passed with no matches; duplicate metadata parser scan passed with no matches in network/Realm mapper files; focused `UnifiedSearchServiceTests`, `UnifiedSearchIntegrationTests`, `UnifiedCategoryServiceTests`, and `CalibreServerServiceTests.testGetMetadataAsyncSuccess` passed (25 tests); standard iOS Simulator build passed with existing warnings; `git diff --check` passed.
- **CalibreActivityLogger Realm Boundary Shrink (2026-07-03, complete):** Removed direct `RealmSwift`, `Realm.Configuration`, and `CalibreActivityLogEntry` usage from `Network/CalibreActivityLogger.swift`. Added pure activity-log write values in `Models/Domain/ActivityLogWriteEvent.swift`; moved Realm object population/update matching into `Models/Realm/CalibreActivityLogRealmMappers.swift`; extended `ActivityLogRepositoryProtocol` / `RealmActivityLogRepository` as the read/write/delete/cleanup boundary for activity logs. `CalibreActivityLogger` now only batches request snapshots and delegates persistence to the repository. `AppContainer` and `DatabaseBootstrapper` construct the logger from `activityLogRepository`, and the now-unused `DatabaseService.loggerConfiguration()` helper was removed. While validating, fixed `RealmActivityLogRepository` to refresh before fetches and key its thread-local Realm cache by configuration to avoid stale in-memory Realm reuse across async tests. Also corrected `UnifiedSearchIntegrationTests` to build its test `CalibreServerService` with `container.databaseService` rather than `DatabaseService.shared`. Validation: logger Realm scans passed with no matches; focused `CalibreActivityLoggerTests`, `ActivityLogRepositoryTests`, `ActivityListViewModelTests`, `DatabaseBootstrapperTests`, `UnifiedSearchServiceTests`, and `UnifiedSearchIntegrationTests` passed (24 tests); standard iOS Simulator build passed; `git diff --check` passed.
- **Book Detail Readium Nil Book Regression Fix (2026-07-03, complete):** Fixed a state mismatch where opening a book from Book Detail populated `ReadingSessionManager.readerInfo` but left `readingBook` nil or stale, which could surface as "Nil Book" when opening Readium EPUB or related reader sheets. `BookDetailViewModel` now routes read and preview-dismiss entry points through a shared helper that sets the active reading book via `CalibreBookManager`, allowing `ReadingSessionManager` to derive matching `readerInfo`. Added regression coverage to `BookDetailViewModelTests.testReadBookWhenInShelf` asserting the active reading book matches the selected detail book. Validation: focused `BookDetailViewModelTests`, `YabrReadiumReaderViewControllerTests`, `ReadiumPreferenceValueTests`, and `ReaderPreferenceRepositoryTests` passed (45 tests); `git diff --check` passed; standard iOS Simulator build passed.
- **Readium Preference Realm Boundary Shrink (2026-07-03, complete):** Split `ReadiumPreferenceValue` out of `Views/ReadiumView/ReadiumPreferenceAdapter.swift` into `Models/Domain/ReadiumPreferenceValue.swift`, leaving the adapter responsible only for Readium/UI preference conversion. Moved `ReadiumPreferenceRealm` value mapping into `Models/Realm/ReadiumPreferenceRealmMappers.swift`, so `ReadiumView` no longer directly imports `RealmSwift` or references `ReadiumPreferenceRealm`. Repository APIs and Realm schema are unchanged. Validation: ReadiumView RealmSwift scan and adapter `ReadiumPreferenceRealm` scan passed with no matches; focused `ReadiumPreferenceValueTests`, `ReaderPreferenceRepositoryTests`, `YabrReadiumReaderViewControllerTests`, and `YabrReaderSettingsViewModelTests` passed (24 tests); standard iOS Simulator build passed.
- **SwiftUI Realm Environment Injection Removal (2026-07-03, complete):** Removed the remaining `RealmSwift` import and `.environment(\.realmConfiguration, ...)` injections from `MainView.swift` and `RecentShelfView.swift`. `MainView` still gates tab rendering on database configuration readiness, but no longer binds or forwards a Realm configuration into Browse/Settings tabs; `RecentShelfView` no longer injects a per-server Realm configuration into the reading-history sheet. This is a View-layer cleanup only: persistence/repository/model RealmSwift ownership remains unchanged. Validation: target-file Realm injection scan passed with no matches; Views Realm SwiftUI dependency scan found only an unrelated commented example; `git diff --check` passed; focused `MainViewModelTests`, `RecentShelfViewModelTests`, and `BookDetailViewModelTests` passed on rerun (40 tests) after one simulator bootstrap termination; standard iOS Simulator build passed.
- **AppContainer RealmSwift Concrete Boundary Shrink Step 2 (2026-07-03, complete):** Removed direct `RealmSwift` usage from concrete `AppContainer.swift` by moving Realm default configuration installation and schema-version ownership into the persistence runtime. Added `DatabaseSchema` plus `DatabaseService` APIs for initial default, test, and production configuration installation; `AppContainer` now delegates Realm setup through those methods and uses the protocol default resolver implementations instead of referencing Realm schema primary-key helpers. Updated schema-version references in server-scoped configuration providers, test Realm environment setup, book preference configuration, and related tests to use `DatabaseSchema.version`. `DatabaseMigrator` now only builds and validates migrated configurations; `DatabaseService.prepareProductionConfiguration` installs the Realm default configuration. Validation: AppContainer RealmSwift scan passed with no matches; legacy `AppContainer.RealmSchemaVersion` scan passed with no matches; `git diff --check` passed; focused `DatabaseBootstrapperTests`, `MockAppContainerFactoryTests`, `DefaultServerScopedRealmConfigurationProviderTests`, `CalibreBookManagerTests`, `UnifiedCategoryServiceTests`, and `V2MigrationDependencyTests` passed (68 tests); standard iOS Simulator build passed.
- **UnifiedCategoryService Realm Observer Test Isolation Fix (2026-07-02, complete):** Fixed the failing `UnifiedCategoryServiceTests.testRealmSearchCacheStoreObserveCategorySummariesPublishesInitialAndUpdatedSnapshots` after the RealmSwift boundary shrink. Root cause was the test helper still using the old `DatabaseService.shared.setup(conf:)` path while `RealmSearchCacheStore` now depends on container-scoped `DatabaseService`; repeated/class-level runs could reuse category cache rows across observer tests and make the initial summary snapshot non-deterministic. The helper now keeps the class-scoped in-memory Realm identifier to avoid Realm handle exhaustion, creates the container through `MockAppContainerFactory.makeContainer(mainRealmConfiguration:testName:)`, clears category cache objects before each helper use, and constructs `RealmSearchCacheStore` from the container runtime. Validation: the failing test passed individually, full `UnifiedCategoryServiceTests` passed (9 tests).
- **AppContainer RealmSwift Protocol Boundary Shrink (2026-07-02, complete):** Removed RealmSwift from `AppContainerProtocol` by deleting the protocol-level `realm`, `realmSaveBooksMetadata`, and `realmConf` properties. `AppContainer` now owns an instance-scoped `DatabaseService` instead of binding directly to `DatabaseService.shared`, and main Realm configuration, main Realm opening, metadata Realm opening, refresh, and reset now flow through explicit `DatabaseService` methods. `DatabaseBootstrapper`, `MainView`, `RealmSearchCacheStore`, `ReadingSessionManager`, `CalibreBookManager`, and `BookDownloadManager` were migrated away from `container.realm*` access; `BookDownloadManager` no longer imports RealmSwift. `RealmSearchCacheStore` now depends on `DatabaseService` plus a narrow library snapshot provider rather than using the full container for Realm configuration. Validation: `AppContainerProtocol` Realm scan passed with no matches; production `container.realm*` scan passed with no matches; `BookDownloadManager` Realm scan passed with no matches; `git diff --check` passed; focused `DatabaseBootstrapperTests`, `MockAppContainerFactoryTests`, `CalibreBookManagerTests`, `BookDownloadManagerTests`, `UnifiedSearchServiceTests`, and `V2MigrationDependencyTests` passed (91 tests); standard iOS Simulator build passed.
- **App Layer Combine Timer/Cancellable Removal (2026-07-02, complete):** Removed the remaining App-layer Combine timer/cancellable boundary. `AppContainer` and `AppContainerProtocol` no longer import Combine, expose `probeTimer`, or carry the unused `calibreCancellables` set; `DatabaseBootstrapper` also dropped its unused Combine import. `YetAnotherEBookReaderApp` now owns reachability probing as a Swift Concurrency `Task` stored in `@State`, cancels any previous loop on re-enable/background, probes immediately on active, and then probes every 60 seconds via `Task.sleep`. Validation: App/AppProtocol/Bootstrapper scan passed with no `import Combine`, `AnyCancellable`, `Timer.publish`, `.sink`, or `.store(in:)`; `git diff --check` passed; focused `DatabaseBootstrapperTests`, `SettingsViewModelTests`, `CalibreServerManagerTests`, and `MainViewModelTests` passed (31 tests); standard iOS Simulator build passed.
- **Review Follow-up: Repository Streams and Download State Isolation (2026-07-02, complete):** Resolved review findings on the repository Combine-removal branch. Realm repository `AsyncStream` observers now install notification tokens before first emission and use Realm `.initial` notifications for initial values, with explicit Realm refresh before observation to avoid stale main-Realm snapshots after background writes. `BookDownloadManager` download state and APIs are main-actor isolated, the subscriber lock was removed, and `BookDetailViewModel` now registers its book stream synchronously during setup so metadata refreshes cannot race observer installation. Added/updated book and library repository observer regression tests for initial-plus-update delivery. Validation: focused repository/download/UI tests passed (`RealmBookRepositoryTests`, `RealmLibraryRepositoryTests`, `BookDetailViewModelTests`, `LibraryViewModelTests`, `UnifiedCategoryServiceTests`, `BookDownloadManagerTests`, `LibraryInfoBookListViewModelTests`, `RecentShelfViewModelTests`, `V2MigrationDependencyTests`); `git diff --check` passed; standard iOS Simulator build passed.
- **Metadata Sync Realm ownership rollback to repositories (2026-06-28, complete):** Simplified `BookMetadataSyncWorker` so it no longer imports Realm, manages store identity, or chooses cross-repository write transactions. `SyncJob` dropped the unused `formatKey`, `executeSync` now only calls value-type repository APIs on its serial queue, and `needUpload == true` still forces empty-remote annotation merges so local pending highlights/bookmarks are discovered for upload. `ReadingPositionRepositoryProtocol` and `AnnotationRepositoryProtocol` no longer expose `getRealm(...)` or public `in realm:` sync entry points; both Realm repositories keep Realm acquisition plus short-transaction apply helpers private again. Mock repositories and worker tests were reduced to domain-level API assertions, and a real repository worker integration test now verifies position/highlight/bookmark persistence end-to-end. Validation: `xcodebuild test -only-testing:YetAnotherEBookReaderTests/BookMetadataSyncWorkerTests -only-testing:YetAnotherEBookReaderTests/RealmReadingPositionRepositoryTests -only-testing:YetAnotherEBookReaderTests/RealmAnnotationRepositoryTests` passed (27 tests, 0 failures).
- **Metadata Sync Worker + LaunchState review-fix pass (2026-06-28, complete):** Fixed four review findings in `BookMetadataSyncWorker` and app startup bootstrap handling. `BookMetadataSyncWorker` now always runs annotation merge for `needUpload` jobs even when remote annotations are empty, removed worker-level outer Realm write transactions so repositories keep short apply transactions, and compares Realm store identity via standardized `fileURL` or matching `inMemoryIdentifier` instead of `nil == nil`. Startup gating in `YetAnotherEBookReaderApp` now only reuses `.ready` when `launchState` is already `.ready` and `AppContainer.isDatabaseReady` confirms both main + metadata Realm availability; bootstrap failures clear partial container state via `resetDatabaseBootstrapState(clearConfiguration:)`. Added focused regression coverage in `BookMetadataSyncWorkerTests` and `DatabaseBootstrapperTests`. Validation: `xcodebuild test -only-testing:YetAnotherEBookReaderTests/BookMetadataSyncWorkerTests -only-testing:YetAnotherEBookReaderTests/DatabaseBootstrapperTests` passed (13 tests, 0 failures).
- **Stage P8 & P9 (Performance Plan): Linearize Highlight and Bookmark Sync (2026-06-28, complete):** Linearized highlights and bookmarks synchronization inside `RealmAnnotationRepository`. Replaced live-results looping mutations with single-prefetch indexing and dry-run action generation in memory. Standardized remote timestamp parsing to run exactly once, resolving oldest/newest comparisons and CFI page parsing outside the database transaction block. Commits are executed in a single transaction only when actions are non-empty. Added stress tests for 500+ records and special-case verification. All 378 unit tests and 1 UI test passed.
- **Stage P3 & P4 (Performance Plan): ReadingSessionHandle and Lifecycle Migration (2026-06-27, complete):** Migrated the e-book reading session tracking to use an explicit, immutable `ReadingSessionHandle` and precise session-reuse time thresholds (60s ended, 300s active). Replaced legacy `session(start:)` / `session(end:)` with `beginSession(at:forBookId:)` and `endSession(_:at:)`. Completely eliminated the deprecated `takePrecedence` mutable flags. Fixed a shared test-environment race condition by isolating `DatabaseService` instantiations in the repository tests. Addressed and completed code review optimization requirements (captured handle before async Folio saves to avoid race conditions, validated history bookId to prevent cross-book tampering, pre-reduced duplicate highlights by unique UUID and newest timestamp while filtering invalid entries before deduplication, and pre-indexed local positions on device ID to optimize syncPositions complexity from $O(remote \times local)$ to $O(remote + local)$). All 366 unit tests and 1 UI test passed.
- **Stage P3: Optimize Highlight and Bookmark Memory Merges (2026-06-27, complete):** Optimized `syncBookmarks` and `syncHighlights` in `AnnotationRepository` to avoid O(N^2) query behavior in the merge loops. Built memory dictionaries (`localBookmarksByPos` grouped by pos, and `localHighlightsById` keyed by highlightId) from a single initial query per book, reducing the complexity to O(N) memory lookups. Hardened state tracking to bypass Realm live-query array update dependencies. Verified with unit tests, passing all 365 unit tests and 1 UI test.
- **Test Realm In-Memory Unification (2026-06-25, complete):** Six-commit refactor (`refactor/test-in-memory-realm` branch) that introduces a `ServerScopedRealmConfigurationProviding` protocol boundary, swaps the per-server sidecar Realm (reading position, reader preference, FolioReader settings) from disk-backed (`Application Support/<server-uuid>.realm`) to in-memory for tests, and migrates the full test suite to a single `MockAppContainerFactory` entry point. The pre-existing `testEndSession_logsActivity` state-pollution failure is fixed (the test now passes 3-for-3 across repeat runs in the same process). New files: `Models/Repositories/ServerScopedRealmConfigurationProviding.swift`, `DefaultServerScopedRealmConfigurationProvider.swift`, `InMemoryServerScopedRealmConfigurationProvider.swift`, `Models/Testing/TestRealmEnvironment.swift`, `TestHelpers/MockAppContainerFactory.swift`, `MockAppContainerFactoryTests.swift`. Sidecar Realm tests verify the in-memory identifier, distinct identifiers per container, same-UUID caching, and the original pollution scenario. AppContainer.init gains an optional `testRealmEnvironment` parameter. Full unit suite: 338/338 (332 + 6 new) green.
- **Test Realm Audit Fixes (2026-06-26, complete):** Two follow-up commits to the test-realm-in-memory refactor. (1) `CalibreServer.realmPerf` extension was routing every call through `AppContainer.shared` and keying the thread-local cache on `realmPerf-<uuid>`, which aliased the same `LocalServerUUID` from two different test containers onto a single cached Realm. The fix moves the lookup to a new `CalibreServer.realm(in: AppContainer)` method that includes `ObjectIdentifier(container)` in the cache key; the single call site in `Views/ShelfView/RecentShelfView.swift` is updated accordingly. (2) Several migrated test files passed literal `testName` strings to `MockAppContainerFactory.makeContainer(testName:)` (e.g. `"MainViewModelTests-${UUID().uuidString}"`), so every run shared the same in-memory Realm identifier and writes from one test method leaked into the next. 13 files are fixed across three typo variants (6 with `${...}`, 6 with `{...}`, 1 with `(...)`); all are corrected to `\(UUID().uuidString)`. New regression test `testPerContainerSidecarRealmDistinguishesContainersByConfig` covers the cache-key fix. Full unit suite: 339/339 green.
- **Database Bootstrap Error Propagation Fix (2026-06-25):** Addressed two issues from the post-Phase-4 audit. (P1) `AppContainer.initializeDatabase()` previously caught and silently swallowed bootstrap errors, allowing `YetAnotherEBookReaderApp` to close the upgrade overlay, call `enableProbeTimer()`, and send `.active` on the activity subject even when the Realm failed to open. The method now rethrows, throwing `DatabaseBootstrapError.realmConfigurationMissing` when called before `tryInitializeDatabase` populated the configuration. The `onChange(of: scenePhase)` block in `YetAnotherEBookReaderApp.swift` now wraps `initializeDatabase()` in `do/catch` and only runs `enableProbeTimer()` / `bookReaderActivitySubject.send(newScenePhase)` on success, keeping the upgrade overlay up with the error message otherwise. (P2) `DatabaseBootstrapper.bootstrap` previously used `try? Realm(...)` for the background metadata Realm and silently lost the result; `CalibreBookManager.getBooksMetadata` would then skip persistence with no log. The metadata open is now wrapped in `do/try/catch/throw DatabaseBootstrapError.metadataRealmOpenFailed(underlying:)`; `.sync` rethrow propagates the error out of the queue closure. `AppContainerProtocol.initializeDatabase()` signature changed to `throws`. `AppContainer.init(mock:)` uses `try?` so previews still tolerate the failure. `AppContainerProtocol` and `AppContainer` updates include the new `throws` signature. Added `YetAnotherEBookReaderTests/DatabaseBootstrapperTests.swift` (3 tests) covering: `testBootstrapRethrowsRealmOpenFailed` (broken config), `testInitializeDatabaseRethrowsBootstrapErrors` (broken config through the public API), and `testInitializeDatabaseThrowsWhenConfigurationMissing` (nil `realmConf`). The metadata-Realm error path is verified by code review of the same `do/try/catch/throw` pattern. All 332 unit tests pass (1 pre-existing `testEndSession_logsActivity` failure documented in AGENTS.md, unrelated to this change).
- **Phase 2: Repository Independent Testing Implemented (2026-06-24):** Added comprehensive unit test coverage for `RealmBookRepository`, `RealmAnnotationRepository`, `RealmReadingPositionRepository`, `RealmServerRepository`, and `RealmLibraryRepository` under `YetAnotherEBookReaderTests`. Solved an in-memory Realm cache key collision in `ReadingPositionRepository.getRealm(forBookId:)` by including `inMemoryIdentifier` when `fileURL` is nil. Verified that all unit tests pass (300/300) and macOS Catalyst build succeeds.
- **Views Realm Cleanup Phase 3b Implemented (2026-06-24):** Removed direct Realm category-cache observation from `LibraryInfoViewModel` and `UnifiedCategoryViewModel` by extending `CategoryCacheRepository` with `observeCategorySummaries()` and `observeCategoryCacheUpdates(categoryName:)`. `RealmSearchCacheStore` now owns the Realm `changesetPublisher` bridge for category summaries and per-category cache update notifications, while `UnifiedCategoryService` remains merge-only. `LibraryInfoViewModel` now subscribes to repository-driven summaries, and `UnifiedCategoryViewModel` now observes repository cache updates and retriggers merge through `UnifiedCategoryService`, including rebinding when the selected category changes. `MockCategoryCacheRepository` was promoted into `TestHelpers`, and coverage was added for repository observe behavior, `LibraryInfoViewModel` category summary updates, `UnifiedCategoryViewModel` dependency resolution/rebind/update flows, and invalidate-on-force-refresh behavior. Validation: 10 focused category tests passed, then full `RunAllTests` passed (274/274).
- **Views Realm Cleanup Phase 3a Implemented (2026-06-24):** Removed direct Realm usage from `Views/FolioReaderView/Providers.swift` by introducing a dedicated `FolioReaderProfileRepositoryProtocol` / `RealmFolioReaderProfileRepository` boundary plus `FolioReaderProfileValue`. `FolioReaderDelegatePreferenceProvider` no longer accepts `Realm.Configuration` or opens Realm directly; it now holds runtime `values` only and delegates default-profile seeding, list/load/save/remove operations to the repository. `ModelData` now exposes `folioReaderProfileRepository`, and `FolioReaderPreferenceRealm` gained value-type bridge methods (`toValue(defaults:)`, `apply(_)`). Validation: 7 focused Folio/profile tests passed via Xcode MCP `RunSomeTests`, then full `RunAllTests` passed (267/267).
- **Views Realm Cleanup Phase 2 Remaining Implemented (2026-06-24):** Removed direct Realm usage from `BookDetailViewModel`, `ActivityListViewModel`, `ReadingPositionViewModel`, and `LibraryViewModel` by extending repository boundaries with value-type publishers and query helpers. Added `ActivityLogRepositoryProtocol` / `RealmActivityLogRepository`, `BookRepositoryProtocol.observeBook(id:)`, `LibraryRepositoryProtocol.getLibrary(id:)` / `observeLibrary(id:)` / `updateLibraryFlags(id:discoverable:autoUpdate:)`, and `ReadingPositionRepositoryProtocol.debugPositions(forBookId:)` / `historyBook(for:bookId:)`. Views-layer `RealmSwift` imports are now down to 4 files: `LibraryInfoViewModel.swift`, `UnifiedCategoryViewModel.swift`, `Providers.swift`, and `ReadiumPreferenceAdapter.swift`. Validation: focused ViewModel tests passed (29/29), then full `xcodebuild test -project YetAnotherEBookReader.xcodeproj -scheme YetAnotherEBookReader -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/YabrDerivedData` passed (261 unit tests + 1 UI test).
- **Views Realm Cleanup Phase 1 Implemented (2026-06-23):** Removed `RealmSwift` imports from `MainViewModel.swift`, `SettingsViewModel.swift`, `ServerViewModel.swift`, and `YabrEBookReader.swift`. Added `ModelData.isDatabaseReady`, `ModelData.refreshDatabase()`, and removed the dead `realm:` parameter from `updateServerDSReaderHelper` across `ModelData`, `CalibreServerManager`, and all call sites. Added `ReaderPreferenceRepositoryProtocol` / `RealmReaderPreferenceRepository` so `YabrEBookReader` no longer directly opens preference Realms or writes `ReadiumPreferenceRealm` / `PDFOptions` / `FolioReaderPreferenceRealm`. Added regression coverage in `MainViewModelTests`, `SettingsViewModelTests`, and new `ReaderPreferenceRepositoryTests`. Validation: focused tests passed, then full `xcodebuild test -project YetAnotherEBookReader.xcodeproj -scheme YetAnotherEBookReader -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17'` passed (235 unit tests + 1 UI test).
- **Phase 1d: Mock Infrastructure Enhancements (2026-06-23):** Centralized mock infrastructure under a new navigator group and directory `YetAnotherEBookReaderTests/TestHelpers/`. Created reusable doubles for repositories (`MockServerRepository`, `MockLibraryRepository`, `MockBookRepository`, `MockReadingPositionRepository`, `MockAnnotationRepository`), managers (`MockServerManager`, `MockLibraryManager`, `MockBookManager`), in-memory Realm DB bootstrappers (`MockDatabaseService`), and a factory builder for domain types (`TestFixtures`). Centralized `MockURLProtocol` into `TestHelpers/` and removed its duplicate inline definition in `UnifiedSearchIntegrationTests.swift`. All 227 unit tests compile and pass successfully, and Catalyst build succeeds.
- **P2/A27 Stage A27-S6 Remote Payload Boundary Cleanup Implemented (2026-06-22):** Deleted unused legacy `toCalibreBookAnnotationHighlightEntry()` and `toCalibreBookAnnotationBookmarkEntry()` extensions on `BookHighlightRealm` and `BookBookmarkRealm` from `BookAnnotationRealm.swift`. The codebase now uses domain-value mappers on `BookHighlight` and `BookBookmark` for Calibre API payload conversion, successfully separating Realm models from remote sync details. All 174 unit tests compiled and passed successfully.
- **P2/A27 Stage A27-S5 Standardize Realm Write Helpers Implemented (2026-06-22):** Introduced a generic `replaceAll(_:)` extension on Realm `List`. Defined in-place `applyDomain(_:)` updates on all 7 Realm model entities to prevent duplicate mapping code, and refactored `makeRealmObject()` to delegate to `applyDomain(_:)`. Refactored `RealmAnnotationRepository.saveBookmark(_:)` to perform in-place updates on existing managed bookmarks using `applyDomain(_:)`. Guarded primary keys and identity fields against modification when objects are already managed. Added test coverage in `RealmDomainMappingTests` and successfully ran all 174 unit tests.
- **P2/A27 Stage A27-S4 Reduce Manager, Service, And ViewModel Conversion Bridges Implemented (2026-06-22):** Refactored calls to `CalibreBook(managedObject:library:)` in `LibrarySearchService` and `RealmSearchCacheStore` to use `toDomain(library:)`, refactored `CalibreBookManager.convert(library:bookRealm:)` to call the mapper directly, and refactored direct Realm-to-domain mapping in `ReadingSessionManager` and `ReadingPositionViewModel` to call the new `.toDomain()` mapper methods directly, removing duplicate conversion shims outside the persistence boundary. All 173 unit tests compiled and passed successfully.
- **P2/A27 Stage A27-S3 Centralize Repository Callers Implemented (2026-06-22):** Refactored all 5 core persistence repositories (`RealmServerRepository`, `RealmLibraryRepository`, `RealmBookRepository`, `RealmAnnotationRepository`, and `RealmReadingPositionRepository`) to directly call the newly extracted explicit domain mappers (`toDomain()` and `makeRealmObject()`) instead of using legacy manual mapping blocks or the compatibility facades (`toValue()`, `init(value:)`). Open database connections and transaction management were preserved within repository scopes. All 173 unit tests compiled and passed successfully.
- **P2/A27 Stage A27-S2 Extract Explicit Domain Mappers Implemented (2026-06-22):** Extracted explicit Realm-to-domain and domain-to-Realm object mapping methods into three newly created dedicated mapper files (`CalibreRealmMappers.swift`, `ReadingPositionRealmMappers.swift`, and `AnnotationRealmMappers.swift`) under `Models/Realm/`. Registered the files to iOS and Mac Catalyst targets. Refactored the legacy mapping extensions (`Persistable`, `toValue()`, `init(value:)`) in model and repository files to delegate directly to the new mappers to preserve 100% backward compatibility. Extended the `RealmDomainMappingTests` suite to test the new mapper methods directly. All 173 unit tests compiled and passed successfully.
- **P2/A27 Realm Value Conversion Plan Captured (2026-06-22):** Added `.agents/memory-bank/p2-a27-realm-value-conversion-plan.md` after auditing current Realm/domain conversion sites. The plan recommends a conservative staged path: first add golden mapper tests for server/library/book/annotation/reading-position round trips, then extract explicit mapper helpers under `Models/Realm/`, migrate repositories first, reduce manager/service/view-model conversion bridges, standardize write-side `makeRealmObject`/`applyDomain` helpers, and finally clean up remote annotation payload boundaries plus documentation guardrails. No schema change, migration bump, macro/codegen pass, or reader-adapter merge is recommended for the first implementation.
- **P2/A12+A24 RealmModel Split + Readium Decoupling Implemented & Crash Resolved (2026-06-19):** Split the massive monolithic `RealmModel.swift` file into individual, focused Realm schema files under `YetAnotherEBookReader/Models/Realm/`. Moved `FolioReaderPreferenceRealm` from views to models, and decoupled the `Models/` layer from Readium Navigator/Shared dependencies by extracting mapping extensions of `ReadiumPreferenceRealm` into the view/adapter layer `Views/ReadiumView/ReadiumPreferenceAdapter.swift`. Resolved an `EXC_BAD_ACCESS` crash in `swift_retain` when copying `Realm.Configuration` on concurrent background queues by resolving the configuration dynamically in `RealmSearchCacheStore.getRealm()` and stripping the `migrationBlock` and `shouldCompactOnLaunch` closures prior to opening the database. Verified that the app builds and all 90 unit tests + 1 UI test pass.
- **P2/A12+A24 RealmModel Split + Readium Decoupling Plan Captured (2026-06-18):** Added `.agents/memory-bank/p2-a12-a24-realmmodel-readium-plan.md` after analyzing the 1357-line `Models/RealmModel.swift`, `BookPreference.swift`, Readium reader adapters, and FolioReader preference storage. The recommended sequence is: first perform a zero-schema-change split into focused Realm files under `Models/Realm/`; move `FolioReaderPreferenceRealm` out of `Views/FolioReaderView/Providers.swift` into the model/persistence layer; then move `ReadiumPreferenceRealm` conversions to/from `EPUBPreferences`, `PDFPreferences`, `EPUBSettings`, and `PDFSettings` into a Readium adapter file so `Models/` no longer imports `ReadiumNavigator` or `ReadiumShared`. Preserve Realm class names, persisted property names, primary keys, `objectTypes`, and migration references.
- **P2/A23 DatabaseService Force Unwrap Fixed (2026-06-18):** Converted `DatabaseService.realmConf` from `Realm.Configuration!` to `Realm.Configuration?` and `DatabaseService.realm` from `Realm!` to `Realm?`. Changed `setup()` from `try! Realm(configuration: conf)` to a `do/catch` that logs via `OSLog.Logger` and sets `realm = nil` on failure. All 5 callers (`CalibreBookManager`, `RealmServerRepository`, `RealmLibraryRepository`, `RealmBookRepository`, `RealmAnnotationRepository`) already had defensive `getRealm() -> Realm?` patterns that return nil on the main thread if `databaseService.realm` is nil — no changes needed there. The only caller code change was in `LibrarySearchService.getRealm()`, which passed `service.database.realmConf` directly to `Realm(configuration:)`; added a `guard let` unwrap with `URLError(.cannotConnectToHost)` fallback. Verified with `xcodebuild build` and full `xcodebuild test` (90 unit tests + 1 UI test green).
- **P2/A22 CalibreSearchCache Deprecated Properties Removed (2026-06-18):** Removed 4 deprecated `@Persisted` properties from `CalibreLibrarySearchObject` in `CalibreSearchCache.swift`: `generation`, `totalNumber`, `bookIds`, and `books: List<CalibreBookRealm>`. All were marked `@available(*, deprecated, message: "use sources")` and had zero callers after P1e deleted `CalibreBrowser.swift`. The modern `RealmSearchCacheStore` only accesses `searchObj.sources`, `searchObj.libraryId`, `searchObj.search`, `searchObj.sortBy`, `searchObj.sortAsc`, and `searchObj.filters`. Bumped Realm schema version from 139 to 140 by updating `ModelData.RealmSchemaVersion`, `CFBundleVersion` in both iOS and macOS Info.plist files (since `tryInitializeDatabase` derives the schema version from the build number), and added an `oldSchemaVersion < 140` migration block entry. Realm automatically drops the removed columns during migration. Verified with `xcodebuild build` and full `xcodebuild test` (90 unit tests + 1 UI test green).
- **P2/A07 Providers.swift Dead Code Removed (2026-06-18):** Deleted 417 lines of unreachable/deprecated code from `Providers.swift` and `RealmModel.swift`. Removed: `FolioReaderRealmPreferenceProvider` (176 lines, replaced by `FolioReaderDelegatePreferenceProvider`, zero callers), `FolioReaderHighlightRealm` (68 lines, `@available(*, deprecated)`, not in Realm schema `objectTypes`), `FolioReaderYabrHighlightProvider` (51 lines, replaced by `FolioReaderDelegateHighlightProvider`, zero callers), `FolioReaderReadPositionRealm` (88 lines, `@available(*, deprecated)`, not in Realm schema `objectTypes`), and `extension FolioReaderHighlightRealm { toBookHighlightRealm }` in `RealmModel.swift` (30 lines, zero callers). `Providers.swift` shrank from 1095 to 708 lines, `RealmModel.swift` from 1387 to 1357. No Realm schema change needed since the deprecated Realm Object classes were already excluded from `objectTypes`. Verified with `xcodebuild build` and full `xcodebuild test` (90 unit tests + 1 UI test green).
- **Realm ReadingPosition Thread Crash Fixed (2026-06-17):** Resolved `Realm accessed from incorrect thread` in the `recent-shelf-updater` queue and in `UnifiedSearchServiceTests.testLimitExpansionIsolatesCriteria()`. Root cause was `RealmReadingPositionRepository` reusing `server.realmPerf` / `databaseService.realm` across threads. The repository now resolves a `Realm.Configuration` and opens or caches a Realm on the current thread. Added `ReadingPositionRepositoryThreadingTests` to verify main-thread writes plus background-queue reads. Full validation now passes with 49 unit tests + 1 UI test.
- **P1c A14+A15 Confirmation (2026-06-17):** Reader theme/preferences abstraction (A15) is implemented through `ReaderEnginePreferences` and `ReaderEngineController.applyPreferences(_:)` across PDF, Readium, and FolioReader, with `YabrEBookReader.Coordinator` persisting engine-neutral preference updates back into each engine's storage model. Highlight/annotation abstraction (A14) is implemented for the shared model/storage path through `ReaderEngineHighlight`, `BookHighlight`, and `AnnotationRepository`; PDF and FolioReader also restore/apply highlights through their engine adapters. Caveat: `YabrReadiumReaderViewController.applyHighlights(_:)` is currently a protocol-complete stub, so Readium highlight UI rendering is not yet implemented.
- **AnnotationRepository (Milestone P0-3c):** Introduced `BookBookmark` and `BookHighlight` value types and `RealmAnnotationRepository` to decouple bookmarks and highlights from direct `RealmSwift` database logic. Ported Calibre annotation sync logic from `CalibreData.swift` into the repository.
- **UI Adapters Decoupling:** Refactored PDF adapter `YabrEBookReaderMetaSource.swift` and FolioReader/EPUB adapter `Providers.swift` to consume decoupled value types `BookBookmark` and `BookHighlight` instead of their Realm model counterparts, keeping UI adapters and rendering pipelines decoupled from Realm entities. Added Calibre serialization extensions directly to the value types.
- **CalibreBookManager getRealm() Optimization:** Optimized the `getRealm()` helper in `CalibreBookManager.swift` to check `Thread.isMainThread` and return the cached `databaseService.realm` directly, preventing expensive Realm instance allocation on the main thread and ensuring UI reactivity.
- **ServerRepository and LibraryRepository (Milestone P0-3b):** Decoupled `CalibreServerManager` and `CalibreLibraryManager` completely from Realm database logic. Created `RealmServerRepository` and `RealmLibraryRepository` to isolate CRUD operations for Calibre servers and libraries. Extracted all direct database references from the managers, resulting in the total removal of `import RealmSwift` from both managers.
- **Realm Thread-Safety Resolution (CalibreBookManager):** Resolved the fatal Realm thread verification crash by replacing direct main-thread `databaseService.realm` references with a thread-safe `getRealm()` helper, ensuring background thread queries dynamically initialize thread-safe Realm instances. Annotated `getBooksMetadata(request:)` with `@MainActor` to ensure UI state updates are published on the main thread.

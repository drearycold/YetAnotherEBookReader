# Active Context

## Current Focus
The current branch is `fix/library-book-list`. This is a fresh project context after archiving the completed Realm boundary convergence handoff records on 2026-07-04.

Keep this file focused on the active branch and the few current project notes a future agent needs by default. Full historical records live under `.agents/memory-bank/history/`.

## Current Branch Projects
Use this section for the new project. Archive completed entries into the matching history file before merge.

- **Header Category Multi-Select Union (2026-07-05, complete):** Header-launched category detail now supports draft multi-select for values in the same category. In header flow, item rows toggle a local draft selection with a checkmark and only apply when the toolbar `Done` button is tapped; direct back navigation cancels the draft. `LibraryInfoView.ViewModel` gained `setFilterCategoryValues` and `applyCategoryValuesSelection`, preserving other category filters and current library scope for header flow while root Browse by Category keeps the existing single-select `NavigationLink` behavior. Same-category values continue to be treated as OR/union by the existing Calibre query builder and Realm local search predicates, while cross-category filters remain AND. Active category chips also have a `Clear All` action that clears all category filters, preserves the current library scope, and restarts the search. Added coverage for batch filter setting/chips, clear-all behavior, header draft multi-select structure, Calibre query OR/AND construction, and local Realm same-category OR search. Validation: `git diff --check` passed; focused `LibraryInfoBookListViewModelTests` + targeted `CalibreServerServiceTests` + targeted `UnifiedSearchIntegrationTests` passed (26 tests); standard iOS Simulator build passed with existing warnings.
- **Single-Library Category Scope (2026-07-05, complete):** Header-launched category browsing from a single-library Browse book list now scopes both the category menu and category detail values to the current library. `CategoryCacheRepository` gained scoped summary fetching (`libraryIds` empty means global), with Realm and mock implementations aggregating only matching library cache objects. `LibraryInfoView.ViewModel.fetchAvailableCategories` and its observer now re-fetch summaries using the current `filterCriteriaLibraries`; All Books clears scope and single-library entry refreshes after inserting the library id. `UnifiedCategoryService.mergeCategory`, `UnifiedCategoryViewModel.mergeCategory`, cache-update retriggers, and `forceRefreshCategory` now accept/reuse the same library scope, so header detail lists and refreshes stay single-library while root Browse by Category remains global. Added coverage for scoped Realm summaries, scoped category merge, scoped ViewModel retrigger/refresh, and header/category source structure. Validation: `git diff --check` passed; focused `LibraryInfoBookListViewModelTests` + `UnifiedCategoryServiceTests` + `V2MigrationDependencyTests` passed (68 tests); standard iOS Simulator build passed with existing warnings.
- **Browse Header Filter Menu And Active Category Chips (2026-07-05, complete):** Fixed the Browse book-list filter button semantics after the previous menu-only active-filter approach made All Books/single-library pages appear disabled. The header filter button is now a category entry menu driven by `availableCategoryMenuItems`; active category filters render as removable chips below the search field via `visibleFilterItems`. Header-launched category browsing uses `CategoryDetailView(..., preservesLibraryScope: true)` so choosing a category item from a single-library list preserves that library scope, applies the filter, and clears `headerCategorySelected` to return to the existing book list instead of pushing a nested book-list route; root Browse by Category keeps its original `NavigationLink(tag:selection:)` flow. A one-shot `preserveFilterCriteriaOnNextBookListAppear` flag prevents the original book-list `onAppear` from clearing the just-applied header category filter, which keeps chips visible after the pop. Category filter mutation and category sort selection are centralized in `LibraryInfoView.ViewModel` via copy-on-write APIs. Added `LibraryInfoBookListViewModelTests` coverage for category menu availability, active chips, scope-preserving header category selection, header category selection returning to the existing book list, and the one-shot preserve flag. Validation: `git diff --check` passed; focused `LibraryInfoBookListViewModelTests` passed (23 tests); standard iOS Simulator build passed with existing warnings.
- **Browse Search Header Layout Fix (2026-07-05, complete):** Fixed the Browse book-list search header so the clear X button is hidden when the search field is empty and the title/author field expands across the available width beside the history button. `LibraryInfoBookListHeader` now uses a single inline `HStack` instead of a `ZStack` overlay, keeps history/filter actions unchanged, and conditionally renders the clear button only for non-empty draft search text. Added `LibraryInfoBookListViewModelTests.testBrowseSearchHeaderUsesInlineLayoutAndHidesClearButtonWhenEmpty`. Validation: `git diff --check` passed; focused `LibraryInfoBookListViewModelTests` passed (15 tests); standard iOS Simulator build passed with existing warnings.
- **Browse/Settings System Chrome Fix (2026-07-04, complete):** Browse and Settings now override app chrome back to system navigation/tab bar appearances while Recent and Discover keep the wood shelf chrome. Added `YabrAppChromeStyle` and a SwiftUI/UIKit appearance applicator. Root-wide runtime updates were too broad and made Browse flash wood during Browse -> shelf transitions; applicator-local updates were too narrow and left Browse/Settings wood. The final runtime path finds the nearest `UITabBarController`, verifies the applicator is inside the selected tab subtree, then updates only that selected subtree's navigation bars plus the shared tab bar; startup only seeds UIAppearance defaults. Validation: `git diff --check` passed; standard iOS Simulator build passed with existing warnings.
- **Library Book List Refresh Pagination Fix (2026-07-04, complete):** Fixed a browse/search list cache bug where server-side book additions could update `totalNumber` while leaving newly inserted sorted entries invisible and duplicating old tail entries. `LibrarySearchService.searchAndFetchMetadata` now rebuilds from `offset=0` for force refreshes, missing/stale cache sources, and detected unstable incremental pages (`total_num` change or overlapping returned IDs). Per-library source `bookIds` are deduplicated while preserving order before metadata resolution and cache save. Added `UnifiedSearchServiceTests` regressions for force refresh offset reset and overlap-triggered rebuild after server insert.
- **Library Search Server Mutation Coverage (2026-07-04, complete):** Expanded `UnifiedSearchServiceTests` service-layer coverage for server-side delete (`total_num` drops), same-total reorder/metadata mutation (overlap-triggered rebuild), stale-generation metadata refresh, and duplicate server ID de-duplication. Validation: full `UnifiedSearchServiceTests` passed (15 tests); `UnifiedSearchIntegrationTests` + `UnifiedSearchMergeServiceTests` passed (14 tests); `git diff --check` passed.
- **Library Book Detail Navigation Selection Fix (2026-07-04, complete):** Root cause: Browse rows used `NavigationLink(tag:selection:)` bound to `CalibreBookManager.selectedBookId`, but that manager property is not the SwiftUI-observed navigation source for the list. Taps could write the selected ID without invalidating the navigation view immediately, so detail presentation appeared only after unrelated view refreshes such as tab switching, and stale selection could block re-entry after returning. Fix keeps Browse detail as a normal navigation-stack push and removes selection-driven row navigation entirely: `LibraryInfoBookListContent` now uses `NavigationLink(destination:)` for shelf-backed books, `LibraryInfoBookListView` has no sheet/local presentation state, and the destination only syncs `bookManager.selectedBookId` in `onAppear` for existing downstream consumers. Added `LibraryInfoBookListViewModelTests.testBrowseBookRowsUseDirectNavigationLinkInsteadOfSelectionDrivenNavigation` as a structure-level regression guard. Added UI-test-only mock launch data (`--ui-testing-mock-library`) and `YetAnotherEBookReaderUITests.testBrowseBookDetailOpensImmediatelyAndCanReopenAfterBack`, covering Browse -> UI Test Library -> first detail open -> back -> reopen. Validation: `git diff --check` passed; `LibraryInfoBookListViewModelTests` passed (14 tests); focused UI test result passed (1 test, 0 failures via xcresult metrics); standard iOS Simulator build passed with existing warnings.

## Pre-Merge Archive Rule
- Before merging a branch, archive completed branch work from `Current Branch Projects` into the matching `.agents/memory-bank/history/` project file.
- Keep `activeContext.md` as a short entrypoint: current focus, active branch projects, constraints, and links to history.
- Prefer project boundaries over dates or arbitrary entry counts when moving records.

## History Index
- [Realm Boundary Convergence](history/realm-boundary-convergence.md): Realm boundary shrinkage, repository ownership, mapper extraction, and persistence cleanup.
- [AppContainer And Concurrency Modernization](history/app-container-concurrency-modernization.md): AppContainer demotion, manager async streams, Combine removal, and concurrency hardening.
- [Shelf Modernization](history/shelf-modernization.md): Recent/Discover shelf UI, YabrShelfDataModel, shelf bootstrap, and shelf Swift Concurrency migration.
- [ModelData Elimination](history/modeldata-elimination.md): ModelData reduction, AppContainer introduction, protocol migration, and final deletion.
- [Reader Modernization](history/reader-modernization.md): Readium, FolioReader, PDF reader, reading position, highlights, preferences, and Book Detail reader work.
- [Network, Search, And Cache Modernization](history/network-search-cache-modernization.md): Calibre networking, unified search/category, cache behavior, downloads, and network tests.
- [Legacy Completed Tasks And Milestones](history/legacy-completed-milestones.md): Old completed checklists and cross-cutting historical context.

## Active Constraints
- **Do NOT** introduce CocoaPods or modify workspace files; the project relies entirely on Swift Package Manager.
- **Decoupling Goal:** Views should minimize direct dependency on `ModelData` for network operations; logic should reside in dedicated ViewModels.

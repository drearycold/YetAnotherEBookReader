# Active Context

## Current Focus
The primary focus is the modernization of the EBook Reader Architecture. Reading-position persistence now has an explicit book/server boundary, and shelf bootstrap completion is deterministic. All 392 unit tests and 1 UI test pass successfully; the Mac Catalyst build also succeeds.

## Recent Changes & Decisions
- **Recent Shelf Reader Presentation Regression Fix (2026-07-03, complete):** Fixed a regression where tapping a readable Recent shelf cover set the session reader presentation flag but did not immediately open the reader until another `MainViewModel` published change, such as tab switching. Root cause was the `AppContainer` observable removal: `ReadingSessionManager` state changes no longer reached `MainView` through container `objectWillChange`, while `MainView.fullScreenCover` was bound to a non-published computed property. `MainViewModel` now owns a published reader presentation flag, mirrors `ReadingSessionManager.presentingReaderSnapshots()`, and syncs dismissals back to the session manager. Added regression coverage for Recent shelf tap presentation and dismissal synchronization. Validation: focused `MainViewModelTests` and `RecentShelfViewModelTests` passed (18 tests); `git diff --check` passed; standard iOS Simulator build passed.
- **Review Follow-up: Settings Server Status Refresh and Async Broadcaster Registration (2026-07-03, complete):** Fixed two review findings from the AppContainer/manager async migration. `ManagerAsyncBroadcaster` now registers continuations before yielding the initial value, closing a small subscription race. `SettingsViewModel` now consumes `CalibreServerManager.serverInfoStagingSnapshots()` into published view-model state, so Settings server refresh/probe status and reachability row text update even though `AppContainer` is no longer an `ObservableObject`. Added focused regression coverage for Settings server-info snapshots and broadcaster initial/subsequent delivery. Validation: focused `SettingsViewModelTests` and `CalibreServerManagerTests` passed (21 tests); `git diff --check` passed; standard iOS Simulator build passed.
- **SwiftUI Realm Environment Injection Removal (2026-07-03, complete):** Removed the remaining `RealmSwift` import and `.environment(\.realmConfiguration, ...)` injections from `MainView.swift` and `RecentShelfView.swift`. `MainView` still gates tab rendering on database configuration readiness, but no longer binds or forwards a Realm configuration into Browse/Settings tabs; `RecentShelfView` no longer injects a per-server Realm configuration into the reading-history sheet. This is a View-layer cleanup only: persistence/repository/model RealmSwift ownership remains unchanged. Validation: target-file Realm injection scan passed with no matches; Views Realm SwiftUI dependency scan found only an unrelated commented example; `git diff --check` passed; focused `MainViewModelTests`, `RecentShelfViewModelTests`, and `BookDetailViewModelTests` passed on rerun (40 tests) after one simulator bootstrap termination; standard iOS Simulator build passed.
- **AppContainer RealmSwift Concrete Boundary Shrink Step 2 (2026-07-03, complete):** Removed direct `RealmSwift` usage from concrete `AppContainer.swift` by moving Realm default configuration installation and schema-version ownership into the persistence runtime. Added `DatabaseSchema` plus `DatabaseService` APIs for initial default, test, and production configuration installation; `AppContainer` now delegates Realm setup through those methods and uses the protocol default resolver implementations instead of referencing Realm schema primary-key helpers. Updated schema-version references in server-scoped configuration providers, test Realm environment setup, book preference configuration, and related tests to use `DatabaseSchema.version`. `DatabaseMigrator` now only builds and validates migrated configurations; `DatabaseService.prepareProductionConfiguration` installs the Realm default configuration. Validation: AppContainer RealmSwift scan passed with no matches; legacy `AppContainer.RealmSchemaVersion` scan passed with no matches; `git diff --check` passed; focused `DatabaseBootstrapperTests`, `MockAppContainerFactoryTests`, `DefaultServerScopedRealmConfigurationProviderTests`, `CalibreBookManagerTests`, `UnifiedCategoryServiceTests`, and `V2MigrationDependencyTests` passed (68 tests); standard iOS Simulator build passed.
- **UnifiedCategoryService Realm Observer Test Isolation Fix (2026-07-02, complete):** Fixed the failing `UnifiedCategoryServiceTests.testRealmSearchCacheStoreObserveCategorySummariesPublishesInitialAndUpdatedSnapshots` after the RealmSwift boundary shrink. Root cause was the test helper still using the old `DatabaseService.shared.setup(conf:)` path while `RealmSearchCacheStore` now depends on container-scoped `DatabaseService`; repeated/class-level runs could reuse category cache rows across observer tests and make the initial summary snapshot non-deterministic. The helper now keeps the class-scoped in-memory Realm identifier to avoid Realm handle exhaustion, creates the container through `MockAppContainerFactory.makeContainer(mainRealmConfiguration:testName:)`, clears category cache objects before each helper use, and constructs `RealmSearchCacheStore` from the container runtime. Validation: the failing test passed individually, full `UnifiedCategoryServiceTests` passed (9 tests).
- **CalibreLibraryManager Sync State Actor Fix (2026-07-02, complete):** Fixed an `EXC_BAD_ACCESS` crash in `CalibreLibraryManager.syncLibrary(request:)` where concurrent probe-triggered tasks wrote `librarySyncStatus` from a cooperative background executor. Root cause was unsynchronized mutation of manager-owned Swift dictionaries after managers were converted to plain classes. `syncLibrary(request:)` is now `@MainActor`, matching the existing `saveBookMetadata` status-update path and serializing sync-state mutations while async network work still suspends normally. Validation: focused `CalibreLibraryManagerTests` and `CalibreServerManagerTests` passed (24 tests); `git diff --check` passed; standard iOS Simulator build passed.
- **AppContainer RealmSwift Protocol Boundary Shrink (2026-07-02, complete):** Removed RealmSwift from `AppContainerProtocol` by deleting the protocol-level `realm`, `realmSaveBooksMetadata`, and `realmConf` properties. `AppContainer` now owns an instance-scoped `DatabaseService` instead of binding directly to `DatabaseService.shared`, and main Realm configuration, main Realm opening, metadata Realm opening, refresh, and reset now flow through explicit `DatabaseService` methods. `DatabaseBootstrapper`, `MainView`, `RealmSearchCacheStore`, `ReadingSessionManager`, `CalibreBookManager`, and `BookDownloadManager` were migrated away from `container.realm*` access; `BookDownloadManager` no longer imports RealmSwift. `RealmSearchCacheStore` now depends on `DatabaseService` plus a narrow library snapshot provider rather than using the full container for Realm configuration. Validation: `AppContainerProtocol` Realm scan passed with no matches; production `container.realm*` scan passed with no matches; `BookDownloadManager` Realm scan passed with no matches; `git diff --check` passed; focused `DatabaseBootstrapperTests`, `MockAppContainerFactoryTests`, `CalibreBookManagerTests`, `BookDownloadManagerTests`, `UnifiedSearchServiceTests`, and `V2MigrationDependencyTests` passed (91 tests); standard iOS Simulator build passed.
- **AppContainer Kingfisher Boundary Removal (2026-07-02, complete):** Removed direct Kingfisher exposure from `AppContainer` and `AppContainerProtocol`. The container now depends on an app-local `BookCoverCaching` protocol and a narrow `CalibreServerSnapshotProviding` protocol; the concrete `DefaultBookCoverCache` in `Network/ImageRequest.swift` owns `ImageCache.default`, Kingfisher request modifier setup, and the authentication challenge responder. `BookDetailViewModel` and `CalibreBookManager` now call `coverCache` for cover removal/storage instead of reaching into Kingfisher APIs through the container. Views still use `KFImage` directly for rendering, which remains out of scope for this container-boundary cleanup. Validation: `Models/App` scan passed with no Kingfisher/ImageCache/AuthPlugin/AuthResponsor matches; legacy `kfImageCache` / `authResponsor` scan passed with no matches; `git diff --check` passed; focused `BookDetailViewModelTests`, `CalibreBookManagerTests`, `LibraryInfoBookListViewModelTests`, and `V2MigrationDependencyTests` passed (81 tests); standard iOS Simulator build passed.
- **App Layer Combine Timer/Cancellable Removal (2026-07-02, complete):** Removed the remaining App-layer Combine timer/cancellable boundary. `AppContainer` and `AppContainerProtocol` no longer import Combine, expose `probeTimer`, or carry the unused `calibreCancellables` set; `DatabaseBootstrapper` also dropped its unused Combine import. `YetAnotherEBookReaderApp` now owns reachability probing as a Swift Concurrency `Task` stored in `@State`, cancels any previous loop on re-enable/background, probes immediately on active, and then probes every 60 seconds via `Task.sleep`. Validation: App/AppProtocol/Bootstrapper scan passed with no `import Combine`, `AnyCancellable`, `Timer.publish`, `.sink`, or `.store(in:)`; `git diff --check` passed; focused `DatabaseBootstrapperTests`, `SettingsViewModelTests`, `CalibreServerManagerTests`, and `MainViewModelTests` passed (31 tests); standard iOS Simulator build passed.
- **AppContainer ObservableObject Removal (2026-07-02, complete):** Demoted `AppContainer` from a SwiftUI `ObservableObject` to a plain composition root. Removed its `ObservableObject` conformance, `@Published` wrappers, and manager `stateChanges()` to `objectWillChange` forwarding; `deviceName`, `downloadManager`, metadata update flags, and `fontsManager` are now regular stored properties. Added `EnvironmentValues.appContainer` for plain dependency injection, changed the app root from `@StateObject` container ownership to a stable stored container, and migrated container reads in Views/previews/sheets from `.environmentObject` / `@EnvironmentObject` / `@ObservedObject` to `.environment(\.appContainer, ...)` or ordinary stored references. `ServerDetailView` no longer depends on container redraw for library sync/hidden-library UI; `ServerViewModel` now publishes those snapshots from `CalibreLibraryManager` async streams. Follow-up cleanup removed the remaining AppContainerProtocol `AnyCancellable` timer state. Validation: AppContainer observable/environment-object scans passed with no matches; `git diff --check` passed; focused `MainViewModelTests`, `SettingsViewModelTests`, `LibraryInfoBookListViewModelTests`, `BookDetailViewModelTests`, `RecentShelfViewModelTests`, and `SectionShelfViewModelTests` passed (73 tests); standard iOS Simulator build passed.
- **AppContainer Legacy Subject Bridge Removal (2026-07-02, complete):** Removed the remaining app-wide `PassthroughSubject` bridges from `AppContainer` / `AppContainerProtocol`; a later cleanup also removed `AppContainer`'s SwiftUI `ObservableObject` / `@Published` compatibility. `publishBookImport`, `publishDismissAll`, `publishBookReaderActivity`, `publishProbeLibraryLastModifiedRequest`, and `publishCalibreUpdate` now emit only through async streams; `calibreUpdates()` uses the shared `ManagerAsyncBroadcaster`. Removed legacy Recent/Discover shelf subject forwarding and simplified shelf snapshot test helpers to snapshot-only publishing. Extracted `ManagerAsyncBroadcaster` from `ShelfDataManager.swift` into `Models/Managers/ManagerAsyncBroadcaster.swift` and registered it in both app targets. Migrated remaining direct subject tests to async streams or removed legacy bridge assertions. Validation: `rg "PassthroughSubject|CurrentValueSubject" YetAnotherEBookReader` passed with no matches; `git diff --check` passed; focused `CalibreBookManagerTests`, `CalibreLibraryManagerTests`, `V2MigrationDependencyTests`, `RecentShelfViewModelTests`, `SectionShelfViewModelTests`, and `ShelfDisplayModelsTests` passed (84 tests); standard iOS Simulator build passed.
- **Views Combine Subscription Removal (2026-07-02, complete):** Removed explicit Combine subscriptions/imports from `Views/` while preserving `@Published` / `ObservableObject` as SwiftUI state. `AppContainer` exposes async event bridges for dismiss-all, book-import, and reader scene-phase activity; the legacy subject forwarding that still existed at this stage was removed in the later AppContainer legacy subject cleanup. Migrated `LibraryInfoView`, `YabrEBookReaderNavigationController`, `MainViewModel`, `ReaderOptionsViewModel`, `SettingsViewModel`, and `ServerViewModel` from `sink`/`AnyCancellable` to `Task` / async streams or `didSet` state handling, and updated event senders to use publish methods. Validation: `Views/` Combine scan passed with no matches for imports/subscriptions; `git diff --check` passed; focused `MainViewModelTests`, `ReaderOptionsViewModelTests`, `SettingsViewModelTests`, and `BookDetailViewModelTests` passed (41 tests; no standalone `ServerViewModelTests` suite exists); standard iOS Simulator build passed.
- **ActivityListViewModel Stream Test Race Fix (2026-07-02, complete):** Fixed the failing `ActivityListViewModelTests.testInitializationUsesRepositoryAndReceivesUpdates` after the async repository migration. `ActivityListViewModel` now creates the repository activity stream before launching its consuming task and updates `@Published` activities from a main-actor task. The mock repository exposes observer subscriber count, and the test now waits on observer registration/state conditions instead of a fixed `RunLoop` delay. Validation: focused `ActivityListViewModelTests.testInitializationUsesRepositoryAndReceivesUpdates` plus `BookDetailViewModelTests` passed (25 tests); `git diff --check` passed; standard iOS Simulator build passed.
- **Managers Combine Boundary Removal (2026-07-02, complete):** Removed Combine from `Models/Managers/` as a layer dependency. `CalibreBookManager`, `CalibreLibraryManager`, `CalibreServerManager`, `ReadingSessionManager`, and `FontsManager` are now plain classes with explicit `AsyncStream` snapshot/state outputs backed by a small app-local `ManagerAsyncBroadcaster`; manager event buses now use direct methods plus async streams. AppContainer legacy subjects and later `ObservableObject` / `@Published` compatibility were removed in follow-up cleanups. Migrated affected Settings/Server/Library view models and manager tests from manager `$property`/subject subscriptions to cancellable stream-consuming tasks. Validation: Managers Combine scan passed with no matches; `git diff --check` passed; focused manager/view-model tests passed (63 tests); standard iOS Simulator build passed.
- **Review Follow-up: Repository Streams and Download State Isolation (2026-07-02, complete):** Resolved review findings on the repository Combine-removal branch. Realm repository `AsyncStream` observers now install notification tokens before first emission and use Realm `.initial` notifications for initial values, with explicit Realm refresh before observation to avoid stale main-Realm snapshots after background writes. `BookDownloadManager` download state and APIs are main-actor isolated, the subscriber lock was removed, and `BookDetailViewModel` now registers its book stream synchronously during setup so metadata refreshes cannot race observer installation. Added/updated book and library repository observer regression tests for initial-plus-update delivery. Validation: focused repository/download/UI tests passed (`RealmBookRepositoryTests`, `RealmLibraryRepositoryTests`, `BookDetailViewModelTests`, `LibraryViewModelTests`, `UnifiedCategoryServiceTests`, `BookDownloadManagerTests`, `LibraryInfoBookListViewModelTests`, `RecentShelfViewModelTests`, `V2MigrationDependencyTests`); `git diff --check` passed; standard iOS Simulator build passed.
- **UnifiedSearchService Combine Bridge Removal (2026-07-02, complete):** Removed `UnifiedSearchService`'s final service-level Combine dependency by deleting `searchUpdatePublisher(for:)` and `publisher(for:)`; `search(key:force:) -> AsyncStream<SearchUpdate>` is now the only service output API. Migrated `UnifiedSearchServiceTests`, `UnifiedSearchIntegrationTests`, and `V2MigrationDependencyTests` call sites to consume async search streams directly with test-local cancellation. Repository/cache Combine APIs remain intentionally out of scope. Validation: `git diff --check` passed, focused `UnifiedSearchServiceTests` / `UnifiedSearchIntegrationTests` / `V2MigrationDependencyTests` passed (47 tests), and the standard iOS Simulator build passed.
- **Models Directory Structure Cleanup (2026-07-02, complete):** Reorganized the remaining root-level files under `YetAnotherEBookReader/Models/` into responsibility-based folders (`App/`, `Calibre/`, `Domain/`, `Managers/`, `Persistence/`) without changing Swift declarations or behavior; preserved existing `Protocols/`, `Repositories/`, `Realm/`, `Services/`, `Testing/`, and `Utils/`; moved historical `CalibreBrowser/CalibreSearchCache.swift` into `Models/Realm/CalibreSearchCache.swift`; removed empty `CalibreBrowser` and `UnifiedSearch` project groups; updated `project.pbxproj` so Xcode navigator groups match the filesystem. Validation: `git diff --check`, iOS Simulator build, and test suite passed in the main checkout.
- **Review Follow-up: Download Active State Consumers (2026-07-02, complete):** Addressed review findings from the Network Swift Concurrency cleanup. `LibraryInfoBookListViewModel` now consumes `BookDownloadManager.downloadSnapshots()` and exposes UI-facing active download state, so `LibraryInfoBookRow` no longer directly reads the manager's non-observable dictionary. `RecentShelfViewModel.tapBook` now treats paused/pausing/resumable downloads as active via `isActive`, matching Book Detail semantics. Validation: focused `LibraryInfoBookListViewModelTests`, `RecentShelfViewModelTests`, and `BookDownloadManagerTests` passed (48 tests), `V2MigrationDependencyTests` passed (34 tests), and `git diff --check` passed.
- **BookFormatRow Pause/Resume UI State Fix (2026-07-02, complete):** Fixed the regression where pausing an active book format download made the row fall back to the pre-download "Not cached" state. `BookFormatDownload` now tracks `isPaused` and `isPausing`, exposes a unified `isActive` predicate, and `BookDetailViewModel` / Book Detail UI entry points use that predicate instead of only `isDownloading`. `BookDownloadManager.pauseDownload` now immediately publishes a paused active snapshot and keeps the entry active even when URLSession returns nil resume data; `resumeDownload` restarts from resume data when present or from the original source URL for paused entries without resume data. Stale URLSession pause/progress/completion callbacks are ignored when a newer resumed task is already attached. Validation: focused `BookDownloadManagerTests` + `BookDetailViewModelTests` passed (50 tests), `V2MigrationDependencyTests` passed (34 tests), and `git diff --check` passed.
- **Network Cleanup Finalization (2026-07-02, complete):** Completed the final `Network/` cleanup pass after the Combine bridge removals. Deleted remaining deprecated fire-and-forget wrappers from `DSReaderHelperConnector` and completion/Int metadata wrappers from `CalibreServerService+Metadata`, renamed `BookDownloadManager.startDownloadNew` to the final `startDownload` Result-returning API, removed the old Bool `startDownload` overload, and renamed `getMetadataNew(task:)` to `getMetadataData(task:)`. The download progress delegate no longer reads `activeDownloads` from the URLSession callback queue before hopping to the main actor. Also stabilized the previously intermittent `V2MigrationDependencyTests.testShelfDataModelPendingInitialCategoryKeepsInitialLoadIncomplete` by making `YabrShelfDataModel.seedCategoriesForTesting` cancel the initialization snapshot task and synchronize the outer initial-load projection with the seeded actor state. Validation: `Network/` scan is empty for Combine/deprecated/old `New` symbols; `git diff --check` passed; focused `CalibreServerServiceTests`, `DSReaderHelperConnectorTests`, `BookDownloadManagerTests`, `BookDetailViewModelTests`, and `V2MigrationDependencyTests` passed (132 tests).
- **Network Download Combine Boundary Removal - Stage 4 (2026-07-02, complete):** Removed the last download-side legacy Combine bridges from `BookDownloadManager`: deleted `bookFormatDownloadSubject`, `bookDownloadedSubject`, the internal `AnyCancellable` sink, and the unused custom `Downloader.swift` / `downloadTaskPublisher` file plus Xcode project references. `BookDownloadManager` is no longer an `ObservableObject` and no longer exposes `@Published activeDownloads`; download state is observed through the existing `downloadSnapshots()` async stream, with `BookDetailViewModel` keeping its UI-facing `@Published activeDownloads` by consuming that stream in a cancellable task. Success completion continues to publish `.book` updates through `AppContainer.publishCalibreUpdate`, while failure/non-2xx/empty-file paths stay non-success. Validation: `Network/` Combine scan is empty; `git diff --check` passed; focused `BookDownloadManagerTests`, `V2MigrationDependencyTests`, `BookDetailViewModelTests`, and `CalibreServerServiceTests` passed (123 tests). One V2 shelf initial-load test failed once in the combined run and then passed both individually and in the rerun, indicating an existing intermittent order/timing issue outside this download cleanup.
- **Network Legacy Combine Bridge Removal - Stage 3 (2026-07-02, complete):** Removed the zero-production-reference Combine publisher bridges from `CalibreServerService` and `DSReaderHelperConnector`, converting the affected tests to async-only coverage. `CalibreServerService` no longer exposes `validatedDataPublisher`, metadata/books/annotations/reading-position/update publisher overloads, legacy library/custom-columns/category publisher overloads, or `probeServerReachabilityNew`; the legacy `/cdb/cmd/list/0?library_id=...` and `/cdb/cmd/custom_columns/0?library_id=...` wire shapes are preserved as async helpers (`syncLegacyLibraryResult`, `getLegacyCustomColumns*`). `DSReaderHelperConnector.refreshConfiguration` now uses async `validatedData` so HTTP non-2xx responses are rejected consistently. `Network/` Combine scan now leaves only the intentional `BookDownloadManager` legacy subjects and deprecated `Downloader.swift` download publisher adapter. Validation: focused Network/service/helper/manager/download suite passed (100 tests), and `git diff --check` passed.
- **Network Swift Concurrency Migration - Stage 2 (2026-07-02, complete):** Continued narrowing `Network/` Combine boundaries by rebuilding `CalibreServerService.validatedDataPublisher` as an async `validatedData` bridge, making `probeServerReachabilityNew` wrap the async reachability implementation while preserving `CalibreServerInfo.error`, and converting DSReader Helper configuration sync in `CalibreServerManager` from a Combine `flatMap` pipeline to a buffered async subject consumer with child tasks for bursty multi-server events. `DSReaderHelperConnector.refreshConfiguration()` publisher now bridges the async helper call, and unused `Downloader.swift` download publisher entry points are deprecated rather than removed to avoid project-file churn. Added coverage for publisher validation/status/auth failures, reachability publisher equivalence, helper config success/failure/no-op cases, and independent multi-server helper sync. Validation: focused Network/manager/download tests passed (89 tests), and `V2MigrationDependencyTests` passed (34 tests).
- **BookDownloadManager Lifecycle Test Expansion (2026-07-02, complete):** Expanded `BookDownloadManagerTests` from basic start/progress/completion checks to full lifecycle coverage for start validation failures, cancel, pause, resume, error/non-2xx/empty-file completion paths, background delegate callback actor hops, async download snapshots, legacy subjects, and independent single/multiple active downloads. The test suite now uses local helpers and stub URLSession tasks to drive manager/delegate state deterministically without production code changes. Validation: focused `BookDownloadManagerTests` passed (25 tests), and related `V2MigrationDependencyTests` passed (34 tests).
- **Network Swift Concurrency Migration - Stage 1 (2026-07-02, complete):** Established async/await as the primary implementation path for additional `Network/` APIs while preserving legacy Combine surfaces. Added async task-level metadata fetches (`getMetadata(task:)`, `getMetadataNew(task:)`), async last-read-position fetch, throwing async library sync/custom-column/category helpers, and async legacy-compatible helpers for the old publisher endpoint shapes (`/cdb/cmd/list/0?library_id=...` and `/custom_columns/0?library_id=...`) so publisher callers keep existing wire behavior. Rebuilt several publisher methods as `Deferred/Future` bridges over async implementations. Migrated `CalibreLibraryManager.registerProbeLibraryLastModifiedCancellable` from a Combine `flatMap` network pipeline to task-based async handling while intentionally preserving the legacy sync-library response shape. Added async fallback `DSReaderHelperConnector.refreshConfiguration()` and migrated `ServerViewModel.connectDSReader` off its Combine fallback sink. Added `BookDownloadManager.downloadSnapshots()` as an async snapshot stream and moved download delegate completion/progress/pause callbacks from `DispatchQueue.main.async` / `MainActor.assumeIsolated` to explicit `Task { @MainActor ... }` hops. Legacy `@Published`/subjects and publisher APIs remain for compatibility. Validation: `git diff --check`; focused network/manager tests passed (`CalibreServerServiceTests`, `DSReaderHelperConnectorTests`, `BookDownloadManagerTests`, `CalibreLibraryManagerTests`: 63 tests); focused `BookDownloadManagerTests` rerun passed after the final pause-callback actor-hop adjustment (10 tests).
- **YabrShelfDataModel ObservableObject Removal (2026-07-02, complete):** Completed the next shelf Swift Concurrency cleanup by making `YabrShelfDataModel` a plain `@MainActor final class` instead of an `ObservableObject`. Removed `Combine` and `@Published` state from `ShelfDataManager.swift`; Recent and Discover internal state is now private and only projected through `RecentShelfSnapshot` / `DiscoverShelfSnapshot` async streams plus the existing legacy subject bridge. `CategoryObject` no longer conforms to `ObservableObject`, and tests now read shelf state through snapshot helpers instead of direct internal arrays/properties. Validation: `git diff --check`; `ShelfDataManager.swift` scan found no `import Combine`, `@Published`, or `ObservableObject`; `V2MigrationDependencyTests` passed (34 tests), `ShelfDisplayModelsTests` passed (6 tests), and focused `RecentShelfViewModelTests` + `SectionShelfViewModelTests` passed (22 tests).
- **YabrShelfDataModel Legacy Bridge Confinement (2026-07-02, complete):** Further narrowed shelf legacy boundaries after the input/output async migration. `YabrShelfDataModel` now uses a private main-actor snapshot broadcaster for Recent and Discover async streams, preserving immediate current-snapshot delivery, duplicate suppression, termination cleanup, and deinit finishing without hand-managed continuation dictionaries. Legacy Recent/Discover subject sends are centralized behind `AppContainer.publishLegacyRecentShelfItems(_:)` and `publishLegacyDiscoverShelfItems(_:)`, so shelf data model code no longer directly touches those subjects. Removed the internal `bookDownloadedSubject` Combine sink from `AppContainer`; successful downloads now publish `.book` calibre updates directly via `publishCalibreUpdate`, while `bookDownloadedSubject` remains a legacy output notification only. `BookDownloadManager.requestDownload` is now main-actor isolated to match the old main-queue subject behavior. Validation: `git diff --check`; shelf bridge scan leaves only the two intentional AppContainer legacy subject sends; `V2MigrationDependencyTests` passed (34 tests), `BookDownloadManagerTests` passed (9 tests), and focused Recent/Section/Shelf display tests passed (28 tests).
- **YabrShelfDataModel Input Swift Concurrency Modernization (2026-07-01, complete):** Migrated shelf-related calibre update input consumption from Combine `.values` to an async event stream on `AppContainerProtocol`. Added `publishCalibreUpdate(_:)` / `calibreUpdates()`; `AppContainer.publishCalibreUpdate` now yields async subscribers and still sends `calibreUpdatedSubject` as a legacy bridge. `YabrShelfDataModel`, `RecentShelfViewModel`, and `SectionShelfViewModel` now consume `container.calibreUpdates()` for shelf rebuilds and deleted-detail dismissal. App-side calibre update producers now call `publishCalibreUpdate` instead of `calibreUpdatedSubject.send`, while the legacy subject remains available for old subscribers. Added `BookDownloadManager.requestDownload(book:format:overwrite:)`; shelf auto-download and refresh-format paths call it directly, with `bookFormatDownloadSubject` retained as a legacy bridge. Validation: `git diff --check`; forbidden app scan leaves only `AppContainer.publishCalibreUpdate`'s legacy `calibreUpdatedSubject.send`; `V2MigrationDependencyTests` passed (32 tests), `RecentShelfViewModelTests` passed (8 tests), `SectionShelfViewModelTests` passed (14 tests), and `BookDownloadManagerTests` passed (8 tests).
- **YabrShelfDataModel Recent Shelf Swift Concurrency Modernization (2026-07-01, complete):** Migrated Recent shelf production and consumption into `YabrShelfDataModel` async snapshots. Added `RecentShelfSnapshot` / `recentSnapshots()`, moved Recent rebuilds out of the old Combine `registerRecentShelfUpdater()` pipeline into cancellable tasks backed by a private builder actor, and preserved the legacy `recentShelfItemsSubject` bridge for compatibility. `RecentShelfViewModel` now consumes `container.shelfDataModel.recentSnapshots()` with a cancellable task and keeps the temporary `calibreUpdatedSubject.values` deletion bridge. Recent rebuilds continue to preserve reading-position sorting, local/no-connect/downloading/has-update status mapping, signposts, and missing-format auto-download requests. Review follow-up hardened cancellation: cancelled Recent rebuilds now check `Task` cancellation before reading positions, before sorting, and while building items so stale rebuilds stop occupying the builder actor promptly. Validation: `git diff --check`; forbidden Recent updater scan found no `registerRecentShelfUpdater`, `recent-shelf-updater`, `recentShelfItemsSubject.sink`, or old Recent shelf dispatch queue references; `V2MigrationDependencyTests` passed (29 tests), `RecentShelfViewModelTests` passed (8 tests), and `ShelfDisplayModelsTests` passed (6 tests).
- **YabrShelfDataModel InitialLoadState Cleanup (2026-07-01, complete):** Replaced `ShelfCategoryStore`'s distributed initial-load bookkeeping (`initialCategories`, `completedInitialCategories`, `shelfSnapshotComplete`, and internal completion Bool) with a private `InitialLoadState` state machine. The first Discover shelf snapshot now defines the initial-load gate, loading snapshots only shrink pending initial category keys, and `.complete` is terminal so later category additions/deletions do not regress UI readiness. `YabrShelfDataModel.isInitialLoadComplete` remains the main-actor UI projection. Added focused `V2MigrationDependencyTests` coverage for empty snapshots, pending initial categories, category completion, pending-category deletion, and terminal completion behavior. Validation: `git diff --check`; forbidden shelf pattern scan found no Realm notification / RealmSwift / shelf queue / old search publisher reintroductions; `V2MigrationDependencyTests` passed (24 tests), `SectionShelfViewModelTests` passed (14 tests), and `ShelfDisplayModelsTests` passed (6 tests).
- **YabrShelfDataModel Output Swift Concurrency Modernization (2026-07-01, complete):** Added `YabrShelfDataModel.DiscoverShelfSnapshot` and an async `snapshots()` stream so Discover shelf UI can consume value snapshots directly instead of subscribing to `discoverShelfItemsSubject` and `$isInitialLoadComplete`. `SectionShelfViewModel` now owns cancellable `Task` instances for `shelfDataModel.snapshots()` and the temporary `calibreUpdatedSubject.values` deletion/bootstrap bridge, cancelling both on deinit. Shelf display value types now conform to `Sendable`; `discoverShelfItemsSubject` remains as a legacy compatibility bridge emitted by `YabrShelfDataModel`. Updated Discover shelf tests to inject snapshots through `YabrShelfDataModel` and added stream coverage for initial snapshots, section updates, initial-load-only updates, termination, and legacy subject publication. Validation: `git diff --check`; forbidden shelf pattern scan found no Realm notification / RealmSwift / shelf queue / old search publisher reintroductions; `V2MigrationDependencyTests` passed (19 tests), `SectionShelfViewModelTests` passed (14 tests), and `ShelfDisplayModelsTests` passed (6 tests).
- **YabrShelfDataModel Consumer Swift Concurrency Modernization (2026-07-01, complete):** Modernized the Discover shelf consumer side while keeping the app-wide `calibreUpdatedSubject` / `discoverShelfItemsSubject` compatibility boundary. `YabrShelfDataModel` is now `@MainActor`; author category state, in-shelf IDs, and initial-load tracking moved into a private `ShelfCategoryStore` actor; category searches now use per-category `Task` instances over `UnifiedSearchService.search(key:)` async streams instead of `searchUpdatePublisher` subscriptions. Removed the internal `shelf-queue` / category `AnyCancellable` path without reintroducing Realm notifications, `RealmSwift`, live Realm objects, or `DispatchQueue.sync`. Added async testing helpers and focused coverage for `.book` rebuilds, `.deleted` stale-category removal, category search task cancellation, deinit event-task release, initial loaded-snapshot rebuild, and `refresh()` limit reset. Validation: `git diff --check`; no forbidden shelf patterns found by scan; `V2MigrationDependencyTests` passed (14 tests), `SectionShelfViewModelTests` passed (14 tests), and `ShelfDisplayModelsTests` passed (6 tests).
- **Discover Shelf `.book` / `.deleted` Refresh Fix (2026-07-01, complete):** Fixed the post-Realm-token-removal regression where `YabrShelfDataModel` only rebuilt Discover author categories on `.shelf`. It now treats `.shelf`, `.book`, and `.deleted` as value-snapshot invalidation signals, gated by `bookManager.isShelfLoaded`, and rebuilds from `bookManager.booksInShelf` without reintroducing Realm notifications. Added focused `V2MigrationDependencyTests` coverage for add-to-shelf `.book` category creation and `.deleted` stale-category removal. Validation: `V2MigrationDependencyTests` passed (11 tests); all `SectionShelfViewModelTests` passed in focused batches (3 + 6 + 5 tests) after one full-class run was interrupted during Xcode test-runner cleanup; `git diff --check` passed.
- **Discover Shelf Realm Notification Removal (2026-07-01, complete):** Removed `YabrShelfDataModel`'s direct Realm dependency and `CalibreBookRealm` notification token. Discover shelf category state now rebuilds from `CalibreBookManager.booksInShelf` value snapshots when `.shelf` is emitted, with category subscriptions diffed on the existing `shelf-queue`. `ShelfDataManager.swift` no longer imports `RealmSwift`, opens a queue-local Realm, or uses `changesetPublisher`; `V2MigrationDependencyTests` now seeds refresh categories through a small test helper instead of mutating `dispatchQueue` internals. Validation: `git diff --check`; focused `SectionShelfViewModelTests` (14), `ShelfDisplayModelsTests` (6), `V2MigrationDependencyTests` (9), and the combined focused run (29 tests) passed.
- **Shelf Bootstrap Early Notification Fix (2026-07-01, complete):** Added `sendShelfUpdate: Bool = true` to `CalibreBookManager.populateBookShelf` so normal callers keep current `.shelf` publication semantics while database bootstrap can suppress the early shelf rebuild. `DatabaseBootstrapper` now calls `populateBookShelf(sendShelfUpdate: false)` and emits the single bootstrap `.shelf` only after `populateLocalLibraryBooks` completes, removing the remaining startup empty-shelf flash without adding a new loading state machine. Validation: `git diff --check`, focused `CalibreBookManagerTests` + `CalibreLibraryManagerTests` (24 tests), and `RecentShelfViewModelTests` (8 tests) passed.
- **Reading Position Realm Boundary + Shelf Bootstrap Finalization (2026-07-01, complete):** Replaced repository-side `bookPrefId` parsing, mutable manager dictionary lookups, and `DispatchQueue.main.sync` with explicit `CalibreBook` / `CalibreServer` context and injected `ServerScopedRealmConfigurationProviding`. Migrated metadata sync, shelf rebuilds, reading sessions, Book Detail, Readium, FolioReader, and migration helpers to the domain-level repository API. `FolioReaderYabrReadPositionProvider` now captures its repository dependency instead of repeatedly consulting `AppContainer.shared`. Fixed `AppContainer` initialization order so a `TestRealmEnvironment` is installed before lazy managers/repositories are materialized. Shelf bootstrap now completes exactly once on the main thread only after remote shelf and local-library reconciliation finish, preventing transient loaded/empty states. Schema 141 migration coverage verifies the search-cache indexes. Validation: 392 unit tests and 1 UI test passed; Mac Catalyst build succeeded.
- **Section Shelf Refresh Realm Stall Fix (2026-07-01, implemented):** The reported refresh "deadlock" was a long Realm commit rather than a lock cycle: every Discover category launched a forced unified search concurrently, and `RealmSearchCacheStore.saveLibrarySourceResult` rewrote every returned `CalibreBookRealm` with `.modified` even though `LibrarySearchService` had already persisted metadata. Discover refresh now snapshots category keys on `shelf-queue`, refreshes them sequentially through the new awaitable `UnifiedSearchService.resetSearchAndWait`, and keeps the refresh UI active until all categories complete while rejecting duplicate refresh commands. Search-cache saves now skip transactions when generation/count/IDs are unchanged and only insert genuinely missing books as a compatibility fallback; existing books are never redundantly updated. Focused validation passed 36 tests across `SectionShelfViewModelTests`, `UnifiedSearchIntegrationTests`, `UnifiedSearchServiceTests`, and `V2MigrationDependencyTests`.
- **Intermittent Swift Actor Runtime Trap Recorded (2026-07-01, unresolved):** During app startup, Xcode stopped once with `EXC_BREAKPOINT` at `(anonymous namespace)::DefaultActorImpl::unlock` / `swift_task_switchImpl`. Repeated attempts did not reproduce it, and the captured Task 1320 stack contained no application frame or console fatal-error text, so no root cause is confirmed. Concurrent neighboring tasks were mapping Realm-backed category/search cache values in `RealmSearchCacheStore`, but each visible path opened and consumed a queue-local Realm before suspension; their presence is correlation, not evidence that Realm caused the trap. Two concurrency risks remain candidates if the issue recurs: `RealmSearchCacheStore` is `@unchecked Sendable` while resolving mutable `container.realmConf` and `container.calibreLibraries` from cooperative tasks, and background shelf work may read manager dictionaries such as `booksInShelf` while the main thread publishes mutations. On recurrence, capture the first console runtime diagnostic, whether Continue resumes or terminates the process, the complete crashing-task backtrace, and a reproduction under Thread Sanitizer or Address Sanitizer before changing code.
- **Metadata Sync Realm ownership rollback to repositories (2026-06-28, complete):** Simplified `BookMetadataSyncWorker` so it no longer imports Realm, manages store identity, or chooses cross-repository write transactions. `SyncJob` dropped the unused `formatKey`, `executeSync` now only calls value-type repository APIs on its serial queue, and `needUpload == true` still forces empty-remote annotation merges so local pending highlights/bookmarks are discovered for upload. `ReadingPositionRepositoryProtocol` and `AnnotationRepositoryProtocol` no longer expose `getRealm(...)` or public `in realm:` sync entry points; both Realm repositories keep Realm acquisition plus short-transaction apply helpers private again. Mock repositories and worker tests were reduced to domain-level API assertions, and a real repository worker integration test now verifies position/highlight/bookmark persistence end-to-end. Validation: `xcodebuild test -only-testing:YetAnotherEBookReaderTests/BookMetadataSyncWorkerTests -only-testing:YetAnotherEBookReaderTests/RealmReadingPositionRepositoryTests -only-testing:YetAnotherEBookReaderTests/RealmAnnotationRepositoryTests` passed (27 tests, 0 failures).
- **Metadata Sync Worker + LaunchState review-fix pass (2026-06-28, complete):** Fixed four review findings in `BookMetadataSyncWorker` and app startup bootstrap handling. `BookMetadataSyncWorker` now always runs annotation merge for `needUpload` jobs even when remote annotations are empty, removed worker-level outer Realm write transactions so repositories keep short apply transactions, and compares Realm store identity via standardized `fileURL` or matching `inMemoryIdentifier` instead of `nil == nil`. Startup gating in `YetAnotherEBookReaderApp` now only reuses `.ready` when `launchState` is already `.ready` and `AppContainer.isDatabaseReady` confirms both main + metadata Realm availability; bootstrap failures clear partial container state via `resetDatabaseBootstrapState(clearConfiguration:)`. Added focused regression coverage in `BookMetadataSyncWorkerTests` and `DatabaseBootstrapperTests`. Validation: `xcodebuild test -only-testing:YetAnotherEBookReaderTests/BookMetadataSyncWorkerTests -only-testing:YetAnotherEBookReaderTests/DatabaseBootstrapperTests` passed (13 tests, 0 failures).
- **Stage P12 (Performance Plan): Cache Server-Scoped Configurations (2026-06-28, complete):** Refactored `DefaultServerScopedRealmConfigurationProvider` to cache `Realm.Configuration` instances using a thread-safe cache keyed by `serverUUID` and `schemaVersion`. Implemented double-checked locking using `NSLock` to perform expensive configuration setup outside the lock when missing. Added focused tests in `DefaultServerScopedRealmConfigurationProviderTests.swift` verifying caching, server differentiation, schema modification behavior, and multi-thread concurrent safety. All 381 unit tests and 1 UI test passed.
- **Stage P11 (Performance Plan): Batch Manager State Publication (2026-06-28, complete):** Refactored server/library/book manager populators (`populateServers`, `populateLibraries`, `populateBookShelf`) to assemble items in local in-memory dictionaries and perform single batch assignments to their `@Published` property variables. Checked cache status for all book formats in-memory, avoided multiple calls to `updateBook` in loops (now at most one save per book), and eliminated startup debugging print statements. All 378 unit tests and 1 UI test passed.
- **Stage P10 (Performance Plan): Stable App and ViewModel Lifetime (2026-06-28, complete):** Hoisted `MainViewModel` ownership to `YetAnotherEBookReaderApp` root, passing it down to observed views. Implemented an explicit `LaunchState` state machine (`initializing`, `ready`, `failed`) to render progress indicators or initialization failures, and only load `MainView` once the database transitions to `.ready`. Moved appearance configuration to a single-setup block in `init()`, and implemented a `bootstrapInFlight` guard to prevent duplicate concurrent DB initialization routines on active scene phase changes. All 378 unit tests and 1 UI test passed.
- **Stage P8 & P9 (Performance Plan): Linearize Highlight and Bookmark Sync (2026-06-28, complete):** Linearized highlights and bookmarks synchronization inside `RealmAnnotationRepository`. Replaced live-results looping mutations with single-prefetch indexing and dry-run action generation in memory. Standardized remote timestamp parsing to run exactly once, resolving oldest/newest comparisons and CFI page parsing outside the database transaction block. Commits are executed in a single transaction only when actions are non-empty. Added stress tests for 500+ records and special-case verification. All 378 unit tests and 1 UI test passed.
- **Stage P7 (Performance Plan): Linearize Position Sync (2026-06-28, complete):** Linearized position merges inside `RealmReadingPositionRepository` by querying all local position Realm objects once per sync, index-resolving newer/older checks and duplicates in memory, and executing updates/deletions in a single database transaction. Added unit tests verifying edge cases, duplicate remote entries, Kindle/iPad device updates, and a 500+ records stress check. All 375 unit tests and 1 UI test passed.
- **Stage P6 (Performance Plan): Create Metadata Sync Worker (2026-06-28, complete):** Migrated position and annotation merging to run in a dedicated serial background queue `"book-metadata-sync"` via a new `BookMetadataSyncWorker` helper. Bypassed DB calls entirely when no annotations are returned from the server. Removed short-circuit evaluation of annotations to run both bookmark and highlight merges. Added unit tests in `BookMetadataSyncWorkerTests.swift` verifying thread contexts, task counts, skipped DB operations, and continuation resilience. All 373 unit tests and 1 UI test passed.
- **Stage P5 (Performance Plan): Tighten FolioReader Adapter Boundary (2026-06-28, complete):** Quarantined the legacy `takePrecedence` flag to the boundary of the `FolioReaderKit` adapter. Removed unused direct conversion methods on `BookDeviceReadingPositionRealm` in `Providers.swift`. Documented the `@Persisted var takePrecedence` property as legacy/inert in `BookReadingPositionRealm.swift` and added focused tests asserting correct isolation during conversion. All 368 unit tests and 1 UI test passed.
- **Stage P3 & P4 (Performance Plan): ReadingSessionHandle and Lifecycle Migration (2026-06-27, complete):** Migrated the e-book reading session tracking to use an explicit, immutable `ReadingSessionHandle` and precise session-reuse time thresholds (60s ended, 300s active). Replaced legacy `session(start:)` / `session(end:)` with `beginSession(at:forBookId:)` and `endSession(_:at:)`. Completely eliminated the deprecated `takePrecedence` mutable flags. Fixed a shared test-environment race condition by isolating `DatabaseService` instantiations in the repository tests. Addressed and completed code review optimization requirements (captured handle before async Folio saves to avoid race conditions, validated history bookId to prevent cross-book tampering, pre-reduced duplicate highlights by unique UUID and newest timestamp while filtering invalid entries before deduplication, and pre-indexed local positions on device ID to optimize syncPositions complexity from $O(remote \times local)$ to $O(remote + local)$). All 366 unit tests and 1 UI test passed.
- **Stage P4: Eliminate App Startup & Redraw Duplicate ViewModel Instantiations (2026-06-27, complete):** Fixed repeated ViewModel initialization by passing persistent `container` reference to `MainView` and `SettingsView` and wrapping ViewModel creation inside `@StateObject(wrappedValue:)` in their initializers. Wrapped `LibraryDetailView`, `libraryRestoreHiddenView`, and `ActivityList` in `LazyView` inside navigation links to prevent eager destination evaluation on parent redraws. Verified that all 365 unit tests and 1 UI test passed.
- **Stage P3: Optimize Highlight and Bookmark Memory Merges (2026-06-27, complete):** Optimized `syncBookmarks` and `syncHighlights` in `AnnotationRepository` to avoid O(N^2) query behavior in the merge loops. Built memory dictionaries (`localBookmarksByPos` grouped by pos, and `localHighlightsById` keyed by highlightId) from a single initial query per book, reducing the complexity to O(N) memory lookups. Hardened state tracking to bypass Realm live-query array update dependencies. Verified with unit tests, passing all 365 unit tests and 1 UI test.
- **Stage P2: Migrate Repository and ReaderInfo Selection (2026-06-27, complete):** Upgraded `getPosition` repository signature to accept explicit selection policy. Refactored `RealmReadingPositionRepository` to delegate to `policy.select(from: getPositions(forBookId:))` instead of querying `takePrecedence == true`. Added `prepareBookReading(book:withLoadedPositions:)` overload in `ReadingSessionManager`. Refactored `registerRecentShelfUpdater()` in `ShelfDataManager` to query and pass `positions` in background Combine worker, eliminating main-thread Realm queries during shelf rebuilds. Migrated all 5 call sites in the app and 3 in the tests. Verified all 365 unit tests and 1 UI test passed.
- **Stage P1: Lock reading position selection behavior (2026-06-27, complete):** Defined `ReadingPositionSelectionPolicy` enum and its pure `select(from:)` method in `ReadingPositionModels.swift` ensuring stable sorting (keeping first occurrence) when epochs are identical. Created `ReadingPositionSelectionTests.swift` with 6 tests verifying edge cases and permutations. All 365 unit tests and 1 UI test passed successfully.
- **Shelf View Alignment Stage S9: Visual Acceptance and Full Verification (2026-06-27, complete):** Conducted full project regression testing on EBook simulator configurations. Ran the complete EBook Reader application test suite (359 unit tests and 1 UI test) and verified all passed with 0 failures, validating end-to-end correct behavior on both Recent and Discover shelf views. Completed active context records and walkthrough documentation.
- **Shelf View Alignment Stage S8: State Matrix and Interactive Regression (2026-06-27, complete):** Extended `ShelfLegacyStyleTests.swift` to add layout validation tests covering single-book padding rows, exact column count fits, horizontal scrolling row border kinds, and dynamic vertical filler row count calculations. Verified all 37 tests pass without regression.
- **Shelf View Alignment Stage S7: Switch Discover Header and Horizontal Shelf Row (2026-06-27, complete):** Created `ShelfLegacySectionHeader.swift` implementing the 32pt tall banner with custom text centering, shadow properties, and wood image backgrounds. Refactored `SectionShelfView.swift` to align with the wood theme layout. Rows with count `<= columnCount` display in a fixed width row with trailing filler tiles. Rows with count `> columnCount` use a horizontal scroll view with zero margins. Added layout computations inside `GeometryReader` to render empty sections and bottom filler rows to cover the viewport. All tests passed.
- **Shelf View Alignment Stage S6: Handle Recent Safe Area, Tab Bar, and Edit Toolbar (2026-06-27, complete):** Updated `RecentShelfView.swift` to use SwiftUI's native `.safeAreaInset(edge: .bottom)` to present the edit toolbar. This maps the edit toolbar as a first-class content inset, allowing the grid content to scroll smoothly under the bar and avoid tab bar clipping. Removed redundant grid padding and bottom safe area overlaps to match the edge-to-edge legacy layout. All tests passed.
- **Shelf View Alignment Stage S5: Switch Recent Continuous Grid (2026-06-27, complete):** Refactored `RecentShelfView.swift` to use `GeometryReader` container width/height evaluations for dynamic grid sizing. Extracted columns and margins to use a zero-spacing, zero-padding `LazyVGrid` with fixed-width items. Combined active reading books with local filler tiles using a dynamically constructed identity sequence `RecentShelfRenderItem`. Standardized the grid tile kind mapping solely from item index and column counts. Replaced background color with legacy `#C49E7A` and overlaid empty-state views. All tests passed.
- **Shelf View Alignment Stage S4: Progress, Status, Options, and Edit Overlay (2026-06-27, complete):** Replaced nested Button pattern in `ShelfBookCard` with native tap and long-press gestures on `ShelfLegacyCoverView` to prevent event propagation conflicts. Implemented the legacy cover-overlay controls: progress badge (top-right, size 36x24, system size 11 text), status icon (left-bottom, size 20x24), options menu button (right-bottom, size 64x32, overlapping the right edge by 16pt) exposing the options Menu, and selection indicator (top-left, size 32x32, visible in edit mode only). Added VoiceOver accessibility attributes. All tests passed.
- **Shelf View Alignment Stage S3: Cover, Fallback, and Spine Migration (2026-06-27, complete):** Created `ShelfLegacyCoverView.swift` implementing separate load states (`.loading`, `.success`, `.failure`) for Kingfisher book covers. Covers are constrained to `120 x 160`, clipped, and rendered with wood binding `spine` overlays and 0.7 opacity black drop shadows. Empty or failed cover loads render multiline centered fallback text. Refactored `ShelfBookCard` to use the legacy tile/cover views and introduced `tileKind` and `tileWidth` properties with backward-compatible defaults. registered files in Xcode. All tests passed.
- **Shelf View Alignment Stage S2: Reusable Shelf Tile Shell (2026-06-27, complete):** Created `ShelfLegacyTile.swift` providing `ShelfLegacyTile` and `ShelfLegacyFillerTile` structures. The wooden backgrounds stretch to `width x 200`, while the cover occupies a fixed `120 x 160` centered frame. Filled tiles hide from accessibility features. Added previews for development. registered files in Xcode.
- **Shelf View Alignment Stage S1: Layout & Presentation Contract (2026-06-27, complete):** Implemented `ShelfLegacyStyle.swift` and `ShelfLegacyStyleTests.swift` defining `ShelfLegacyMetrics` (dimensions/colors), `ShelfTileKind` (left/center/right assets), `ShelfLegacyLayout` (column, width, completed items, and viewport counts), and `ShelfLegacyPresentation` (status assets and progress labels). All tests passed.
- **Shelf View Alignment Stage S0: Baseline Frozen (2026-06-27, complete):** Created the baseline registry artifact at `file:///Users/peterlee/.gemini/antigravity/brain/66f5de34-be1d-40f3-9e7e-d1a22f95d1f5/shelf_view_baseline.md` mapping baseline screenshot details for iPhone and iPad in Light/Dark modes, outlining the discrepancy checklist for wood-theme tiles, column layout, status overlays, and bottom safe areas. No application source code changes were made in this stage.
- **Phase 4d: ModelData + CalibreServerConfigProvider Deleted (2026-06-25):** Deleted `Models/ModelData.swift` (382 lines) and `Models/CalibreServerConfigProvider.swift` (24 lines). `AppContainer` is now the sole composition root. `AppContainerProtocol` absorbed the deleted protocol's members (`updateBook`, `getPreferredFormat`, plus the runtime state already added in 4a+4b). `CalibreServerService.config` and `init` now take `AppContainerProtocol` instead of `CalibreServerConfigProvider`. Xcode project updated to drop the two deleted file references from iOS app and Mac Catalyst targets. All 329 unit tests pass, iOS Simulator build succeeds. The full Phase 4 ModelData elimination is complete: ModelData's 1,073-line god object is replaced by a 366-line AppContainer + 130-line AppContainerProtocol.
- **Test Realm In-Memory Unification (2026-06-25, complete):** Six-commit refactor (`refactor/test-in-memory-realm` branch) that introduces a `ServerScopedRealmConfigurationProviding` protocol boundary, swaps the per-server sidecar Realm (reading position, reader preference, FolioReader settings) from disk-backed (`Application Support/<server-uuid>.realm`) to in-memory for tests, and migrates the full test suite to a single `MockAppContainerFactory` entry point. The pre-existing `testEndSession_logsActivity` state-pollution failure is fixed (the test now passes 3-for-3 across repeat runs in the same process). New files: `Models/Repositories/ServerScopedRealmConfigurationProviding.swift`, `DefaultServerScopedRealmConfigurationProvider.swift`, `InMemoryServerScopedRealmConfigurationProvider.swift`, `Models/Testing/TestRealmEnvironment.swift`, `TestHelpers/MockAppContainerFactory.swift`, `MockAppContainerFactoryTests.swift`. Sidecar Realm tests verify the in-memory identifier, distinct identifiers per container, same-UUID caching, and the original pollution scenario. AppContainer.init gains an optional `testRealmEnvironment` parameter. Full unit suite: 338/338 (332 + 6 new) green.
- **Test Realm Audit Fixes (2026-06-26, complete):** Two follow-up commits to the test-realm-in-memory refactor. (1) `CalibreServer.realmPerf` extension was routing every call through `AppContainer.shared` and keying the thread-local cache on `realmPerf-<uuid>`, which aliased the same `LocalServerUUID` from two different test containers onto a single cached Realm. The fix moves the lookup to a new `CalibreServer.realm(in: AppContainer)` method that includes `ObjectIdentifier(container)` in the cache key; the single call site in `Views/ShelfView/RecentShelfView.swift` is updated accordingly. (2) Several migrated test files passed literal `testName` strings to `MockAppContainerFactory.makeContainer(testName:)` (e.g. `"MainViewModelTests-${UUID().uuidString}"`), so every run shared the same in-memory Realm identifier and writes from one test method leaked into the next. 13 files are fixed across three typo variants (6 with `${...}`, 6 with `{...}`, 1 with `(...)`); all are corrected to `\(UUID().uuidString)`. New regression test `testPerContainerSidecarRealmDistinguishesContainersByConfig` covers the cache-key fix. Full unit suite: 339/339 green.
- **Comprehensive Test Failures Fix (2026-06-26, complete):** Four follow-up commits resolve a cluster of test failures reported after the test-realm refactor landed. Three independent root causes were at play: (1) `AppContainer.swift:222` force-unwrapped `libraryManager.calibreLibraries.first!.value`, which crashed with 'Unexpectedly found nil' whenever the bootstrap returned an empty library list. The trigger was the iOS Simulator's per-process file-descriptor limit: each `MockAppContainerFactory.makeContainer` call opened a new in-memory main Realm, and ~339 tests exhausted the limit (last test to hit the cap was `UnifiedCategoryServiceTests.testFetchAndCacheCategorySuccess` with 'Too many open files'). Replaced the force-unwrap with a `guard let` that skips the mock-data population block on bootstrap failure; the container still constructs and any test not depending on the populated mock data continues to pass. (2) Added a per-testName cache to `MockAppContainerFactory` so the in-memory main Realm and `InMemoryServerScopedRealmConfigurationProvider` are reused across tests in the same class. `AppContainer` instances remain fresh per test (so per-instance in-memory state is reset), but file descriptors are no longer per-test. Updated ~20 test files to use class-scoped `testName` values (`"ClassName"` instead of `"ClassName-\(UUID().uuidString)"`); `BookDetailViewModelTests.swift` had four inner test classes (ReadingPositionViewModelTests, ActivityListViewModelTests, LibraryViewModelTests, ReadingPositionRepositoryThreadingTests) all incorrectly using the `BookDetailViewModelTests-` prefix, renamed each. `MockAppContainerFactoryTests` is unchanged because its tests already use distinct testNames and explicitly assert per-call isolation; the new cache is a no-op for them. (3) `SectionShelfViewModel` used `.collect(.byTime(RunLoop.main, .seconds(1))).receive(on: DispatchQueue.main)` for batching. Both operators require their target scheduler to be running on the main thread, but `async` tests on the MainActor yield the main thread to async machinery, not the RunLoop or main dispatch queue. Removed both operators; the sink is now wired directly to `discoverShelfItemsSubject` and fires synchronously on the send thread. All current `send` callers are on the main thread (YabrShelfDataModel.notifyDiscoverShelfChanged is `@MainActor`; addToShelf wraps its send in `Task { @MainActor in }`), so the synchronous sink runs on the main thread in production. The 1-second batching buffer was a perf optimization that just deferred applyFiltering; removing it makes the viewModel more responsive. Tests `SectionShelfViewModelTests.testUpdateShelfModels` and `ShelfDisplayModelsTests.testSectionShelfViewModelMappingAndFilters` changed from `async`/`await fulfillment`/`Task.sleep` to synchronous assertions immediately after `send` — the sink is now synchronous, and the assertion must happen before the YabrShelfDataModel bootstrap's later "Author: Unknown" emission overwrites displaySections. (4) `UnifiedSearchIntegrationTests.testUnifiedSearchEndToEndWithMockServer` sink was fulfilling the expectation on every non-empty emission, which the publisher can do more than once (cache hit then network result, or two cache hits). Added `&& finalResult == nil` guard so the expectation is fulfilled only once. Full unit suite: 339/339 green; iOS Simulator and Mac Catalyst builds succeed.
- **Database Bootstrap Error Propagation Fix (2026-06-25):** Addressed two issues from the post-Phase-4 audit. (P1) `AppContainer.initializeDatabase()` previously caught and silently swallowed bootstrap errors, allowing `YetAnotherEBookReaderApp` to close the upgrade overlay, call `enableProbeTimer()`, and send `.active` on the activity subject even when the Realm failed to open. The method now rethrows, throwing `DatabaseBootstrapError.realmConfigurationMissing` when called before `tryInitializeDatabase` populated the configuration. The `onChange(of: scenePhase)` block in `YetAnotherEBookReaderApp.swift` now wraps `initializeDatabase()` in `do/catch` and only runs `enableProbeTimer()` / `bookReaderActivitySubject.send(newScenePhase)` on success, keeping the upgrade overlay up with the error message otherwise. (P2) `DatabaseBootstrapper.bootstrap` previously used `try? Realm(...)` for the background metadata Realm and silently lost the result; `CalibreBookManager.getBooksMetadata` would then skip persistence with no log. The metadata open is now wrapped in `do/try/catch/throw DatabaseBootstrapError.metadataRealmOpenFailed(underlying:)`; `.sync` rethrow propagates the error out of the queue closure. `AppContainerProtocol.initializeDatabase()` signature changed to `throws`. `AppContainer.init(mock:)` uses `try?` so previews still tolerate the failure. `AppContainerProtocol` and `AppContainer` updates include the new `throws` signature. Added `YetAnotherEBookReaderTests/DatabaseBootstrapperTests.swift` (3 tests) covering: `testBootstrapRethrowsRealmOpenFailed` (broken config), `testInitializeDatabaseRethrowsBootstrapErrors` (broken config through the public API), and `testInitializeDatabaseThrowsWhenConfigurationMissing` (nil `realmConf`). The metadata-Realm error path is verified by code review of the same `do/try/catch/throw` pattern. All 332 unit tests pass (1 pre-existing `testEndSession_logsActivity` failure documented in AGENTS.md, unrelated to this change).
- **Phase 4c: View / ViewModel / Adapter Renamed to AppContainer (2026-06-25):** Bulk-renamed `ModelData` → `AppContainer` and `modelData` → `container` across 36 app source files and 6 test files (95 files, 1,171 insertions / 1,150 deletions). `YetAnotherEBookReaderApp.swift` now creates `AppContainer()` as the sole `@StateObject`. All 16 View files declare `@EnvironmentObject var container: AppContainer`. All 9 ViewModels + the `YabrEBookReaderNavigationController`, `EpubFolioReaderContainer`, and `FolioReaderViewController` adapters take `container: AppContainer`. The 11 manager / repository / init-helper files updated in 4a+4b had their `init` and `setup` parameter label renamed from `modelData:` to `container:` so the composition root call sites still bind. `AuthPlugin` in `Network/ImageRequest.swift`, the `ModelData.shared` access sites in `CalibreCoreModels.swift` / `Views/FolioReaderView/Providers.swift` / `YabrEBookReaderMetaSource.swift` / `YabrReaderNavigationViewModel.swift` / `FolioReaderViewController.swift` / `ReadingPositionRepository.swift`, and the test `setUp` methods all switched to `AppContainer`. The `FolioReaderProviderBookIdTests` helper disambiguates the class-level `container: AppContainer` field from the `EpubFolioReaderContainer` instance by using `epubContainer`. All 329 unit tests pass.
- **Phase 4a+4b: AppContainer Introduced Alongside ModelData (2026-06-25):** Created `Models/AppContainer.swift` (~367 lines) as a verbatim mirror of `ModelData` so both classes can coexist during the remaining Phase 4 sub-steps. Decoupled 11 manager/repository/init-helper files from the concrete `ModelData` class by widening their `init(modelData:)` parameter and `weak var modelData` property types to `AppContainerProtocol`. Extended `AppContainerProtocol` to add runtime state (`calibreLibraries`, `calibreServers`, `calibreServerInfoStaging`, `librarySyncStatus`, `booksInShelf`, `deviceName`), sync progress (`updatingMetadata*`), the missing database-lifecycle and activity-log methods (`getBook`, `refreshDatabase`, `tryInitializeDatabase`, `initializeDatabase`, `migrateLegacyReadPosData`, `getCustomDictViewer`, `getCustomDictViewerNew`, `updateCustomDictViewer`, `cleanCalibreActivities`, `logStartCalibreActivity`, `logFinishCalibreActivity`), and `LibraryResolver`/`ServerResolver` conformances. Converted the three `ModelData` extensions in `ShelfDataManager.swift` (`registerRecentShelfUpdater`, `buildShelfBookItem`, `parseShelfSectionId`) to extensions on `AppContainerProtocol where Self: ObservableObject` so both classes receive them through protocol conformance. All 329 unit tests pass, iOS Simulator build succeeds. The repository-layer and helper-layer are now container-agnostic; Phase 4c (View injection) can follow.
- **Phase 4: Coverage Deepening & Boundary Testing Implemented (2026-06-24):** Expanded existing suites and added integration coverage across search, server service, book detail flows, and FolioReader provider boundaries. `UnifiedSearchServiceTests` now covers cancellation, timeout, and empty-result flows; `CalibreServerServiceTests` now covers discovery/probe and successful sync/category fetch paths; `BookDetailViewModelTests` now covers download interaction, preview manifest selection, metadata refresh, and uses a dedicated reachable remote-library fixture for async network-backed paths; `FolioReaderProviderBookIdTests` now covers concurrent highlight writes and large bookmark lists with explicit highlight cleanup isolation. Added `IntegrationTests.swift` for database initialization, full reading flow, search-to-download, and server setup/library sync smoke coverage. Removed the empty `YetAnotherEBookReaderTests.swift` template file and registered `IntegrationTests.swift` in the Xcode project. Validation: focused Phase 4 suites passed (88/88), then full `RunAllTests` passed (329/329).
- **Phase 3: Network & Download Testing Implemented (2026-06-24):** Added comprehensive unit test coverage for `BookDownloadManager` and `ReadingSessionManager` under `YetAnotherEBookReaderTests`. Refactored `BookDownloadManager` to support `sessionConfiguration` property injection, enabling mock network stubbing via `MockURLProtocol`. Verified that all unit tests pass (313/313) and macOS Catalyst build succeeds.
- **Phase 2: Repository Independent Testing Implemented (2026-06-24):** Added comprehensive unit test coverage for `RealmBookRepository`, `RealmAnnotationRepository`, `RealmReadingPositionRepository`, `RealmServerRepository`, and `RealmLibraryRepository` under `YetAnotherEBookReaderTests`. Solved an in-memory Realm cache key collision in `ReadingPositionRepository.getRealm(forBookId:)` by including `inMemoryIdentifier` when `fileURL` is nil. Verified that all unit tests pass (300/300) and macOS Catalyst build succeeds.
- **Views Realm Cleanup Phase 3b Implemented (2026-06-24):** Removed direct Realm category-cache observation from `LibraryInfoViewModel` and `UnifiedCategoryViewModel` by extending `CategoryCacheRepository` with `observeCategorySummaries()` and `observeCategoryCacheUpdates(categoryName:)`. `RealmSearchCacheStore` now owns the Realm `changesetPublisher` bridge for category summaries and per-category cache update notifications, while `UnifiedCategoryService` remains merge-only. `LibraryInfoViewModel` now subscribes to repository-driven summaries, and `UnifiedCategoryViewModel` now observes repository cache updates and retriggers merge through `UnifiedCategoryService`, including rebinding when the selected category changes. `MockCategoryCacheRepository` was promoted into `TestHelpers`, and coverage was added for repository observe behavior, `LibraryInfoViewModel` category summary updates, `UnifiedCategoryViewModel` dependency resolution/rebind/update flows, and invalidate-on-force-refresh behavior. Validation: 10 focused category tests passed, then full `RunAllTests` passed (274/274).
- **Views Realm Cleanup Phase 3a Implemented (2026-06-24):** Removed direct Realm usage from `Views/FolioReaderView/Providers.swift` by introducing a dedicated `FolioReaderProfileRepositoryProtocol` / `RealmFolioReaderProfileRepository` boundary plus `FolioReaderProfileValue`. `FolioReaderDelegatePreferenceProvider` no longer accepts `Realm.Configuration` or opens Realm directly; it now holds runtime `values` only and delegates default-profile seeding, list/load/save/remove operations to the repository. `ModelData` now exposes `folioReaderProfileRepository`, and `FolioReaderPreferenceRealm` gained value-type bridge methods (`toValue(defaults:)`, `apply(_)`). Validation: 7 focused Folio/profile tests passed via Xcode MCP `RunSomeTests`, then full `RunAllTests` passed (267/267).
- **Views Realm Cleanup Phase 2 Remaining Implemented (2026-06-24):** Removed direct Realm usage from `BookDetailViewModel`, `ActivityListViewModel`, `ReadingPositionViewModel`, and `LibraryViewModel` by extending repository boundaries with value-type publishers and query helpers. Added `ActivityLogRepositoryProtocol` / `RealmActivityLogRepository`, `BookRepositoryProtocol.observeBook(id:)`, `LibraryRepositoryProtocol.getLibrary(id:)` / `observeLibrary(id:)` / `updateLibraryFlags(id:discoverable:autoUpdate:)`, and `ReadingPositionRepositoryProtocol.debugPositions(forBookId:)` / `historyBook(for:bookId:)`. Views-layer `RealmSwift` imports are now down to 4 files: `LibraryInfoViewModel.swift`, `UnifiedCategoryViewModel.swift`, `Providers.swift`, and `ReadiumPreferenceAdapter.swift`. Validation: focused ViewModel tests passed (29/29), then full `xcodebuild test -project YetAnotherEBookReader.xcodeproj -scheme YetAnotherEBookReader -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/YabrDerivedData` passed (261 unit tests + 1 UI test).
- **Views Realm Cleanup Phase 1 Implemented (2026-06-23):** Removed `RealmSwift` imports from `MainViewModel.swift`, `SettingsViewModel.swift`, `ServerViewModel.swift`, and `YabrEBookReader.swift`. Added `ModelData.isDatabaseReady`, `ModelData.refreshDatabase()`, and removed the dead `realm:` parameter from `updateServerDSReaderHelper` across `ModelData`, `CalibreServerManager`, and all call sites. Added `ReaderPreferenceRepositoryProtocol` / `RealmReaderPreferenceRepository` so `YabrEBookReader` no longer directly opens preference Realms or writes `ReadiumPreferenceRealm` / `PDFOptions` / `FolioReaderPreferenceRealm`. Added regression coverage in `MainViewModelTests`, `SettingsViewModelTests`, and new `ReaderPreferenceRepositoryTests`. Validation: focused tests passed, then full `xcodebuild test -project YetAnotherEBookReader.xcodeproj -scheme YetAnotherEBookReader -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17'` passed (235 unit tests + 1 UI test).
- **Phase 1d: Mock Infrastructure Enhancements (2026-06-23):** Centralized mock infrastructure under a new navigator group and directory `YetAnotherEBookReaderTests/TestHelpers/`. Created reusable doubles for repositories (`MockServerRepository`, `MockLibraryRepository`, `MockBookRepository`, `MockReadingPositionRepository`, `MockAnnotationRepository`), managers (`MockServerManager`, `MockLibraryManager`, `MockBookManager`), in-memory Realm DB bootstrappers (`MockDatabaseService`), and a factory builder for domain types (`TestFixtures`). Centralized `MockURLProtocol` into `TestHelpers/` and removed its duplicate inline definition in `UnifiedSearchIntegrationTests.swift`. All 227 unit tests compile and pass successfully, and Catalyst build succeeds.
- **Phase 1c: CalibreServerManagerTests Implemented (2026-06-23):** Implemented `CalibreServerManagerTests.swift` covering `CalibreServerManager` server CRUD, reachability status helpers, DSReaderHelper config updates, background probing, and library auto-creation flows. Fixed an in-memory dictionary cache leak in `CalibreLibraryManager.removeLibrary(library:)` where deleted libraries were not cleaned up from the active in-memory `calibreLibraries` cache. All 9 focused test cases pass, the unit test suite passes with 227/227 tests succeeded, and the Catalyst build succeeded.
- **Phase 1b: CalibreLibraryManagerTests Implemented (2026-06-23):** Added `CalibreLibraryManagerTests.swift` covering `CalibreLibraryManager` library CRUD, visibility toggling (hide/restore), staging, probing, deletion cascades, and sync workflows. Configured a custom non-local mock server to intercept search-results, custom-columns, categories, and chunked book metadata fetches using `MockURLProtocol`. All 11 focused test cases pass, and the full test suite passes (218/218 tests succeeded).
- **FolioReader Default Profile Behavior Restored (2026-06-23):** Reconnected the database-backed profile store to `FolioReaderDelegatePreferenceProvider` (Phases 1-3). The regression root cause was that when the deprecated `FolioReaderRealmPreferenceProvider` was deleted in milestone P2/A07, the database-backed profile storage and default profile auto-seeding were not reconnected/migrated to the newer delegate-based provider, leaving profile management methods as stubs. To resolve this, we extended `FolioReaderDelegatePreferenceProvider` to accept `profileRealmConfig` and pass `modelData?.realmConf`. The `"Default"` profile is now automatically created in the profile Realm configuration (seeded from the user's Calibre server configuration) during provider initialization. Implemented listing (with filter support), loading, saving, and removing profiles. Added mock delegate unit test verification `testCustomProfileSaveLoadListAndRemove` to verify profile persistence, filtering, delegate didUpdatePreferences callback execution, and automatic recreate behavior. Validated focused provider tests (16/16 green) and full test suite (197/197 green).
- **FolioReader Position Restore Race Fix Implemented (2026-06-23):** Fixed an intermittent FolioReader reading-position restore failure caused by `RealmReadingPositionRepository.savePosition(_:forBookId:)` deleting old same-identity positions in one Realm write transaction and adding the replacement in a second transaction. FolioReaderKit saves `savedPositionForCurrentBook` asynchronously in `viewWillAppear`, then immediately calls `reloadData()` and synchronously asks the provider for the current position; the previous two-transaction save path could expose a momentary zero-row state to `getPosition`. `savePosition` now performs stale-position deletion, same-identity existence check, and replacement insert inside one Realm write transaction. Added regression coverage in `FolioReaderProviderBookIdTests` for atomic older-position replacement, provider restore after replacement, and concurrent save/read behavior that must not return nil. Validation: focused `FolioReaderProviderBookIdTests` passed, and full `xcodebuild test -project YetAnotherEBookReader.xcodeproj -scheme YetAnotherEBookReader -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/YabrDerivedData` passed.
- **FolioReader Highlight Position Restore Fix Implemented (2026-06-23):** Implemented the fix from `.agents/plans/FOLIO_READER_HIGHLIGHT_POSITION_RESTORE_IMPLEMENTATION.md`. The `BookHighlight.toFolioReaderHighlight()` extension in `Views/FolioReaderView/Providers.swift` now calls `highlight.encodeContents()` before returning, so `contentEncoded`, `contentPreEncoded`, and `contentPostEncoded` are populated. FolioReaderKit's `Bridge.js` `injectHighlight()` calls `decodeURIComponent(oHighlight.contentEncoded)` on every injected highlight; without encoded content the decode throws, aborts the highlight batch, and blocks the page load that also restores reading position. Also fixed `EpubFolioReaderContainer.applyHighlights(_:)` to obtain the provider through `folioReaderHighlightProvider(self.folioReader)` instead of only consuming the optional cached property, so persisted highlights are never dropped when `applyHighlights` runs before the provider exists. Added a fileprivate `ensureEncoded` last-line guard applied to every `FolioReaderHighlight` returned to FolioReaderKit (`getById`, `allByBookId andPage`, `all`). Added 4 regression tests in `FolioReaderProviderBookIdTests`: `testBookHighlightToFolioReaderHighlightPopulatesEncodedContent`, `testHighlightProviderReturnsEncodedHighlightsForFolioReaderRuntimeId`, `testEpubFolioReaderContainerApplyHighlightsCreatesProviderWhenNil`, and `testCombinedPositionAndHighlightRestoreViaFolioReaderRuntimeId`. Validation: focused `FolioReaderProviderBookIdTests` 11/11 green, full `xcodebuild test` green (191 tests).
- **FolioReader Book-ID Compatibility Fix Implemented (2026-06-23):** Fixed legacy FolioReader reading-position restore when FolioReaderKit calls providers with the EPUB filename-derived runtime `bookId` instead of Yabr's canonical `CalibreBook.bookPrefId`. Added `FolioReaderBookIdentity` in `Views/FolioReaderView/Providers.swift` so FolioReader providers accept both ids while all repository reads/writes continue using canonical `book.bookPrefId`. Updated read-position, highlight, and bookmark providers to normalize incoming FolioReaderKit ids; `folioReaderReadPosition(_:set:completion:)` now always calls completion. Also corrected FolioReader bookmark save status handling so repository success states `0/1/2` do not surface as runtime errors. Added `FolioReaderProviderBookIdTests` covering canonical/runtime/unrelated ids, reading-position restore via runtime id, completion behavior, highlight query/add canonicalization, and bookmark add canonicalization. Validation: focused FolioReader provider tests passed (7 tests), then full `xcodebuild test -project YetAnotherEBookReader.xcodeproj -scheme YetAnotherEBookReader -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/YabrDerivedData` passed (187 unit tests + 1 UI test).
- **P2/A26 Stage A26-S5 Harden Lifecycle And Settings Toggles Implemented (2026-06-22):** De-duplicated volume observer registration and AudioSession activation. Removed premature `setupVolumeKeyPaging` from `init` to prevent premature UIKit view loading (which triggered `viewDidLoad` too early). Added an idempotency guard at the beginning of `setupVolumeKeyPaging(isEnabled:)` to skip configuration if the observer and volumeView are already active. Verified that observer teardown and AudioSession deactivation safely release system resources. All 180 unit tests compiled and passed successfully.
- **P2/A26 Stage A26-S4 Replace Event Unlock Delay With Explicit Completion Implemented (2026-06-22):** Removed the fixed 0.1-second delay for resetting baseline volume and unlocking the paging coordinator. Introduced `performVolumeKeyPage(up:) async` on `YabrReadiumReaderViewController` and overrode it in `YabrReadiumEPUBViewController`. Implemented an async `scroll(_:to:)` animation helper using a safe non-blocking polling loop to await scroll animation completion in EPUB scroll mode. Updated KVO observer callback to await `performVolumeKeyPage` before resetting baseline system volume and unlocking the coordinator, creating a robust completion-based action boundary. All 180 unit tests compiled and passed successfully.
- **P2/A26 Stage A26-S3 Replace Slider Polling With Layout-Driven Readiness Implemented (2026-06-22):** Replaced the recursive, wall-clock slider polling retry loop in `YabrReadiumReaderViewController.swift` with a layout-driven discovery strategy using the new static method `ReadiumVolumeKeyPagingCoordinator.findVolumeSlider(in:)`. Tracked a `pendingBaselineVolume` that gets applied immediately when the `UISlider` becomes discoverable during layout (`viewDidLayoutSubviews()`). Added a dedicated unit test `testFindVolumeSlider()` verifying recursive slider discovery on synthetic view hierarchies. All 180 unit tests compiled and passed successfully.
- **P2/A26 Stage A26-S1 & S2 Readium Volume Key Paging Coordinator Extracted (2026-06-22):** Extracted pure event resolver `ReadiumVolumeKeyEventResolver` and coordinator `ReadiumVolumeKeyPagingCoordinator` into a dedicated file under `Views/ReadiumView/`. Integrated the coordinator into `YabrReadiumReaderViewController.swift`, replacing direct properties (`isHandlingVolumeChange`, `lastRequestedVolume`) with coordinator delegations. Created comprehensive unit tests in `ReadiumVolumeKeyPagingCoordinatorTests.swift` covering direction logic, programmatic reset matching, massive volume jump adjustments, and busy-state suppression. All 179 unit tests compiled and passed successfully.
- **P2/A26 Readium Volume Key Timing Plan Captured (2026-06-22):** Updated `.agents/plans/REFACTOR_PLAN.claude.md` and `.agents/plans/REFACTOR_PLAN.claude.progress.md` to mark A27 complete (commit `1e053da`) and select A26 as the next execution item. Added `.agents/memory-bank/p2-a26-readium-volume-key-timing-plan.md` after auditing the live Readium code: both fixed `0.1s` delays are in the volume-key paging path (`MPVolumeView` slider readiness and event unlock/baseline reset), not Readium locator loading. The plan recommends extracting a pure event resolver/coordinator first, then replacing slider polling with layout-driven readiness and replacing the event unlock delay with explicit navigation/scroll completion handling.
- **P2/A27 Stage A27-S6 Remote Payload Boundary Cleanup Implemented (2026-06-22):** Deleted unused legacy `toCalibreBookAnnotationHighlightEntry()` and `toCalibreBookAnnotationBookmarkEntry()` extensions on `BookHighlightRealm` and `BookBookmarkRealm` from `BookAnnotationRealm.swift`. The codebase now uses domain-value mappers on `BookHighlight` and `BookBookmark` for Calibre API payload conversion, successfully separating Realm models from remote sync details. All 174 unit tests compiled and passed successfully.
- **P2/A27 Stage A27-S5 Standardize Realm Write Helpers Implemented (2026-06-22):** Introduced a generic `replaceAll(_:)` extension on Realm `List`. Defined in-place `applyDomain(_:)` updates on all 7 Realm model entities to prevent duplicate mapping code, and refactored `makeRealmObject()` to delegate to `applyDomain(_:)`. Refactored `RealmAnnotationRepository.saveBookmark(_:)` to perform in-place updates on existing managed bookmarks using `applyDomain(_:)`. Guarded primary keys and identity fields against modification when objects are already managed. Added test coverage in `RealmDomainMappingTests` and successfully ran all 174 unit tests.
- **P2/A27 Stage A27-S4 Reduce Manager, Service, And ViewModel Conversion Bridges Implemented (2026-06-22):** Refactored calls to `CalibreBook(managedObject:library:)` in `LibrarySearchService` and `RealmSearchCacheStore` to use `toDomain(library:)`, refactored `CalibreBookManager.convert(library:bookRealm:)` to call the mapper directly, and refactored direct Realm-to-domain mapping in `ReadingSessionManager` and `ReadingPositionViewModel` to call the new `.toDomain()` mapper methods directly, removing duplicate conversion shims outside the persistence boundary. All 173 unit tests compiled and passed successfully.
- **P2/A27 Stage A27-S3 Centralize Repository Callers Implemented (2026-06-22):** Refactored all 5 core persistence repositories (`RealmServerRepository`, `RealmLibraryRepository`, `RealmBookRepository`, `RealmAnnotationRepository`, and `RealmReadingPositionRepository`) to directly call the newly extracted explicit domain mappers (`toDomain()` and `makeRealmObject()`) instead of using legacy manual mapping blocks or the compatibility facades (`toValue()`, `init(value:)`). Open database connections and transaction management were preserved within repository scopes. All 173 unit tests compiled and passed successfully.
- **P2/A27 Stage A27-S2 Extract Explicit Domain Mappers Implemented (2026-06-22):** Extracted explicit Realm-to-domain and domain-to-Realm object mapping methods into three newly created dedicated mapper files (`CalibreRealmMappers.swift`, `ReadingPositionRealmMappers.swift`, and `AnnotationRealmMappers.swift`) under `Models/Realm/`. Registered the files to iOS and Mac Catalyst targets. Refactored the legacy mapping extensions (`Persistable`, `toValue()`, `init(value:)`) in model and repository files to delegate directly to the new mappers to preserve 100% backward compatibility. Extended the `RealmDomainMappingTests` suite to test the new mapper methods directly. All 173 unit tests compiled and passed successfully.
- **P2/A27 Realm Value Conversion Plan Captured (2026-06-22):** Added `.agents/memory-bank/p2-a27-realm-value-conversion-plan.md` after auditing current Realm/domain conversion sites. The plan recommends a conservative staged path: first add golden mapper tests for server/library/book/annotation/reading-position round trips, then extract explicit mapper helpers under `Models/Realm/`, migrate repositories first, reduce manager/service/view-model conversion bridges, standardize write-side `makeRealmObject`/`applyDomain` helpers, and finally clean up remote annotation payload boundaries plus documentation guardrails. No schema change, migration bump, macro/codegen pass, or reader-adapter merge is recommended for the first implementation.
- **P2/A21 Missing Format Auto-downloads Restored (2026-06-22):** Restored the legacy auto-download side effect in the recent-shelf mapper inside `ShelfDataManager.swift`. When the Calibre server is reachable, any selected book format that is not yet cached locally will be automatically queued for download via the `bookFormatDownloadSubject`. Verified that all 159 unit tests and 1 UI test pass successfully.
- **P2/A21 Discover Shelf Initialization Bug Fixed (2026-06-22):** Resolved the bug where the Discover shelf remained empty initially after deleting `SectionShelfController`. Modified `SectionShelfViewModel.swift` to safely bootstrap `shelfDataModel` when `modelData.logger` is initialized (on database readiness), seeding initial discover items immediately and avoiding early-exit crashes during early app startup. Verified that all 159 unit tests and 1 UI test pass successfully.
- **P2/A21 Stage A21-S6 UIKit Shelf Controllers and ShelfView Dependency Removed (2026-06-22):** Deleted the legacy `RecentShelfController.swift`, `RecentShelfUI.swift`, `SectionShelfController.swift`, and `SectionShelfUI.swift` files from the project and filesystem. Completely removed all remaining `ShelfView` dependency imports, subjects, and obsolete properties from `RecentShelfViewModel`, `SectionShelfViewModel`, `ModelData`, `CalibreBookManager`, `ShelfDataManager`, `ShelfDisplayModels`, `MainView`, and associated unit tests. Verified that all 159 unit tests and 1 UI test pass successfully.
- **P2/A21 Stage A21-S5 Switch MainView to SwiftUI Shelves (2026-06-22):** Completed Stage A21-S5 by updating `MainView.swift` to render native SwiftUI `RecentShelfView` and `SectionShelfView` directly inside the `TabView` instead of UIKit wrappers. Updated `MainViewModel.swift` to instantiate and own `RecentShelfViewModel` and `SectionShelfViewModel` to manage their lifecycle. Verified compilation and ran all 163 unit tests and 1 UI test successfully.
- **P2/A21 Stage A21-S4 SwiftUI Discover/Section Shelf Built (2026-06-22):** Completed Stage A21-S4 by implementing native SwiftUI `SectionShelfView` displaying sectioned horizontal shelf rows, library filter menus (with checkmark and Reset actions), top-bar refresh/edit actions, and a bottom download confirmation panel. Extracted the card UI to a shared, customizable `ShelfBookCard.swift` and refactored `RecentShelfView.swift` to use it. Extended `SectionShelfViewModel` with state management for selections, download alerts, and detail routing. Added unit tests in `SectionShelfViewModelTests.swift`, registered all new files, and verified that all 163 unit tests and 1 UI test pass successfully.
- **P2/A21 Stage A21-S3 SwiftUI Recent Shelf Built (2026-06-22):** Completed Stage A21-S3 by implementing native SwiftUI `RecentShelfView` with a responsive LazyVGrid, cover loading via `KFImage`, progress bars, status badges, contextual long-press menus, and dynamic sheets/modals for `BookDetailView` and `ReadingPositionHistoryView`. Extended `RecentShelfViewModel` with state management for alerts, detail/history modals, tap routing, and deletion auto-dismissal. Added unit tests for these flows in `RecentShelfViewModelTests.swift`, registered the new file, and verified that all 159 unit tests and 1 UI test pass successfully.
- **P2/A21 Shelf SwiftUI Native Migration Plan Captured (2026-06-22):** Updated `.agents/plans/REFACTOR_PLAN.claude.md` and `.agents/plans/REFACTOR_PLAN.claude.progress.md` to reflect completed P1/A09, P2/A12+A24, P2/A16, P2/A17, and P2/A19. Selected P2/A21 as the next project because shelf ViewModels already exist and `ShelfDataManager` has moved to `Models/`, making a staged UIKit-to-SwiftUI migration feasible. Added `.agents/memory-bank/p2-a21-shelf-swiftui-native-plan.md` with seven testable stages: behavior audit/safety net, app-owned display models, SwiftUI Recent shelf, SwiftUI Section shelf, `MainView` switch, UIKit controller removal, and documentation handoff.
- **P2/A16 Stage A16-S3 Isolate Format Row and Preview State (2026-06-22):** Completed Stage A16-S3 by extracting `BookFormatRow` from `BookFormatList.swift` and registering the new file to targets. Exposed unit-testable download queries (`isFormatDownloading(bookId:format:)` and `getActiveDownload(bookId:format:)`) and format display methods in `BookDetailViewModel`. Centralized preview sheet presentation at the list level (`BookFormatList`) to prevent SwiftUI multi-sheet registration conflicts. Cleaned up legacy unused code in `BookDetailView` (the duplicate `previewViewModel`, `handleBookDeleted()`, and `generateCommentWithTOC(comments:toc:)`). Added 3 new unit tests in `BookDetailViewModelTests`. Validated with a full `xcodebuild test` run (131 unit tests + 1 UI test passed).
- **P2/A16 Stage A16-S2 Decouple BookDetail Subviews from Global Reads (2026-06-21):** Completed Stage A16-S2 by moving reading progress summary selection and availability check methods to `BookDetailViewModel` (`ReadingProgressSummary` enum, `getReadingProgressSummary(for:)`, and `hasReadingHistory(for:)`). Decoupled `BookProgressSection` from `ModelData.shared` to read exclusively from the view model. Added 6 focused unit tests verifying progress priorities (Goodreads read date, Goodreads progress percentage, local progress on current device, fallback device progress, none, and reading history availability). Resolved thread-local configuration caching freshness issues in `RealmReadingPositionRepository` by calling `cachedRealm.refresh()`. Validated with a full `xcodebuild test` run (128 unit tests + 1 UI test passed).
- **P2/A16 Stage A16-S1 BookDetail Composition Files Split (2026-06-21):** Completed Stage A16-S1 by extracting `BookDetailContentView` and `BookDetailToolbar` from `BookDetailView.swift` and splitting the monolithic `BookDetailSubviews.swift` into 6 separate view files (`BookCoverView.swift`, `BookMetadataSection.swift`, `BookProgressSection.swift`, `BookConnectivitySection.swift`, `BookFormatList.swift`, and `BookCountPagesCorner.swift`). Deleted `BookDetailSubviews.swift` from the project. Validated with a full `xcodebuild test` run (122 unit tests + 1 UI test passed).
- **P2/SettingsView Add Server Navigation Loop Resolved (2026-06-21):** Resolved the Add Server sheet dismissal and immediate re-presentation loops in `SettingsView`. Refactored `AddModServerView` and `ServerViewModel` to accept and handle optional `CalibreServer?` parameters, allowing the "Add Server" form to pass `.constant(nil)` and eliminating the need for `dummyServer` mock instances. Validated with a full `xcodebuild test` run (122 unit tests + 1 UI test passed).
- **P2/A16+A17 BookDetailView + LibraryInfoBookListView Split Plan Captured (2026-06-21):** Added `.agents/memory-bank/p2-a16-a17-bookdetail-librarybooklist-plan.md` after auditing the current post-MVVM state. `BookDetailView.swift` is now only 185 lines, but `BookDetailSubviews.swift` has become a 467-line mixed presentation file with residual `ModelData.shared` reads in `BookProgressSection`. `LibraryInfoBookListView.swift` remains 432 lines and still owns local UI state, grouping, header/footer/toolbar, row navigation, context menus, and direct `ModelData` actions. The plan recommends staged, testable decomposition: first split BookDetail files with zero behavior change, then remove global reads and isolate format/preview rows; next introduce `LibraryInfoBookListViewModel`, extract header/footer/toolbar/content/context-menu components, move grouping and row actions out of the view body, and finish with targeted tests plus full `xcodebuild test`.
- **P2/A19 Stage A19-S7 & A19-S8 Legacy Facades, View & Logging Boundary Cleaned Up (2026-06-20):** Refactored legacy network facades to return typed results (`DownloadStartError`) or throwing async calls (`getMetadata`, `getBookManifest`, `updateMetadata`). Modernized `DictViewModel` autocomplete hints networking with HTTP validation and error logging in a dedicated async `loadHints` method, conformed VM to `ObservableObject`, and refactored `MDictViewEdit` to update from it. Deleted unused `ImageLoader`/`YabrAsyncImage` dead code from `Utils.swift`. Modernized zip export errors in `SupportInfoViewModel` with conformed `SupportInfoError` types. Cleaned up noisy debug `print` statements in `ImageRequest`, `CalibreServerService`, and `BookDownloadManager`. Verified with a full `xcodebuild test` run (122 unit tests + 1 UI test passed).
- **P2/A19 Stage A19-S6 Upload and Task-Builder Failure Visibility Implemented (2026-06-20):** Added optional `error: CalibreAPIError?` property to `CalibreBookSetLastReadPositionTask` and `CalibreBookUpdateAnnotationsTask` in `CalibreTasks.swift`. Refactored `buildSetLastReadPositionTask` and `buildUpdateAnnotationsTask` in `CalibreServerService` to throw errors instead of returning optionals. Caught and logged builder errors inside `CalibreBookManager.swift`. Modernized upload Combine publishers to return `CalibreAPIError` directly, and updated async upload helpers to catch and store the `CalibreAPIError` on tasks, logging localized descriptions to Calibre activities instead of generic `"Unknown"` values. Added unit and publisher success/failure tests to `CalibreServerServiceTests.swift`. Verified that all 112 unit tests + 1 UI test passed successfully.
- **P2/A19 Stage A19-S5 Modern CalibreAPIError Publisher Overloads Implemented (2026-06-20):** Overloaded/modified `getAnnotations(task:)`, `getBooksMetadata(task:qos:)`, `getLastReadPosition(task:)`, and `getMetadataNew(task:)` to return Combine publishers with failure type `CalibreAPIError` instead of down-mapping to `URLError`. Added thin, deprecated adapters matching the legacy `URLError` signatures for backward compatibility. Implemented success and failure unit tests for all 4 publishers in `CalibreServerServiceTests.swift`. Verified that all 107 unit tests + 1 UI test compile and pass successfully.
- **P2/A19 Stage A19-S4 Search Error Preservation Implemented (2026-06-19):** Restructured `SearchError` enum in `UnifiedSearchModels.swift` to wrap `CalibreAPIError` and introduced `invalidState` and `cancelled` cases. Conformed `CalibreAPIError` to `Sendable` and `Equatable`. Routed search HTTP fetches in `LibrarySearchService.swift` through `CalibreServerService.validatedData` and mapped network/cancellation errors accordingly. Added `testUnifiedSearchFailureHttpStatus` in `UnifiedSearchIntegrationTests.swift`. Verified with a full `xcodebuild test` run (99 unit tests + 1 UI test passed).
- **P2/A19 Stage A19-S3 DSReader Helper Result Semantics Implemented (2026-06-19):** Introduced throwing async methods for Goodreads shelf and progress updates in `DSReaderHelperConnector.swift` using `CalibreServerService.validatedData(for:server:)`. Migrated `CalibreBookManager` shelf operations and `ReadingSessionManager` reading position syncing to use the new throwing async methods under concurrent `Task` contexts (with error logging) or direct await boundaries. Added unit tests covering success/failure scenarios in `DSReaderHelperConnectorTests`. Verified with a full `xcodebuild test` run (98 unit tests + 1 UI test passed).
- **P2/A19 Stage A19-S2 Metadata Empty-Entry Protection Implemented (2026-06-19):** Refactored `getMetadata(task:)` in `CalibreServerService+Metadata.swift` to return `AnyPublisher<(CalibreBookTask, CalibreBookEntry), CalibreAPIError>` and propagate network/parsing errors down the stream, preventing silent failures and replacing errors with empty dummy `CalibreBookEntry` objects. Added `testGetMetadataSuccess`, `testGetMetadataFailureHttpStatus`, and `testGetMetadataFailureDecode` to `CalibreServerServiceTests`. Verified compilation and unit tests pass (94 unit tests + 1 UI test passed).
- **P2/A19 Stage A19-S1 Category Fetch HTTP Validation Implemented (2026-06-19):** Routed HTTP/HTTPS category requests in `LibraryCategoryService` through `CalibreServerService.validatedData` to ensure response status code verification and structured error reporting. Handled non-HTTP schemes with a fallback to the standard `URLSession.data(from:)` call. Added the `testFetchAndCacheCategoryFailureHttpStatus` test to `UnifiedCategoryServiceTests` to confirm that HTTP 4xx errors throw a `CalibreAPIError` and leave the cache repository unmodified. Verified with a full `xcodebuild test` run (91 unit tests + 1 UI test passed).
- **P2/A12+A24 RealmModel Split + Readium Decoupling Implemented & Crash Resolved (2026-06-19):** Split the massive monolithic `RealmModel.swift` file into individual, focused Realm schema files under `YetAnotherEBookReader/Models/Realm/`. Moved `FolioReaderPreferenceRealm` from views to models, and decoupled the `Models/` layer from Readium Navigator/Shared dependencies by extracting mapping extensions of `ReadiumPreferenceRealm` into the view/adapter layer `Views/ReadiumView/ReadiumPreferenceAdapter.swift`. Resolved an `EXC_BAD_ACCESS` crash in `swift_retain` when copying `Realm.Configuration` on concurrent background queues by resolving the configuration dynamically in `RealmSearchCacheStore.getRealm()` and stripping the `migrationBlock` and `shouldCompactOnLaunch` closures prior to opening the database. Verified that the app builds and all 90 unit tests + 1 UI test pass.
- **ModelData Elimination Phase 3 Completed (2026-06-25):** Introduced `Models/AppContainerProtocol.swift` (~95 lines) — a single facade protocol aggregating all repositories, managers, services, subjects, and runtime state. `ModelData` now conforms to `AppContainerProtocol` alongside the existing `CalibreServerConfigProvider` and `LibraryProvider`. Deleted 6 dead wrapper methods (`probeServer`, `removeServer`, `probeLibrary`, `removeLibrary`, `getBooksMetadata`, `syncLibrary`) — all were single-call forwarders to a manager and all callers were already using `modelData.xxxManager.xxx` directly. Extracted 4 init helpers (`setupRealmDefaults`, `setupImageCache`, `wireCrossManagerSubscriptions`, `wireObjectWillChangeForwarding`) plus a generic `forwardObjectWillChange<T: ObservableObject>(of:)` helper that eliminates 4 × 5 lines of duplicate objectWillChange-forwarding code. `init` body shrank from 117 → 80 lines (-32%). `ModelData.swift`: 398 → 382 lines (-4% in Phase 3, **-64% cumulative** from Phase 0). 3 atomic commits: `bb8a0f6` (3a AppContainerProtocol), `0b66d31` (3c dead-wrapper deletion), `bb14d4e` (3b init helpers). `CalibreServerConfigProvider` and `LibraryProvider` retained as independent narrow protocols — a typealias would cause a circular reference, and several services (`CalibreServerService.config`, `UnifiedSearchService.libraryProvider`) still depend on them. Verified with `xcodebuild test`: 33 test suites pass, 1 pre-existing failure (`ReadingSessionManagerTests.testEndSession_logsActivity`) is unrelated.
- **ModelData Elimination Phase 2 Completed (2026-06-25):** Extracted 2 new files (`Models/DatabaseMigrator.swift` 259 lines for v42-v140 Realm migrations, `Models/DatabaseBootstrapper.swift` 92 lines for database initialization) and restored + moved `registerSyncServerHelperConfigCancellable` from `ModelData` (was deleted in Phase 1 by mistake) to `CalibreServerManager`. Deleted 8 `// →` forwarding methods and consolidated cross-manager logic: `updateServerLibraryInfo` (now in `libraryManager` with `serverManager` call), `refreshShelfMetadataV2` (now in `bookManager`). Cleaned up `CalibreServerConfigProvider` protocol by removing unused `getBookRealm` and `refreshShelfMetadataV2` requirements. `ModelData.swift`: 763 → 398 lines (-48% in Phase 2, **-67% cumulative** from Phase 0). Added `TestFixtures.populateModelDataWithMockBook` helper for tests that construct `ModelData()` and need to populate mock data manually (the inline ~65-line mock block in `init(mock:)` is kept because view previews in the app target can't access test-target code). 5 atomic commits: `a2d30dc` (2a DatabaseMigrator), `6148b32` (2b DatabaseBootstrapper), `a6fae78` (2d SyncServerHelper), `5a8f590` (2e forwarding deletion), `5cf61d6` (2c TestFixtures helper). Verified with `xcodebuild test`: 33 test suites pass, 1 pre-existing failure (`ReadingSessionManagerTests.testEndSession_logsActivity`) is unrelated.
- **ModelData Elimination Phase 1 Completed (2026-06-24):** Removed 84 `// FACADE: <Manager>`-marked methods and properties from `ModelData.swift`, reducing it from 1,172 → 763 lines (-35%, -409 lines). Migrated 30+ files (10 ViewModels, 15+ Views, 5 Managers, 10+ Tests) to use `modelData.xxxManager.xxx` pattern instead of FACADE accessors. Kept 6 protocol-conformance computed properties (`calibreLibraries`, `calibreServers`, `librarySyncStatus`, `calibreServerInfoStaging`, `booksInShelf` for `CalibreServerConfigProvider`; `getLibraries`/`isServerReachable` for `LibraryProvider`) as thin protocol-conformance wrappers. Kept 5 `// → <TargetManager>`-marked non-Facade methods for Phase 2 (`populateBookShelf`, `populateLibraries`, `populateLocalLibraryBooks`, `onOpenURL`, `calcLocalFileBookId`, `loadLocalLibraryBookMetadata`, `updateServerLibraryInfo`, `refreshShelfMetadataV2`, `registerSyncServerHelperConfigCancellable`). Fixed pre-existing Swift type-check timeout in `ShelfDataManager.swift:318` by extracting the map closure into a separate `buildShelfBookItem` method. ViewModel `init(modelData:)` signatures are preserved for Phase 3-4 cleanup. Verified with `xcodebuild test` (all 33 test suites pass; 1 pre-existing failure `ReadingSessionManagerTests.testEndSession_logsActivity` is unrelated to this change).
- **ModelData Elimination Phase 0 Completed (2026-06-24):** Established baseline metrics (1,073 lines, 82 methods, 17 lazy var, 62 files referencing ModelData, 41 production ModelData.shared sites in 15 files). Fixed 4 force-unwraps in ModelData.swift (`realm`, `realmSaveBooksMetadata`, `realmConf`, `logger` → all `?`) with cascading fixes in `ReadingSessionManager.swift`, `ShelfDataManager.swift`, and 3 test files. Added 84 `// FACADE: <Manager>` markers and 10 `// → <TargetManager>` markers across `ModelData.swift` to mark all pure forwarders and non-Facade methods. Created `.agents/memory-bank/modeldata-baseline-2026-06-24.md` with full baseline. Verified with `xcodebuild build` (pre-existing failures in `ReadingSessionManagerTests` and `ShelfDisplayModelsTests` are unrelated to this change).
- **P2/A12+A24 RealmModel Split + Readium Decoupling Plan Captured (2026-06-18):** Added `.agents/memory-bank/p2-a12-a24-realmmodel-readium-plan.md` after analyzing the 1357-line `Models/RealmModel.swift`, `BookPreference.swift`, Readium reader adapters, and FolioReader preference storage. The recommended sequence is: first perform a zero-schema-change split into focused Realm files under `Models/Realm/`; move `FolioReaderPreferenceRealm` out of `Views/FolioReaderView/Providers.swift` into the model/persistence layer; then move `ReadiumPreferenceRealm` conversions to/from `EPUBPreferences`, `PDFPreferences`, `EPUBSettings`, and `PDFSettings` into a Readium adapter file so `Models/` no longer imports `ReadiumNavigator` or `ReadiumShared`. Preserve Realm class names, persisted property names, primary keys, `objectTypes`, and migration references.
- **P2/A23 DatabaseService Force Unwrap Fixed (2026-06-18):** Converted `DatabaseService.realmConf` from `Realm.Configuration!` to `Realm.Configuration?` and `DatabaseService.realm` from `Realm!` to `Realm?`. Changed `setup()` from `try! Realm(configuration: conf)` to a `do/catch` that logs via `OSLog.Logger` and sets `realm = nil` on failure. All 5 callers (`CalibreBookManager`, `RealmServerRepository`, `RealmLibraryRepository`, `RealmBookRepository`, `RealmAnnotationRepository`) already had defensive `getRealm() -> Realm?` patterns that return nil on the main thread if `databaseService.realm` is nil — no changes needed there. The only caller code change was in `LibrarySearchService.getRealm()`, which passed `service.database.realmConf` directly to `Realm(configuration:)`; added a `guard let` unwrap with `URLError(.cannotConnectToHost)` fallback. Verified with `xcodebuild build` and full `xcodebuild test` (90 unit tests + 1 UI test green).
- **P2/A22 CalibreSearchCache Deprecated Properties Removed (2026-06-18):** Removed 4 deprecated `@Persisted` properties from `CalibreLibrarySearchObject` in `CalibreSearchCache.swift`: `generation`, `totalNumber`, `bookIds`, and `books: List<CalibreBookRealm>`. All were marked `@available(*, deprecated, message: "use sources")` and had zero callers after P1e deleted `CalibreBrowser.swift`. The modern `RealmSearchCacheStore` only accesses `searchObj.sources`, `searchObj.libraryId`, `searchObj.search`, `searchObj.sortBy`, `searchObj.sortAsc`, and `searchObj.filters`. Bumped Realm schema version from 139 to 140 by updating `ModelData.RealmSchemaVersion`, `CFBundleVersion` in both iOS and macOS Info.plist files (since `tryInitializeDatabase` derives the schema version from the build number), and added an `oldSchemaVersion < 140` migration block entry. Realm automatically drops the removed columns during migration. Verified with `xcodebuild build` and full `xcodebuild test` (90 unit tests + 1 UI test green).
- **P2/A19 Network Error Handling Audit Captured (2026-06-18):** Added `.agents/memory-bank/p2-a19-network-error-audit.md` after a thorough audit of the codebase. P1d-A04 introduced `CalibreAPIError` and the `validatedData`/`validatedDataPublisher`/`makeJSONRequest` shared helpers, but ~40 gaps remain across network files, managers, services, and views. Gaps are categorized by priority: 3 P0 (data corruption: `LibraryCategoryService` no HTTP check, `replaceError(with: CalibreBookEntry())` metadata corruption, `DSReaderHelperConnector` fire-and-forget Bool methods), 10 P1 (error mis-attribution + print noise: `LibrarySearchService` bypass, 4 `.mapError(\.asURLError)` publisher downgrades, `ImageRequest`/`CalibreServerTaskDelegate` print on every request), and 27 P2 (silent failures, compatibility debt, two parallel error taxonomies). The audit includes a 4-phase remediation plan for future execution.
- **P2/A13 Book.swift Deleted (2026-06-18):** Deleted the entire 378-line `Models/Book.swift` file. The file was not registered in the Xcode project (not in app or Catalyst targets) and was therefore never compiled. All 7 types defined in it (`ServerInfo`, `LibraryInfo`, `Library`, `Book`, `BookRealm`, `BookReadingPosition`, `BookDeviceReadingPosition` old duplicate) had zero external references — only self-references. The active types with similar names (`CalibreServerInfo`, `CalibreBookRealm`, `BookDeviceReadingPosition`) are defined elsewhere and are unrelated. Verified with `xcodebuild build` and full `xcodebuild test` (90 unit tests + 1 UI test green).
- **P2/A07 Providers.swift Dead Code Removed (2026-06-18):** Deleted 417 lines of unreachable/deprecated code from `Providers.swift` and `RealmModel.swift`. Removed: `FolioReaderRealmPreferenceProvider` (176 lines, replaced by `FolioReaderDelegatePreferenceProvider`, zero callers), `FolioReaderHighlightRealm` (68 lines, `@available(*, deprecated)`, not in Realm schema `objectTypes`), `FolioReaderYabrHighlightProvider` (51 lines, replaced by `FolioReaderDelegateHighlightProvider`, zero callers), `FolioReaderReadPositionRealm` (88 lines, `@available(*, deprecated)`, not in Realm schema `objectTypes`), and `extension FolioReaderHighlightRealm { toBookHighlightRealm }` in `RealmModel.swift` (30 lines, zero callers). `Providers.swift` shrank from 1095 to 708 lines, `RealmModel.swift` from 1387 to 1357. No Realm schema change needed since the deprecated Realm Object classes were already excluded from `objectTypes`. Verified with `xcodebuild build` and full `xcodebuild test` (90 unit tests + 1 UI test green).
- **P2/A10 ShelfDataManager Move Implemented (2026-06-18):** Moved `ShelfDataManager.swift` (containing `YabrShelfDataModel` and the `ModelData.registerRecentShelfUpdater()`/`parseShelfSectionId` helpers) from `Views/ShelfView/` to `Models/` via `xcode_XcodeMV`. Pure file move with no declaration changes; both app and Catalyst target memberships preserved. Verified with `xcodebuild build` and full `xcodebuild test` (87 unit tests + 1 UI test green). Resolves the A10 organization issue where a data manager class sat under the Views tree.
- **P2/A11 CalibreData Split Implemented (2026-06-18):** Decomposed the 1307-line `Models/CalibreData.swift` into ten focused files as a zero-behavior-change move: `Models/CalibreCoreModels.swift` (`CalibreServer`, `CalibreLibrary`, `CalibreBook`, `CalibreSyncStatus`), `Models/ReadingPositionModels.swift` (`BookDeviceReadingPosition`, `BookDeviceReadingPositionHistory`, statistics), `Models/CalibreHighlightStyle.swift` (`BookHighlightStyle` — only file in the split that imports UIKit), `Models/CalibreTasks.swift` (network/metadata task structs), `Models/CalibrePayloadModels.swift` (Codable API entry/result payloads), `Models/CalibreSyncModels.swift` (custom columns, category keys, probe/sync requests, cdb cmd list result), `Models/CalibrePluginModels.swift` (DSReader Helper / Count Pages / Goodreads Sync prefs + configuration), `Network/CalibreActivityModels.swift` (`CalibreActivity`/`Start`/`Finish`), `Models/CalibreServerConfigProvider.swift` (the protocol), and `Models/Array+Chunks.swift`. Deleted the original `CalibreData.swift`. All new files registered in both app and Catalyst targets via the Xcode project. Added `CalibreDataSplitTests` (20 cases) covering `CalibreServer`/`CalibreLibrary` identity and hashing, `CalibreBook.inShelfId`, `BookHighlightStyle` class/color mapping, plugin preference Codable decoding, custom-column snake_case CodingKeys, `Array.chunks`, and `CalibreActivity` class hierarchy. Full `xcodebuild test` green: 87 unit tests + 1 UI test. Catalyst build still blocked by a pre-existing SPM package product resolution issue (`R2Navigator`, `GCDWebServer`, `R2Shared`, `R2Streamer`) that is unrelated to the split.
- **P2/A11 CalibreData Split Plan Captured (2026-06-18):** Added `.agents/memory-bank/p2-calibredata-split-plan.md` after analyzing the 1307-line `Models/CalibreData.swift`. The recommended implementation is a zero-behavior split into focused files for core Calibre domain models, reading-position models, highlight style, network task DTOs, Codable payload models, sync/category models, plugin preference models, activity models, `CalibreServerConfigProvider`, and `Array.chunks(size:)`. Preserve type names, Codable keys, hashing/identity semantics, and Xcode target membership; defer semantic cleanup of `CalibreBook`, plugin fallback lookups, and legacy comments to later work.
- **P1f / MVVM Modernization Completed (2026-06-18):** Enforced clean MVVM pattern on the highest traffic views (`MainView`, `SettingsView`, `SupportInfoView`, `RecentShelfController`, and `SectionShelfController`) to resolve monolithic state trapping in SwiftUI view bodies and UIKit controllers. Introduced `MainViewModel`, `SettingsViewModel`, `SupportInfoViewModel`, `RecentShelfViewModel`, and `SectionShelfViewModel` with thorough `@MainActor`-isolated unit tests covering actions, state, and mutations. Consolidated LibraryInfo child-view commands into the existing V2 ViewModels. Verified the entire project compiles successfully and runs with 67 unit tests + 1 UI test green.
- **P1f/A08 Plan Captured (2026-06-18):** Added `.agents/memory-bank/p1f-main-viewmodel-plan.md` with the proposed plan for introducing ViewModels around remaining high-traffic views. The recommended order is `MainViewModel`, `SettingsViewModel`, LibraryInfo child-view command extraction, `SupportInfoViewModel`, then shelf ViewModels for `RecentShelfController` and `SectionShelfController`. Reader engine adapters remain out of first-pass scope unless acceptance criteria expand.
- **P1e Commit History Corrected (2026-06-17):** Rewrote the pushed `HEAD` commit message for the P1e migration so the history matches the actual change set. The corrected commit is `b15c0c1` (`refactor: complete V2 search/category migration and remove CalibreBrowser V1`) and replaces the stale PDF-oriented message previously attached to the same diff.
- **Agent Context Synced For P1e (2026-06-17):** Updated `AGENTS.md` search/category architecture notes to reflect the post-P1e reality: `ModelData` now owns `searchCacheRepository`, `librarySearchService`, `unifiedSearchService`, `libraryCategoryService`, and `unifiedCategoryService` directly, while `CalibreBrowser.swift` / `CalibreLibrarySearchManager` are historical references only. Revalidated with full `xcodebuild build` and `xcodebuild test` after the documentation fix.
- **P1e Implemented (2026-06-17):** Completed the hard cutover from `CalibreLibrarySearchManager` / `CalibreBrowser.swift` to the V2 search/category stack. `ModelData` now owns `RealmSearchCacheStore`, `LibrarySearchService`, `UnifiedSearchService`, `CategoryCacheRepository`, `LibraryCategoryService`, and `UnifiedCategoryService` directly. `UnifiedSearchViewModel`, `UnifiedCategoryViewModel`, `LibraryInfoView.ViewModel`, `CalibreLibraryManager`, and `YabrShelfDataModel` now resolve V2 services from `ModelData` instead of going through `librarySearchManager`. Moved shared search value types into `UnifiedSearchModels.swift`, moved request construction to `CalibreServerService+Search.swift`, deleted `CalibreBrowser.swift`, removed V1 unified category Realm models, and bumped the Realm schema to `139` with a one-time migration cleanup for deprecated unified category objects. Added `V2MigrationDependencyTests` and updated `UnifiedSearchIntegrationTests`. Full validation now passes with 55 unit tests + 1 UI test.
- **P1d-A04 Implemented (2026-06-17):** Refactored `CalibreServerService` into a shared core plus domain extensions. Added `CalibreAPIError` as the unified network error model and introduced shared helpers in `CalibreServerService.swift` for endpoint URL construction, JSON request creation, validated HTTP loading, publisher-based validated loading, and typed payload decoding. Split endpoint logic into `CalibreServerService+LibrarySync`, `+Metadata`, `+Discovery`, `+ReadingPosition`, and `+Annotations`. Preserved the existing facade surface while routing both async and Combine paths through the shared request/error layer where practical. Added `CalibreServerServiceTests` covering auth error mapping and 4xx payload-to-errmsg compatibility. Full validation now passes with 51 unit tests + 1 UI test.
- **P1d-A04 Implementation Plan Captured (2026-06-17):** The next refactoring target is `CalibreServerService.swift` (1436 lines). The approved direction is to introduce a unified `CalibreAPIError`, add a shared request execution/error-mapping layer, and split endpoint responsibilities by domain while preserving the existing facade API. Planned split targets are `CalibreServerService+LibrarySync`, `+Metadata`, `+Discovery`, `+ReadingPosition`, and `+Annotations`. The implementation order is: define error model, extract shared request helper, split read-only endpoints first, split write endpoints second, and only then collapse duplicated async/Combine paths onto shared internals.
- **Realm ReadingPosition Thread Crash Fixed (2026-06-17):** Resolved `Realm accessed from incorrect thread` in the `recent-shelf-updater` queue and in `UnifiedSearchServiceTests.testLimitExpansionIsolatesCriteria()`. Root cause was `RealmReadingPositionRepository` reusing `server.realmPerf` / `databaseService.realm` across threads. The repository now resolves a `Realm.Configuration` and opens or caches a Realm on the current thread. Added `ReadingPositionRepositoryThreadingTests` to verify main-thread writes plus background-queue reads. Full validation now passes with 49 unit tests + 1 UI test.
- **YabrPDFViewController Tests Added (2026-06-17):** Added `YabrPDFViewControllerTests` to cover controller coordination behavior: `open()`, `applyPreferences()`, default menu construction, `handleScaleChange()`, and `sharePDF()` for original/annotated exports. These tests were validated together with the Realm threading fix in the latest full `xcodebuild test` run.
- **P1c A14+A15 Confirmation (2026-06-17):** Reader theme/preferences abstraction (A15) is implemented through `ReaderEnginePreferences` and `ReaderEngineController.applyPreferences(_:)` across PDF, Readium, and FolioReader, with `YabrEBookReader.Coordinator` persisting engine-neutral preference updates back into each engine's storage model. Highlight/annotation abstraction (A14) is implemented for the shared model/storage path through `ReaderEngineHighlight`, `BookHighlight`, and `AnnotationRepository`; PDF and FolioReader also restore/apply highlights through their engine adapters. Caveat: `YabrReadiumReaderViewController.applyHighlights(_:)` is currently a protocol-complete stub, so Readium highlight UI rendering is not yet implemented.
- **YabrPDFViewController Responsibility Split (Commit `2f26f2f`, 2026-06-17):** Continued P1b-A03 by extracting PDF reader chrome, navigation/progress, reader options, selection menu, sharing, and margin-crop/blank-overlay responsibilities out of the main controller. `YabrPDFViewController.swift` is now 311 lines, with behavior moved into `YabrPDFViewController+Chrome`, `+Navigation`, `+Options`, `+Selection`, `+Sharing`, and `PDFMarginCropController`. Validation passed with `xcodebuild build` and `xcodebuild test` on iPhone 17 simulator: 40 unit tests + 1 UI test, 0 failures.
- **PDF Crash Fix Context:** The nil implicitly-unwrapped optional crash in `YabrPDFViewController.applyHighlights(_:)` was addressed by converting `annotationManager`, `bookmarkManager`, and `searchController` to lazy properties. This allows `YabrEBookReaderRepresentable.makeUIViewController(context:)` to call reader-engine APIs before `viewDidLoad` without dereferencing uninitialized managers.
- **Dismantling YabrPDFViewController (Milestone P1b-A03):** Extracted PDF Kit annotation (highlight), bookmark, and search responsibilities from `YabrPDFViewController` into `PDFAnnotationManager`, `PDFBookmarkManager`, and `PDFSearchController`. Added a native asynchronous "Search" tab to the PDF sidebar list (`YabrPDFSearchList` and `YabrPDFSearchListCell`).
- **AnnotationRepository (Milestone P0-3c):** Introduced `BookBookmark` and `BookHighlight` value types and `RealmAnnotationRepository` to decouple bookmarks and highlights from direct `RealmSwift` database logic. Ported Calibre annotation sync logic from `CalibreData.swift` into the repository.
- **UI Adapters Decoupling:** Refactored PDF adapter `YabrEBookReaderMetaSource.swift` and FolioReader/EPUB adapter `Providers.swift` to consume decoupled value types `BookBookmark` and `BookHighlight` instead of their Realm model counterparts, keeping UI adapters and rendering pipelines decoupled from Realm entities. Added Calibre serialization extensions directly to the value types.
- **CalibreBookManager getRealm() Optimization:** Optimized the `getRealm()` helper in `CalibreBookManager.swift` to check `Thread.isMainThread` and return the cached `databaseService.realm` directly, preventing expensive Realm instance allocation on the main thread and ensuring UI reactivity.
- **ServerRepository and LibraryRepository (Milestone P0-3b):** Decoupled `CalibreServerManager` and `CalibreLibraryManager` completely from Realm database logic. Created `RealmServerRepository` and `RealmLibraryRepository` to isolate CRUD operations for Calibre servers and libraries. Extracted all direct database references from the managers, resulting in the total removal of `import RealmSwift` from both managers.
- **ReadingPositionService Extraction (Milestone P0-1e):** Successfully migrated format preferences (`defaultFormat`, `formatReaderMap`, `formatList`), reader preference logic, manual progress syncing (`updateCurrentPosition`), and reading session helper methods from `ModelData` into `ReadingSessionManager`. Replaced `@Published var sessionManager` with a `lazy var` and established thread-safe change notification forwarding via Combine.
- **ModelData Main Thread Notification Forwarding:** Integrated `.receive(on: DispatchQueue.main)` on manager `objectWillChange` subscriptions in `ModelData.init` to ensure all SwiftUI state-propagation updates originating from nested managers are delivered on the main thread, eliminating runtime background thread update warnings.
- **Realm Thread-Safety Resolution (CalibreBookManager):** Resolved the fatal Realm thread verification crash by replacing direct main-thread `databaseService.realm` references with a thread-safe `getRealm()` helper, ensuring background thread queries dynamically initialize thread-safe Realm instances. Annotated `getBooksMetadata(request:)` with `@MainActor` to ensure UI state updates are published on the main thread.
- **CalibreBookManager Extraction (Milestone P0-1d):** Extracted book-related properties (`booksInShelf`, `booksAnnotation`, `selectedBookId`, `bookModelSection`, etc.) and their management/CRUD methods out of `ModelData` into a dedicated `CalibreBookManager`. Forwarded `bookManager.objectWillChange` to `ModelData` and maintained computed property delegates for legacy compatibility.
- **CalibreLibraryManager Extraction (Milestone P0-1b):** Extracted library-specific state (`calibreLibraries`, `calibreLibraryInfoStaging`, `librarySyncStatus`, `localLibrary`) and their management methods out of `ModelData` into a dedicated `CalibreLibraryManager`. Provided backward-compatible delegate properties and methods, and updated ViewModels (`LibraryViewModel`, `ServerViewModel`) to subscribe directly to `CalibreLibraryManager` properties.
- **CalibreServerManager Extraction (Milestone P0-1a):** Extracted server-specific state (`calibreServers`, `calibreServerInfoStaging`, `documentServer`) and their management methods out of `ModelData` into a dedicated `CalibreServerManager`. Resolved a lazy loading dependency cycle by dynamically resolving `calibreServerService` via `modelData`.
- **ModelData Forwarding Compatibility:** Re-added backward-compatible forwarding properties and methods to `ModelData` to ensure legacy Views, ViewModels, and Tests still compile without large diffs. All 41 tests are verified green.
- **Cleanup of Duplicates:** Removed duplicate physical folders (`Managers 2`, `Managers 3`, `Managers 4`) from the workspace.
- **Unified Search Modernization (Phase 1):** Introduced immutable domain value types (`UnifiedSearchResult`, `MergeOffset`, `LibrarySearchStatus`, `SearchError`) and decoupled the database via `SearchCacheRepository` and its concrete implementation `RealmSearchCacheStore`.
- **Unified Search Modernization (Phase 2):** Implemented in-memory K-way merging using Apple's `swift-collections` `Heap`.
- **Unified Search Modernization (Phase 3):** Introduced `UnifiedSearchManager` and `ActiveSearch` to orchestrate searches and merging in memory, and refactored `CalibreBrowser.swift` to delegate merging to `UnifiedSearchManager`.
- **Unified Search Modernization (Phase 4):** Refactored UI layer (Views and ViewModels) to consume `UnifiedSearchResult` via Combine, removing tight coupling to Realm objects like `CalibreUnifiedSearchObject`. Removed the $O(N)$ lookup `getMergedBookIndex()` logic in favor of straightforward array iteration.
- **Development Environment Migration:** Transitioning the project to an agent-first development workflow using the Google Antigravity CLI.
- **Architectural Shift (MVVM):** Transitioning from a single monolithic `@EnvironmentObject var modelData` to modular `@StateObject` ViewModels for complex views.
- **Unified Search Cache Model Refactoring:** Removed `@Persisted var books: List<CalibreBookRealm>` from `CalibreLibrarySearchValueObject`. Search result mapping in `RealmSearchCacheStore` now resolves the books array dynamically from `bookIds` using primary key lookups in `Realm`. `CalibreBrowser` was refactored to check resolved books count dynamically, completely eliminating the need for `books` list mutations and changeset observation. Bumped database schema version to `139` across all targets.
- **Context Guardrails:** Established the `.agents/memory-bank` directory to provide strict architectural guidelines, preventing subagents from hallucinating or deviating from the Swift Package Manager / SwiftUI architecture.
- **MCP Integration:** Moved Xcode toolchain configurations to `.agents/mcp_config.json` to allow the Antigravity CLI to autonomously interact with `xcodebuild` and the iOS Simulator.

## Active Tasks
- [x] 1. Expand `BookDetailViewModel.swift` to handle all data fetching (metadata, manifest) and UI state variables currently trapped in `BookDetailView.swift`.
- [x] 2. Break down the massive `BookDetailView.swift` (800+ lines) into smaller, manageable subcomponents (`BookDetailSubviews.swift`).
- [x] 3. Replace direct `ModelData` network calls inside `BookDetailView` with unidirectional data flow via `BookDetailViewModel`.
- [x] 4. Compile the project in the terminal using `xcodebuild` to ensure the SwiftUI refactoring doesn't break the build and unit tests pass.
- [x] 5. Successfully decoupled `ModelData` from all `BookDetailView` subviews (`BookCoverView`, `BookMetadataSection`, `BookConnectivitySection`, `BookFormatList`, `BookProgressSection`) by passing state through `BookDetailViewModel`.
- [x] 6. Decoupled `BookDownloadManager` from `BookDetailView` and `BookDetailSubviews` by mapping `activeDownloads` into `BookDetailViewModel` using Combine.
- [x] 7. Decoupled `ModelData` from `BookDetailView`'s state presentation stack, book conversion, and metadata status, proxying them through `BookDetailViewModel`. Unit tests added and verified.
- [x] 8. Migrated presentation state variables (`presentingReadingSheet`, `presentingPreviewSheet`, `activityListViewPresenting`, `readingPositionHistoryViewPresenting`) from `BookDetailView` to `BookDetailViewModel`, utilizing custom Binding wrappers in property observers.
- [x] 9. Decoupled `BookDetailView` entirely from `@EnvironmentObject var modelData`, utilizing dependency injection of `ModelData.shared` inside the ViewModel.
- [x] 10. Enforced MVVM architecture on Reading Position views (`ReadingPositionHistoryView` and `ReadingPositionDetailView`), decoupling them from `ModelData` and Realm queries via `ReadingPositionHistoryViewModel` and `ReadingPositionDetailViewModel`.
- [x] 11. Enforced MVVM architecture on Activity Log views (`ActivityList` and `ActivityDetailView`), extracting Realm queries and resolution logic to `ActivityListViewModel` and exposing plain `ActivityLogUIEntry` structures.
- [x] 12. Fixed the `recent-shelf-updater` background thread Realm concurrency crash by caching `realmPerf` via thread-local storage (`Thread.current.threadDictionary`).
- [x] 13. Completed Unified Search modernization Phase 1: Created `UnifiedSearchModels.swift`, `SearchCacheRepository.swift`, and `RealmSearchCacheStore.swift`, and successfully integrated them into Xcode project and verified build.
- [x] 14. Completed Unified Search modernization Phase 2: Created `UnifiedSearchMergeService` and heap-based iterators.
- [x] 15. Completed Unified Search modernization Phase 3: Created `UnifiedSearchManager` and integrated it with `CalibreBrowser` to perform backend merging in memory. Resolved the background thread Realm notification registration crash in `RealmSearchCacheStore` by utilizing a Combine `Deferred` publisher on the main queue and mapping managed entities to thread-safe value structs. Fixed the infinite loop feedback cycles by adding equality check guards on limitNumber changes and caching the last processed library source search results to prevent redundant merge operations when Realm updates book metadata. Resolved the main thread deadlock by replacing the synchronous `cacheRealmQueue.sync` call in `getMergedBookIndex` with a lightweight, non-blocking `NSRecursiveLock` to protect the runtime dictionaries. Fixed the limit expansion issue by forcing a Realm database refresh on the background queue before reading the updated limit value.
- [x] 16. Resolved the empty libraryIds limit expansion issue in Unified Search (where "All Books" view had empty libraryIds, resulting in 0 merged books and a totalNumber of 0, which blocked limit expansion) by resolving empty libraryIds to all active calibre libraries in UnifiedSearchManager and UnifiedSearchMergeService. Added unit test verification.
- [x] 17. Resolved the Realm write transaction deadlock between main thread limit expansion and background merge updates by converting UnifiedSearchManager mutators (`setLimit`, `expandLimit`, `resetSearch`, `updateLibraryStatus`) to run asynchronously on its serial queue, preventing the blocking of `cacheRealmQueue`. Added asynchronous unit test synchronization.

- [x] 18. Executed Phase 4 of Unified Search modernization: Migrated the UI and ViewModels (e.g., `LibraryInfoBookListView`, `ShelfDataManager`, `LibraryInfoViewModel`) to use the new `UnifiedSearchManager` and removed legacy Realm bindings. Validated through Xcode build.
- [x] 19. Resolved the unit test race condition crash in `MockSearchCacheRepository` using `NSRecursiveLock` and solved the infinite loop in `UnifiedSearchManager` by making `selectActiveSource` sorting deterministic, partially updating `CalibreBookRealm` objects to preserve reading progress, and deduplicating duplicate `CalibreUnifiedSearchObject` and `CalibreLibrarySearchObject` records in Realm on startup.
- [x] 20. Execute Phase 5 of Unified Search modernization: Cleanup, final testing, and deletion of `CalibreUnifiedSearchObject` from the Realm Schema.
- [x] 21. Resolved the issue where changing search criteria (or clicking the manual refresh button) did not trigger search/network actions, by wiring `searchTriggerHandler` and updating `refreshSearchResults` to dynamically initialize and check cache for new search keys.
- [x] 22. Implemented comprehensive test coverage for the Unified Search subsystem, including unit tests for criteria isolation, pagination, sorting stability, and end-to-end integration tests using MockURLProtocol.
- [x] 23. Decouple `CalibreServerService` and remaining `ModelData` dependencies.
- [x] 24. Resolved Realm collection insertion exception (`RLMThrowCollectionException`) in `RealmSearchCacheStore.saveLibrarySourceResult` by creating/updating objects via `realm.create(_:value:update:)` with the `.modified` update policy. Resolved the testing deadlock under Swift Concurrency by aligning the Hashable contract of `CalibreServer` / `CalibreServerURLSessionKey` and migrating `UnifiedSearchServiceTests` to non-blocking `await fulfillment(of:timeout:)` expectations.
- [x] 25. Resolved all Code Review findings on commit `649cbc55`. Eliminated `DispatchQueue.main.sync` deadlocks in `CalibreServerService` and `UnifiedSearchService` via thread-safe configuration caching and async methods. Cleaned up debug print statements, dead variables, safely unwrapped `ModelData.shared`, deleted the orphaned `ActiveSearch.swift`, and verified that all 31 unit tests pass.
- [x] 26. Resolved the fatal nil-unwrap crash in `testEmptyLibraryIdsMergingAndExpansion` (Task 232) by removing redundant `ModelData` initialization and overrides in tests, since the mock library provider handles library lists.
- [x] 27. Decoupled `libraryStatuses` from `UnifiedSearchResult` by updating `UnifiedSearchService` to stream `SearchUpdate` value types and `UnifiedSearchViewModel` to publish statuses separately.
- [x] 28. Refactored search criteria and UI preferences to `LibraryInfoView.ViewModel`, removing criteria properties from `UnifiedSearchViewModel` and passing `SearchCriteriaMergedKey` to `startSearch(key:)` explicitly.
- [x] 29. Fixed the Combine bridge task leak in `publisher(for:)` by wrapping the subscription block inside `Deferred`.
- [x] 30. Bypassed the autoUpdate check in category fetching inside `ModelData.syncLibrary(request:)` to ensure calibre library categories are synced and populated on startup/probes even when autoUpdate ("Available when Offline") is disabled.
- [x] 31. Decoupled `LibraryInfoCategoryListView` and `LibraryInfoView` completely from `RealmSwift` by introducing `CategoryCacheSummary` value types and querying summaries via `CategoryCacheRepository` reactively inside the `LibraryInfoView.ViewModel`.
- [x] 32. Decoupled `LibraryInfoCategoryItemsView` completely from `RealmSwift` by implementing cache invalidation in the repository layer and exposing `forceRefreshCategory` in the `UnifiedCategoryViewModel`.
- [x] 33. Decoupled `BookDetailView` completely from `RealmSwift` by shifting Realm database queries and reactive observations to `BookDetailViewModel`.
- [x] 34. Fixed EnvironmentObject propagation crash in `LibraryInfoCategoryItemsView` and `LibraryInfoView` by explicitly injecting the `libraryInfoViewModel` environment object to `LibraryInfoBookListView`.
- [x] 35. Refactored ServerView components (`AddModServerView`, `ServerDetailView`, `ServerOptionsDSReaderHelper`, `LibraryDetailView`) to MVVM using two view models (`ServerViewModel` and `LibraryViewModel`), completely decoupling `LibraryDetailView` from Realm.
- [x] 36. Fixed `libraryRowBuilder` sync status UI in `ServerDetailView.swift` to display mutually exclusive status text instead of overlapping states.
- [x] 37. Refactored `ReaderOptionsView.swift` to strictly enforce the MVVM pattern by delegating preferred formats, reader types, and font import/deletion state to `ReaderOptionsViewModel`.
- [x] 38. Corrected the FolioReader font size mapping alignment with unified preferences (index 3/"20px" as 100%).
- [x] 39. Implemented all 5 performance optimization fixes to eliminate Realm database hangs and UI freezes when opening `BookDetailView`.
- [x] 40. Continued P1b-A03 by splitting `YabrPDFViewController` into dedicated extension files for chrome, navigation/progress, options, selection, and sharing, plus `PDFMarginCropController` for margin crop and blank overlay handling. Committed as `2f26f2f`.
- [x] 41. Confirmed P1c A14+A15 status: A15 theme/preferences abstraction is complete across PDF, Readium, and FolioReader; A14 highlight abstraction/storage is complete for shared model plus PDF/FolioReader application, with Readium highlight UI rendering still explicitly stubbed.
- [x] 42. Added `YabrPDFViewControllerTests` for core controller coordination behavior and verified them under `xcodebuild test`.
- [x] 43. Fixed `RealmReadingPositionRepository` cross-thread Realm reuse and added `ReadingPositionRepositoryThreadingTests`; full test suite now passes again.
- [x] 44. Execute P1d-A04: refactor `CalibreServerService` by introducing `CalibreAPIError`, extracting a shared request/error-mapping layer, and splitting endpoint logic by domain while preserving facade compatibility. Added `CalibreServerServiceTests` and verified with `xcodebuild test` (51 unit tests + 1 UI test).
- [x] 45. Execute P1e: promote V2 search/category services to `ModelData`, remove `CalibreLibrarySearchManager` / `CalibreBrowser.swift`, delete V1 unified category Realm models, add migration cleanup for schema `139`, and verify the direct V2 dependency graph with `V2MigrationDependencyTests`. Full validation now passes with 55 unit tests + 1 UI test.
- [x] 46. Execute P2/A11: split `Models/CalibreData.swift` (1307 lines) into ten focused model/payload/task/plugin/activity files, delete the original file, register all new files in both app and Catalyst targets, and add `CalibreDataSplitTests` (20 cases) covering identity/hashing, `inShelfId`, `BookHighlightStyle` mapping, plugin preference Codable decoding, custom-column CodingKeys, `Array.chunks`, and `CalibreActivity` hierarchy. Full validation green: 87 unit tests + 1 UI test.
- [x] 47. Execute P2/A10: move `ShelfDataManager.swift` from `Views/ShelfView/` to `Models/` via `xcode_XcodeMV`, preserving both app and Catalyst target memberships and making no declaration changes. Verified with `xcodebuild build` and full `xcodebuild test` (87 unit tests + 1 UI test green).
- [x] 48. Execute P1/A09: remove `DispatchQueue.main.sync` and redundant `URLCredentialStorage.shared.set(...)` from `DSReaderHelperConnector.urlSession`; rely on `CalibreServerTaskDelegate` for auth. Added `DSReaderHelperConnectorTests` (3 cases) for background-thread safety. Full validation green: 90 unit tests + 1 UI test.
- [x] 49. Execute P2/A07: delete 417 lines of dead/deprecated code from `Providers.swift` (`FolioReaderRealmPreferenceProvider`, `FolioReaderHighlightRealm`, `FolioReaderYabrHighlightProvider`, `FolioReaderReadPositionRealm`) and `RealmModel.swift` (`extension FolioReaderHighlightRealm`). No Realm schema change. Full validation green: 90 unit tests + 1 UI test.
- [x] 50. Execute P2/A13: delete the entire 378-line `Models/Book.swift` (not registered in Xcode project, never compiled, zero external references to any of its 7 types). Full validation green: 90 unit tests + 1 UI test.
- [x] 51. Execute P2/A22: remove 4 deprecated `@Persisted` properties from `CalibreLibrarySearchObject` in `CalibreSearchCache.swift`. Bump Realm schema version 139→140 (update `ModelData.RealmSchemaVersion`, `CFBundleVersion` in both Info.plist files, and migration block). Full validation green: 90 unit tests + 1 UI test.
- [x] 52. Execute P2/A23: convert `DatabaseService.realmConf` and `realm` from force-unwrapped optionals (`!`) to regular optionals (`?`), replace `try!` in `setup()` with `do/catch` + `OSLog.Logger` error logging. Fix `LibrarySearchService.getRealm()` to guard-unwrap `realmConf`. Full validation green: 90 unit tests + 1 UI test.
- [x] 53. Execute FolioReader Highlight Position Restore Fix (2026-06-23): call `encodeContents()` in `BookHighlight.toFolioReaderHighlight()`, route `EpubFolioReaderContainer.applyHighlights` through the protocol method to lazily create the provider, add `ensureEncoded` last-line guard. Added 4 regression tests in `FolioReaderProviderBookIdTests`. Full validation green: 191 tests.
- [x] 53. Execute P2/A12+A24: split monolithic `RealmModel.swift` into individual, focused model files under `Models/Realm/`, relocate `FolioReaderPreferenceRealm` from views to models, and extract Readium preference mappings from `ReadiumPreferenceRealm` into the rendering layer `ReadiumPreferenceAdapter.swift` to decouple the core model layer from Readium dependencies. Full validation green: 90 unit tests + 1 UI test.
- [x] 54. Execute P2/A19 Stage A19-S1: route calibre category HTTP requests in `LibraryCategoryService` through `validatedData` to verify HTTP status codes, preserve local scheme fallback, and add a status failure test to verify cache safety. Full validation green: 91 unit tests + 1 UI test.
- [x] 55. Execute FolioReader Position Restore Race Fix (2026-06-23): make `RealmReadingPositionRepository.savePosition(_:forBookId:)` replace older same-identity positions atomically within one Realm write transaction, preventing FolioReaderKit reload reads from observing a transient empty position set. Added focused repository/provider/concurrency regression tests. Full `xcodebuild test` validation green.
- [x] 56. Execute repository Combine output removal (2026-07-02): converted repository observer APIs in `BookRepository`, `LibraryRepository`, `AnnotationRepository`, `CategoryCacheRepository`, and `RealmSearchCacheStore` from Combine publishers to `AsyncStream`, deleted the unused search-cache publisher API, and migrated production consumers (`BookDetailViewModel`, `ActivityListViewModel`, `LibraryViewModel`, `LibraryInfoView.ViewModel`, `UnifiedCategoryViewModel`) to cancellable tasks. Realm observations now use `Results.observe(...)` with token invalidation on stream termination. Focused validation green for `RealmBookRepositoryTests`, `BookDetailViewModelTests`, `LibraryViewModelTests`, `UnifiedCategoryServiceTests`, `UnifiedSearchServiceTests`, and `V2MigrationDependencyTests`; repository/protocol Combine scan is clean; `git diff --check` and the standard iOS Simulator build both pass.

## Completed Refactoring Milestones
- [x] Extract `CalibreServerManager` out of `ModelData` (Milestone P0-1a).
- [x] Extract `CalibreLibraryManager` out of `ModelData` (Milestone P0-1b).
- [x] Extract `CalibreBookManager` out of `ModelData` (Milestone P0-1d).
- [x] Resolve Realm thread safety crash and main-thread publishing in CalibreBookManager.
- [x] Decouple category views completely from RealmSwift.
- [x] Decouple BookDetailView completely from RealmSwift.
- [x] Refactor ServerView components to MVVM.
- [x] Fix libraryRowBuilder sync status UI representation.
- [x] Refactor ReaderOptionsView to MVVM and register project files.
- [x] Extract ReadingPositionService (Milestone P0-1e) into ReadingSessionManager.
- [x] Introduce BookRepository (Milestone P0-3a) to encapsulate book database operations and remove presentation layer Realm dependency.
- [x] Decouple bookmarks and highlights (annotations) into AnnotationRepository (Milestone P0-3c) and refactor reader UI adapters.
- [x] Consolidate reading position saving logic across Readium, FolioReader, and PDF engines (Milestone P1a-A06).
- [x] Unify reader theme/preferences abstraction across PDF, Readium, and FolioReader (Milestone P1c-A15).
- [x] Unify reader highlight/annotation model and storage abstraction, with Readium UI rendering still tracked as a remaining implementation caveat (Milestone P1c-A14).
- [x] Dismantle PDF God VC by extracting Annotation, Bookmark, Search, Margin Crop, Chrome, Navigation, Options, Selection, and Sharing responsibilities (Milestone P1b-A03).
- [x] Correct FolioReader font size mapping discrepancy and align with unified preferences.
- [x] Mitigate Realm database hangs/UI freezes when opening BookDetailView (Fixes 1-5).
- [x] Remove books property from CalibreLibrarySearchValueObject and dynamically resolve books list.
- [x] Split `Models/CalibreData.swift` into ten focused files (Milestone P2/A11) and add boundary tests for identity, Codable, and style mappings.
- [x] Move `ShelfDataManager.swift` from `Views/ShelfView/` to `Models/` (Milestone P2/A10), preserving both app and Catalyst target memberships.
- [x] Fix `DSReaderHelperConnector.urlSession` thread safety by removing `DispatchQueue.main.sync` and redundant credential storage (Milestone P1/A09).
- [x] Remove ~417 lines of dead/deprecated code from `Providers.swift` and `RealmModel.swift` (Milestone P2/A07).
- [x] Delete the entire 378-line `Models/Book.swift` (not compiled, zero external references) (Milestone P2/A13).
- [x] Remove 4 deprecated `@Persisted` properties from `CalibreLibrarySearchObject` and bump Realm schema to 140 (Milestone P2/A22).
- [x] Convert `DatabaseService` force-unwrapped Realm properties to optionals and replace `try!` with error handling (Milestone P2/A23).
- [x] Fix FolioReader highlight position restore by calling `encodeContents()` in `BookHighlight.toFolioReaderHighlight()`, routing `EpubFolioReaderContainer.applyHighlights` through the protocol method, and adding `ensureEncoded` last-line guard (FolioReader Highlight Position Restore fix).
- [x] Split monolithic `RealmModel.swift` (1357 lines) into individual files under `Models/Realm/` and decouple model layer from Readium Navigator/Shared dependencies (Milestone P2/A12+A24).
- [x] Route calibre category HTTP fetches in `LibraryCategoryService` through `validatedData` and implement safety validation tests (Milestone P2/A19 Stage A19-S1).
- [x] Modernize Combine publishers for annotations, library sync, reading positions, and raw metadata to return `CalibreAPIError` and add deprecated adapters (Milestone P2/A19 Stage A19-S5).
- [x] Make annotations/position upload and task-builder failures visible by introducing throwing builders, error properties on task structures, and modern publishers (Milestone P2/A19 Stage A19-S6).
- [x] Shelf View Wood Theme Alignment - Stage S0: Freeze baseline screenshots and discrepancy checklists.
- [x] Shelf View Wood Theme Alignment - Stage S1: Establish Pure Layout & Presentation Contract.
- [x] Shelf View Wood Theme Alignment - Stage S2: Implement Reusable Shelf Tile Shell.
- [x] Shelf View Wood Theme Alignment - Stage S3: Migrate Cover, Fallback, and Spine.
- [x] Shelf View Wood Theme Alignment - Stage S4: Migrate Progress, Status, Options, and Edit Overlay.
- [x] Shelf View Wood Theme Alignment - Stage S5: Switch Recent Continuous Grid.
- [x] Shelf View Wood Theme Alignment - Stage S6: Handle Recent Safe Area, Tab Bar, and Edit Toolbar.
- [x] Shelf View Wood Theme Alignment - Stage S7: Switch Discover Header and Horizontal Shelf Row.
- [x] Shelf View Wood Theme Alignment - Stage S8: State Matrix and Interactive Regression.
- [x] Shelf View Wood Theme Alignment - Stage S9: Visual Acceptance and Full Verification.


## Active Constraints
- **Do NOT** introduce CocoaPods or modify workspace files; the project relies entirely on Swift Package Manager.
- **Decoupling Goal:** Views should minimize direct dependency on `ModelData` for network operations; logic should reside in dedicated ViewModels.
